/* function auto-generated using seed: 7282072 */
void f102(int x, int y){
	int z = 1;
	y = z * x;

	if(x > z){
		z = x * z;
		y = y * y;
		x = y * y;
	}

	x = z - y;

	if(z >= x){
		z = x + z;
	}

	z = x * z;

	if(z != y){
		z = y * x;
	}
	else{
		z = y - x;

		if(x > z){
			x = x + z;

			if(z == y){
				z = x - y;
				y = x + z;
				z = x - x;
			}

			z = x - z;
			z = x * x;

			if(z >= z){
				x = y + z;
				y = z - z;
				z = x + x;
			}

		}

		z = x * y;
	}


	printf("%d", z);
}
