/* function auto-generated using seed: 6600828 */
void f104(int x, int y){
	int z = 1;
	z = y + y;

	if(y != y){
		y = x + x;

		if(x != z){
			y = y - x;
			y = z + x;

			if(x >= x){
				z = y + x;
				x = z * z;
				y = z * z;
			}

			y = x + y;

			if(y >= y){
				z = z * y;
			}

		}

	}


	printf("%d", z);
}
