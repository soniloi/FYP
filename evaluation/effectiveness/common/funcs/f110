/* function auto-generated using seed: 1869210 */
void f110(int x, int y){
	int z = 1;
	z = y + y;

	if(y == y){
		z = x - x;
	}
	else{
		x = z - z;
	}

	z = x * x;

	if(y < z){
		z = z - y;
	}
	else{
		y = y + z;

		if(y <= y){
			y = x * z;
			y = y + z;
		}
		else{
			y = z + y;

			if(z == x){
				z = x + x;
			}

			y = y - y;
		}

	}


	printf("%d", z);
}
