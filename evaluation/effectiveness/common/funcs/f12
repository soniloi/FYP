/* function auto-generated using seed: 9900732 */
void f12(int x, int y){
	int z = 1;
	x = x + x;

	if(z < z){
		z = y * y;

		if(z <= z){
			z = y * x;
		}
		else{
			z = z + x;
			x = y - y;
			z = y - z;

			if(z >= x){
				y = y * x;
				z = z - x;
			}

		}

		x = z * z;

		if(x != y){
			y = x - y;

			if(x == x){
				y = x + x;
				x = z * y;
				y = x + z;
			}
			else{
				z = y * y;
				z = x * z;
				y = z - x;
			}

			z = x + z;
		}
		else{
			x = y * y;
		}

		x = x - x;
	}
	else{
		z = x - y;
	}


	printf("%d", z);
}
