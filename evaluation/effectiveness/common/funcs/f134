/* function auto-generated using seed: 5086235 */
void f134(int x, int y){
	int z = 1;
	x = z + y;

	if(y < z){
		y = x * x;

		if(y > x){
			z = y * z;

			if(y <= z){
				x = z - y;
			}

			z = x - y;
		}

	}
	else{
		x = y * y;
		x = y * y;
		x = y - x;

		if(x < z){
			x = z * y;
			y = y - z;
		}

	}


	printf("%d", z);
}
