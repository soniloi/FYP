/* function auto-generated using seed: 1514383 */
void f137(int x, int y){
	int z = 1;
	x = z + x;

	if(x <= x){
		y = x - x;

		if(x > y){
			y = y * y;

			if(z != z){
				y = y * y;
			}
			else{
				x = x + z;
				x = y + x;
			}

			z = y * y;
			z = x * x;
		}
		else{
			x = x * x;

			if(z > z){
				z = y * y;
			}

		}

		y = z * y;
		y = x * z;

		if(x > z){
			x = x - x;
		}

	}
	else{
		y = x - y;
		x = y - y;
	}


	printf("%d", z);
}
