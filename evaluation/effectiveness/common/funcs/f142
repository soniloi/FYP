/* function auto-generated using seed: 3431709 */
void f142(int x, int y){
	int z = 1;
	z = z - x;

	if(x < x){
		z = z * y;
	}

	z = z * x;

	if(x != y){
		z = z * y;

		if(y >= y){
			z = y * z;
			x = y - y;
		}

	}
	else{
		y = x * x;

		if(x <= z){
			x = x * z;
			z = z * z;
			x = y - z;

			if(x == y){
				z = x * z;
				x = x - y;
				x = z * z;
			}

		}

	}


	printf("%d", z);
}
