/* function auto-generated using seed: 3380171 */
void f143(int x, int y){
	int z = 1;
	z = z - z;

	if(z == y){
		y = z + z;

		if(y <= x){
			z = z * y;

			if(y >= y){
				y = y * y;
			}

			x = x + z;

			if(y > x){
				z = x + y;
				x = x + y;
			}
			else{
				y = z - z;
				x = y * x;
				x = y * x;
			}

		}
		else{
			z = x - z;
			z = z - y;
			x = y * z;
		}

		y = x + z;

		if(y == x){
			x = x + z;
			y = x * x;

			if(z == x){
				z = z - y;
				z = x - x;
				y = z * z;
			}
			else{
				z = y * x;
			}

		}
		else{
			y = x * y;

			if(z >= z){
				x = y * z;
			}

			z = z * y;

			if(x >= y){
				y = y + z;
				z = y - x;
				x = y * z;
			}
			else{
				x = y - x;
				y = z - z;
			}

			z = z * x;

			if(x < y){
				y = x * x;
				z = x - y;
			}

		}

	}
	else{
		x = z - x;

		if(y <= z){
			z = x * x;
			z = x - x;

			if(x >= y){
				x = y + y;
				z = z - y;
			}

		}

		x = x - y;
		x = y - y;
	}


	printf("%d", z);
}
