/* function auto-generated using seed: 5283889 */
void f147(int x, int y){
	int z = 1;
	z = y + z;

	if(x > x){
		z = y - x;
		z = x - y;
	}
	else{
		z = x - z;
	}

	x = z + y;

	if(z >= z){
		y = z * z;
		x = z * z;
		y = y + z;

		if(z >= y){
			y = z - y;
			y = x + z;

			if(x >= y){
				z = x - x;
				y = x * y;
				x = x + y;
			}
			else{
				y = x - x;
			}

		}

	}

	x = y * x;

	if(y > y){
		y = x - y;
	}
	else{
		x = z + z;

		if(z != x){
			x = y - z;
			x = x * x;
			y = z - x;
		}
		else{
			x = y + z;
			x = y - x;
		}

		x = y - x;
	}


	printf("%d", z);
}
