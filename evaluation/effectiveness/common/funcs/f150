/* function auto-generated using seed: 8331176 */
void f150(int x, int y){
	int z = 1;
	z = z - x;

	if(x >= z){
		y = z * x;

		if(y > z){
			z = y + x;
			y = z - z;
			y = x + x;

			if(y >= x){
				x = z * x;
			}
			else{
				y = z + x;
				y = y - x;
				x = z + y;
			}

		}

	}

	x = x - z;

	if(z > x){
		x = x - y;
		x = x - x;

		if(y <= z){
			x = y - z;
			x = z * y;
		}

		y = x * y;

		if(x <= x){
			y = y + x;

			if(y == z){
				x = z - z;
				y = y * x;
				y = x + x;
			}

			z = y * z;
		}
		else{
			y = y * x;

			if(y < z){
				z = y * x;
			}

			y = x + x;
			y = y * z;
		}

	}
	else{
		x = z - y;
		y = x - x;

		if(x >= y){
			z = z + x;
			y = x * z;
		}
		else{
			y = x * z;

			if(x < y){
				y = y * z;
			}
			else{
				z = x + x;
				y = y + z;
				z = y - y;
			}

		}

	}


	printf("%d", z);
}
