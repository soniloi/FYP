/* function auto-generated using seed: 3868246 */
void f153(int x, int y){
	int z = 1;
	y = x * y;

	if(z == y){
		y = z + x;
		z = z + z;

		if(z >= z){
			z = z + y;

			if(y != z){
				y = z + x;
				x = y - z;
				z = y + z;
			}
			else{
				z = x - y;
				y = y - z;
			}

			z = x + z;
		}
		else{
			z = x + y;

			if(x != x){
				y = x * z;
				x = z + y;
			}

		}

	}


	printf("%d", z);
}
