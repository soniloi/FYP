/* function auto-generated using seed: 1700462 */
void f155(int x, int y){
	int z = 1;
	x = z - z;

	if(z >= x){
		x = y - y;

		if(y == z){
			x = x * x;
		}

		x = x - z;

		if(z != x){
			z = z - z;
			y = x - z;
			x = y + x;

			if(z == x){
				y = y * x;
			}

		}

	}
	else{
		z = x + z;
		y = z - z;

		if(z <= y){
			y = z * y;
			y = y + y;
		}

	}


	printf("%d", z);
}
