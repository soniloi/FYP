/* function auto-generated using seed: 6611574 */
void f157(int x, int y){
	int z = 1;
	x = y - z;

	if(y <= x){
		x = x + x;

		if(z > y){
			y = x - y;

			if(z <= y){
				z = z * x;
				z = x * y;
			}

		}
		else{
			z = x * y;
			z = x + z;
		}

		x = y * x;

		if(x == x){
			y = y - z;

			if(y >= z){
				y = z * x;
				z = x + z;
				y = x * x;
			}

			y = x - z;
			y = z + x;
		}
		else{
			y = y + z;
			z = y * y;

			if(y != y){
				y = x * x;
				x = y - y;
			}

		}

	}
	else{
		x = x + y;
	}


	printf("%d", z);
}
