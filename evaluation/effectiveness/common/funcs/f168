/* function auto-generated using seed: 765671 */
void f168(int x, int y){
	int z = 1;
	y = z + x;

	if(x == z){
		x = y + z;

		if(z >= z){
			x = z * x;
		}
		else{
			y = x * z;

			if(z != z){
				x = x + x;
				x = x + y;
			}
			else{
				z = z * z;
			}

		}

		z = x - y;

		if(y <= x){
			z = y + y;
			z = y - z;
			y = z - x;
		}

	}

	x = x + z;

	if(x > z){
		z = x + z;
		z = z * y;

		if(z == x){
			z = x + x;
			x = y - x;
		}
		else{
			z = z - z;

			if(y > z){
				y = z + x;
				x = y * y;
				y = y - x;
			}
			else{
				x = x * y;
				x = z * y;
			}

		}

	}

	z = x * x;

	if(y != x){
		x = y - x;
	}
	else{
		z = x - y;
		y = z - x;

		if(z >= z){
			z = z + y;

			if(x >= y){
				z = z + x;
				y = y * y;
			}
			else{
				z = x + y;
				y = z * y;
				y = x * z;
			}

			z = y * x;

			if(x < x){
				z = x - z;
			}
			else{
				y = z * x;
			}

			y = y * y;

			if(x >= z){
				x = x * y;
				x = z - z;
				y = y - y;
			}
			else{
				z = z - y;
				z = y * x;
			}

		}
		else{
			y = z * y;

			if(x <= y){
				y = x - z;
				x = z + y;
			}
			else{
				y = y - x;
				z = x - y;
			}

			z = z - z;
		}

	}


	printf("%d", z);
}
