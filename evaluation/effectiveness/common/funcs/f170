/* function auto-generated using seed: 9709300 */
void f170(int x, int y){
	int z = 1;
	y = y - y;

	if(z == x){
		x = x + z;
	}
	else{
		z = y * x;
		y = z * z;

		if(z <= x){
			z = x + z;
		}
		else{
			x = x + z;
			x = x + x;
			y = z - y;

			if(z < y){
				x = x * x;
				x = x + x;
				z = x * z;
			}

		}

		x = z + y;

		if(x <= z){
			x = y - x;
			y = z - y;

			if(z >= z){
				x = z - x;
			}

			x = z * z;
		}
		else{
			y = x - y;
			z = z - x;
		}

	}

	x = y + z;

	if(y >= x){
		x = z + y;

		if(z >= z){
			y = y + y;
		}

		x = z * x;

		if(z <= x){
			x = x - x;
			y = z - x;
			y = z + z;

			if(z == z){
				y = x + z;
				y = z + x;
			}
			else{
				y = x - y;
				z = x * y;
			}

		}

		x = x * x;

		if(z == y){
			z = y * z;
		}

	}


	printf("%d", z);
}
