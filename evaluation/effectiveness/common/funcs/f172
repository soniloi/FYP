/* function auto-generated using seed: 1884301 */
void f172(int x, int y){
	int z = 1;
	x = x - z;

	if(x != y){
		z = z * z;
		z = x - z;
	}
	else{
		x = z - x;
		x = x + y;

		if(z <= z){
			y = x + x;

			if(y >= z){
				x = z * y;
				z = z + z;
				x = x - x;
			}

			y = x + y;

			if(z != z){
				x = z - y;
				z = z + y;
				z = z - z;
			}
			else{
				y = x - z;
				z = y - x;
				z = y * x;
			}

		}

	}

	z = z + y;

	if(y != y){
		x = y * z;
		z = y - z;

		if(z < x){
			z = z * x;

			if(z > y){
				z = y * x;
			}
			else{
				y = x + z;
				y = z * x;
				z = z * x;
			}

			x = y - y;

			if(x == x){
				x = x - x;
				y = x * y;
				y = y + x;
			}

			y = x + z;

			if(x < z){
				z = x + y;
			}

		}

		y = z - z;

		if(z >= z){
			z = x - x;

			if(x > z){
				y = z * z;
				y = x + y;
			}
			else{
				z = z * x;
			}

			z = y - y;
		}
		else{
			x = z * y;

			if(y <= x){
				z = y * z;
			}

			z = z - y;
			z = x - z;

			if(y > y){
				z = y + x;
			}
			else{
				x = x * x;
				x = x * x;
			}

		}

	}
	else{
		y = z * x;
		z = y - y;

		if(y > z){
			x = z - x;
		}

	}


	printf("%d", z);
}
