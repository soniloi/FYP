/* function auto-generated using seed: 5093442 */
void f173(int x, int y){
	int z = 1;
	x = y - z;

	if(z != y){
		x = z - y;

		if(y < x){
			y = x * y;
		}
		else{
			x = y * x;

			if(y != x){
				z = z + x;
				y = y + z;
			}

			z = x + z;
		}

		x = z * y;
		z = x + x;
	}


	printf("%d", z);
}
