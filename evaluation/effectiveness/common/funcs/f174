/* function auto-generated using seed: 2088359 */
void f174(int x, int y){
	int z = 1;
	y = y * z;

	if(x < z){
		y = z + z;
		x = x - z;
		x = x - y;

		if(y >= y){
			z = y - y;
			z = y - z;
			y = y * x;

			if(z < y){
				y = x * z;
				x = y * y;
			}
			else{
				z = x + y;
			}

		}

	}

	z = y + y;

	if(y >= x){
		x = y - z;

		if(z == y){
			z = y - y;
			x = z + y;
		}

		y = y - y;

		if(z <= z){
			y = z * y;
			x = y * y;
			z = y - z;
		}
		else{
			x = z + x;

			if(z != x){
				x = y + y;
			}

			y = y - x;
		}

	}
	else{
		x = x + z;
		y = x * x;
		x = y - z;

		if(x <= y){
			y = y - z;
			x = y * x;
		}
		else{
			y = y - x;
		}

	}

	z = x * y;

	if(x <= x){
		y = y - y;
		y = x + x;
	}


	printf("%d", z);
}
