/* function auto-generated using seed: 7708371 */
void f176(int x, int y){
	int z = 1;
	z = y * z;

	if(y < z){
		y = x - y;

		if(y == y){
			y = x - y;

			if(z != y){
				x = x + x;
				x = z - x;
			}

			x = x + y;
			x = z - x;

			if(z <= y){
				z = z * y;
				x = z * x;
				x = x + x;
			}
			else{
				y = y - z;
				y = x + x;
				z = x * y;
			}

		}

		x = z - y;

		if(x < z){
			z = y - x;
		}

	}

	z = x - x;

	if(z <= y){
		y = z + z;
	}


	printf("%d", z);
}
