/* function auto-generated using seed: 4536574 */
void f177(int x, int y){
	int z = 1;
	z = y - x;

	if(y < x){
		y = x * z;
		x = y + x;
	}
	else{
		y = y + z;

		if(z <= x){
			z = x + x;
			z = x * x;
		}
		else{
			y = x - z;

			if(x < z){
				z = z - x;
			}
			else{
				y = y - z;
			}

		}

	}

	y = y - y;

	if(x >= y){
		z = x * y;

		if(y > x){
			y = z * x;
			x = x + y;
			x = z - z;

			if(x > y){
				x = z * y;
			}
			else{
				y = x * y;
				x = y + x;
			}

		}

		y = y - y;
	}
	else{
		x = z + z;
	}


	printf("%d", z);
}
