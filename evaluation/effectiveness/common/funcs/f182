/* function auto-generated using seed: 5439135 */
void f182(int x, int y){
	int z = 1;
	x = z - z;

	if(x == z){
		z = x * x;

		if(x <= y){
			z = z * z;
		}

		z = x + z;
		x = x + z;

		if(x <= y){
			y = x * x;
		}
		else{
			x = y - y;

			if(x == x){
				z = z + y;
				z = z + z;
			}
			else{
				z = z + z;
				x = z + y;
				y = z * x;
			}

			y = x + z;
		}

	}

	y = y * z;

	if(x > y){
		y = y * x;
		x = z + y;

		if(y != x){
			x = x * y;
			z = x - y;
		}
		else{
			z = y * x;
		}

	}
	else{
		x = x - z;
		y = y + y;

		if(z <= z){
			y = z + x;

			if(x != x){
				y = x * z;
				z = x * z;
				y = y + x;
			}

			x = y + z;
			y = z * y;
		}
		else{
			x = x * z;
		}

	}


	printf("%d", z);
}
