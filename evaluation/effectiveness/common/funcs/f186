/* function auto-generated using seed: 3149350 */
void f186(int x, int y){
	int z = 1;
	x = x * x;

	if(y == x){
		x = y + y;
		z = x * z;
		x = z * x;
	}
	else{
		y = y - y;

		if(z < z){
			x = y * y;
			z = x - y;
			x = z * y;

			if(y <= y){
				y = y - z;
				x = z - z;
				y = z - z;
			}
			else{
				y = x - y;
				x = z + y;
			}

		}

	}


	printf("%d", z);
}
