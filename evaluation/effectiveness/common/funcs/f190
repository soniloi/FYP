/* function auto-generated using seed: 7804467 */
void f190(int x, int y){
	int z = 1;
	y = z * z;

	if(z < y){
		z = z + z;
		z = x - x;
		y = x + z;

		if(x < z){
			y = x * x;

			if(y < y){
				x = x - y;
				x = x * x;
			}

			x = y - z;
		}

	}
	else{
		z = z - z;
		z = z + y;

		if(x == y){
			x = y - z;
			y = x * z;

			if(y >= y){
				y = x - y;
			}

			z = z * x;
		}

		x = z - y;

		if(x >= y){
			z = x + z;
			y = z + z;

			if(x != z){
				y = x * z;
				z = x + y;
				x = x - x;
			}

			y = x + x;
		}

	}


	printf("%d", z);
}
