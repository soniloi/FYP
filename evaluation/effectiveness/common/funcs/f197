/* function auto-generated using seed: 7138813 */
void f197(int x, int y){
	int z = 1;
	y = x - x;

	if(x <= x){
		y = y + y;
		z = z * x;
	}
	else{
		y = y + z;

		if(y == x){
			x = x - x;
			z = y + x;

			if(z <= x){
				z = y * x;
				y = z + z;
				z = z + x;
			}

			y = y + x;
		}
		else{
			y = x - z;

			if(y < x){
				z = y * y;
				y = x - x;
			}

			z = y - z;
		}

	}

	z = z * z;

	if(x <= y){
		x = z + z;
		z = y + y;

		if(y < z){
			z = x + x;
		}

	}
	else{
		y = z * z;

		if(y <= x){
			z = y + y;
			z = z - z;
		}

		z = z + x;

		if(z == z){
			z = x + y;

			if(z > x){
				z = y * x;
				z = y * x;
				y = z - z;
			}

			y = y - y;

			if(z == y){
				y = z * x;
			}
			else{
				z = z + y;
				x = y + x;
				y = y + y;
			}

		}
		else{
			y = y * x;
			y = x * y;

			if(z < y){
				y = z + x;
				x = x + y;
				y = y - z;
			}
			else{
				z = x + y;
				z = y * x;
				y = y + y;
			}

		}

	}


	printf("%d", z);
}
