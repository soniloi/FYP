/* function auto-generated using seed: 8412911 */
void f204(int x, int y){
	int z = 1;
	x = x * y;

	if(y <= x){
		z = y * y;

		if(y > x){
			z = z + x;
			x = x + z;

			if(x < x){
				z = y + y;
				z = y - y;
				x = z * x;
			}

		}
		else{
			x = z - z;

			if(x < y){
				z = x * z;
			}

		}

		z = z * x;

		if(y != x){
			y = x + z;
		}

		y = z * z;

		if(x >= z){
			z = z * z;

			if(z > x){
				y = z * z;
				y = x + y;
				z = x * y;
			}
			else{
				x = z - y;
			}

			x = z - y;

			if(x < y){
				x = z - x;
				z = y * y;
				x = x - x;
			}

		}
		else{
			z = x - z;

			if(z <= y){
				y = z + y;
			}

			x = x + x;
		}

	}
	else{
		z = y - z;

		if(z == y){
			z = x + x;
			y = z * z;

			if(y > x){
				x = x + y;
				x = y * y;
				z = x + y;
			}

		}

	}


	printf("%d", z);
}
