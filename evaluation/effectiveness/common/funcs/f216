/* function auto-generated using seed: 8930410 */
void f216(int x, int y){
	int z = 1;
	z = y - y;

	if(z <= x){
		z = z * y;
		z = z - y;

		if(x > y){
			y = z * x;

			if(x > y){
				x = z * y;
				z = y * x;
				y = y - x;
			}

			y = y - z;

			if(z >= z){
				z = z + z;
				y = z + z;
				z = x - y;
			}
			else{
				z = y - z;
				y = x - x;
			}

			y = x + z;

			if(z < x){
				y = x * x;
				x = x + y;
				y = z + y;
			}

		}

		z = z + y;

		if(x >= z){
			x = y - z;
			y = y + x;
		}
		else{
			z = y - y;
			y = x * z;
			x = z + y;
		}

	}
	else{
		z = y - x;
		y = y * y;
	}

	x = z * y;

	if(y < x){
		z = z * x;
		y = x - z;

		if(y <= y){
			y = y - z;

			if(x != y){
				x = z + x;
				z = y * x;
			}

		}
		else{
			z = y - x;
			z = y + z;
		}

	}
	else{
		z = y + x;
	}


	printf("%d", z);
}
