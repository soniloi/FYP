/* function auto-generated using seed: 5925018 */
void f217(int x, int y){
	int z = 1;
	z = y + y;

	if(y >= x){
		x = z - z;

		if(z == x){
			x = x - x;

			if(y < x){
				z = y + z;
				y = x + x;
				z = y * z;
			}

			y = x - x;

			if(z <= x){
				y = z - y;
				x = y * z;
				z = x + x;
			}
			else{
				z = z + z;
				y = z * x;
				y = y - y;
			}

			x = z + x;
		}
		else{
			y = z + y;
		}

		z = x * z;
	}

	z = x * x;

	if(x == y){
		z = x * x;

		if(y < x){
			x = y + z;

			if(z < x){
				x = z - z;
				z = x + y;
			}

		}

		x = x * y;
		x = y - z;

		if(z != y){
			z = y * x;
			y = z * y;

			if(y == z){
				x = y * y;
				y = y + y;
			}

			y = z + z;

			if(z < y){
				x = x + x;
			}

		}

	}

	z = x * x;

	if(y == y){
		z = z * z;
	}
	else{
		z = z + y;
		z = z * z;

		if(z > y){
			z = x + y;
			y = z + z;

			if(z > y){
				z = x - x;
				z = z - y;
				z = y * x;
			}
			else{
				y = x * y;
				y = z + x;
				x = z - z;
			}

			z = z * z;
		}
		else{
			z = z - y;
			x = z - z;
			x = z * x;
		}

		y = z * x;
	}


	printf("%d", z);
}
