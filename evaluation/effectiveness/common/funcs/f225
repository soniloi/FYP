/* function auto-generated using seed: 8028463 */
void f225(int x, int y){
	int z = 1;
	z = z + z;

	if(z > y){
		y = x * y;

		if(y >= x){
			x = y * x;
			x = x - z;
		}

		x = z * z;
	}

	z = z + x;

	if(z <= x){
		y = z - x;

		if(x <= y){
			z = x - x;
			z = y - x;

			if(x > z){
				x = y * x;
				y = z * z;
			}
			else{
				y = x - x;
				x = x + z;
			}

			y = y + z;

			if(z <= y){
				x = z + z;
				z = y - y;
			}

		}
		else{
			y = z - z;
			z = z - x;
			z = z + z;
		}

		y = z + z;
		z = z - z;
	}


	printf("%d", z);
}
