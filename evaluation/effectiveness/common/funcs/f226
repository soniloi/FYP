/* function auto-generated using seed: 6079637 */
void f226(int x, int y){
	int z = 1;
	z = z * z;

	if(y > y){
		x = x + y;
		z = z - z;
	}
	else{
		x = y + y;

		if(x >= y){
			y = z - y;
			x = z * y;
			x = y - y;
		}

	}

	y = x * z;

	if(y > z){
		y = x * z;
		y = z + z;

		if(y < x){
			z = z * x;
			y = y + y;
			z = x + x;

			if(x != y){
				y = z - z;
			}

		}
		else{
			z = y - y;

			if(x != z){
				y = x + x;
				y = y - z;
			}
			else{
				z = x + y;
				y = z * x;
				z = y - z;
			}

			x = z + x;
			z = x + y;
		}

	}
	else{
		z = x + y;

		if(y > x){
			z = y * y;

			if(x > z){
				y = x * x;
				y = y * x;
			}

			y = x * x;
		}
		else{
			z = x - x;
			z = z + y;

			if(x <= z){
				z = z - z;
				z = z + x;
			}
			else{
				x = z * z;
				y = x * z;
				y = y - y;
			}

		}

		x = x - z;

		if(y > z){
			x = x - x;

			if(y > y){
				z = x * z;
				x = y + y;
			}
			else{
				z = z * z;
				z = y + z;
			}

			y = y - y;
		}

	}


	printf("%d", z);
}
