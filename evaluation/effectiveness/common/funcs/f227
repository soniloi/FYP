/* function auto-generated using seed: 8533578 */
void f227(int x, int y){
	int z = 1;
	z = x + y;

	if(x > y){
		z = x - x;

		if(y >= y){
			z = x + z;

			if(x > x){
				x = z - x;
				z = z * x;
			}

			x = x - y;
			z = z * y;
		}
		else{
			y = x * y;

			if(x < z){
				z = y * y;
				x = z + x;
				z = y - x;
			}
			else{
				y = x + z;
			}

			x = z - x;
		}

		y = x * x;

		if(y != z){
			x = x * z;
			z = y - y;

			if(x == x){
				x = y * z;
				y = x * y;
			}
			else{
				y = x - z;
				y = y + x;
			}

			y = x + y;
		}

		x = y * z;

		if(x < x){
			y = z + y;
			z = x + z;

			if(z <= x){
				x = x + z;
				y = y * x;
				z = x * x;
			}
			else{
				y = y - z;
				y = y + y;
				z = z - x;
			}

		}
		else{
			x = x + y;
			z = z + x;
			y = y + x;
		}

	}
	else{
		y = x * x;

		if(y > z){
			y = x * y;
		}

		y = y - y;

		if(y == y){
			x = z - x;
		}

		x = z * y;
	}


	printf("%d", z);
}
