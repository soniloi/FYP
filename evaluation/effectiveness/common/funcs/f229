/* function auto-generated using seed: 3812079 */
void f229(int x, int y){
	int z = 1;
	y = y + z;

	if(y < z){
		x = x - z;

		if(z >= x){
			x = x * z;
			x = z + x;
			z = x - x;
		}

	}
	else{
		x = x * x;
		y = x + x;

		if(y <= y){
			y = z * x;
			y = x + z;
		}
		else{
			y = y - x;
			x = y + z;
		}

		y = x - z;
	}

	y = x * z;

	if(y == x){
		x = y + z;

		if(x <= x){
			z = y + z;

			if(y == y){
				z = x - z;
				x = x - x;
				z = x - y;
			}
			else{
				y = y - y;
				x = z * z;
			}

			z = x * y;

			if(y < x){
				z = x * y;
				x = y * z;
				y = y + y;
			}
			else{
				y = z * y;
				y = x - z;
				y = z - y;
			}

			x = x + x;

			if(y == z){
				x = x + z;
				y = z + z;
				x = x - z;
			}

		}
		else{
			z = z - y;
		}

		x = y + z;
	}

	z = z - y;

	if(z == y){
		z = z - y;

		if(z <= x){
			y = x + y;
			x = x + y;

			if(z != x){
				x = y - y;
				x = y + z;
				y = z + y;
			}
			else{
				z = x + y;
			}

		}
		else{
			z = y * y;
		}

	}


	printf("%d", z);
}
