/* function auto-generated using seed: 1884162 */
void f231(int x, int y){
	int z = 1;
	y = x + y;

	if(z != y){
		x = z * z;

		if(z < z){
			y = x - z;

			if(x <= x){
				y = z - x;
			}
			else{
				z = z - z;
				y = y + x;
			}

		}
		else{
			x = y - z;

			if(z > x){
				x = x * y;
			}

		}

		z = x + y;
		z = z + z;

		if(z < y){
			y = x + y;

			if(x <= x){
				y = y - x;
				y = z - z;
			}

			x = z + y;
			y = x - x;
		}
		else{
			z = y + x;
			z = x - z;

			if(y >= y){
				z = z + z;
				z = y * y;
			}
			else{
				z = z - y;
			}

		}

	}

	z = y + x;

	if(x != y){
		x = y - y;
		x = z * z;
		z = z * y;

		if(z != y){
			z = x + x;
		}
		else{
			z = z * z;

			if(z > x){
				x = y - x;
				y = x - z;
				z = y * x;
			}
			else{
				x = z * y;
				x = x * z;
				y = y + y;
			}

			z = z * y;
		}

	}
	else{
		x = y + z;

		if(z > z){
			z = y + x;

			if(x == x){
				y = z * y;
				y = x - y;
				x = y - z;
			}

		}
		else{
			y = y * x;
			z = y + x;
			x = x * y;
		}

		z = y + x;
	}


	printf("%d", z);
}
