/* function auto-generated using seed: 6997172 */
void f236(int x, int y){
	int z = 1;
	y = x - x;

	if(y > z){
		y = y - x;
		x = x * y;

		if(y > x){
			x = x + x;
			x = y + x;
		}

		y = x - x;

		if(z > z){
			y = y + y;

			if(y <= z){
				y = x + x;
			}

		}

	}

	x = z - x;

	if(z <= z){
		y = z * z;

		if(z == x){
			x = z * z;

			if(z != y){
				x = x - y;
				z = y * x;
				z = z * x;
			}

		}

		z = z - z;
		x = z + z;
	}

	z = x + y;

	if(x < x){
		x = x * z;
		z = x * z;

		if(z > y){
			z = y * z;
		}
		else{
			x = y - x;
			z = y - z;

			if(y >= x){
				x = x - y;
				z = y * z;
			}
			else{
				x = y + x;
				y = x - z;
				y = y - z;
			}

		}

	}


	printf("%d", z);
}
