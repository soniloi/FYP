/* function auto-generated using seed: 5534592 */
void f251(int x, int y){
	int z = 1;
	x = y + z;

	if(y < z){
		x = z + x;
		z = y + x;

		if(x != x){
			y = x - x;
			z = z * x;
			z = z * x;
		}

		z = x - z;
	}
	else{
		z = z + x;

		if(z != z){
			z = z + y;
			x = x - y;
		}

	}

	z = z + y;

	if(z >= z){
		z = z * z;
	}
	else{
		y = y - y;

		if(x >= x){
			x = y + y;

			if(z < y){
				y = z * y;
				y = y - y;
				y = y + x;
			}

			y = x - x;
			x = y - y;
		}

		z = z * y;

		if(y >= x){
			y = y + x;

			if(x == x){
				z = x * x;
				z = y * x;
			}

		}
		else{
			x = x - x;

			if(z != x){
				x = z + z;
			}
			else{
				z = y * z;
			}

			x = y - z;
			x = y - y;

			if(y >= z){
				z = y - y;
				z = x - x;
			}

		}

		z = z + y;
	}

	z = x * y;

	if(x < z){
		z = z - y;

		if(z >= z){
			y = z - x;
		}
		else{
			x = z * x;
			z = x * x;
			x = x - z;

			if(y == x){
				x = y * y;
				z = y * y;
			}
			else{
				y = x - x;
			}

		}

	}
	else{
		y = y * y;
	}


	printf("%d", z);
}
