/* function auto-generated using seed: 7969328 */
void f260(int x, int y){
	int z = 1;
	z = y - x;

	if(z != z){
		z = x * x;
	}
	else{
		y = z + x;

		if(y > z){
			y = z - y;
			x = z + z;
		}

		x = y + x;
	}

	y = z + z;

	if(z != x){
		y = z + x;
	}
	else{
		z = y * x;

		if(z <= x){
			z = x + z;
			x = z - z;
			y = z - z;

			if(z != z){
				z = x + x;
				x = y - z;
				y = y * x;
			}

		}

		z = z * y;
	}


	printf("%d", z);
}
