/* function auto-generated using seed: 9763071 */
void f261(int x, int y){
	int z = 1;
	x = x + z;

	if(x <= x){
		y = z + y;
		z = y - x;
	}

	x = z - y;

	if(y < z){
		y = x + x;

		if(x >= x){
			z = y + z;
		}
		else{
			y = z - z;
			y = x * z;

			if(z < y){
				x = z + z;
				z = x + x;
			}

		}

	}
	else{
		y = y - y;
	}


	printf("%d", z);
}
