/* function auto-generated using seed: 9908359 */
void f263(int x, int y){
	int z = 1;
	x = x - z;

	if(x == z){
		z = y * z;

		if(z != x){
			z = z * x;

			if(z == z){
				z = z * z;
			}
			else{
				x = x - y;
				z = x * z;
				z = x * y;
			}

			x = y - y;
		}

		z = x - z;

		if(x <= y){
			y = z + y;
			y = y + y;

			if(x >= z){
				x = y - y;
				z = x - y;
				y = z - y;
			}

			x = x * z;
		}

	}
	else{
		y = x + z;
		x = x + x;
	}


	printf("%d", z);
}
