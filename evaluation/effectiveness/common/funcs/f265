/* function auto-generated using seed: 1894806 */
void f265(int x, int y){
	int z = 1;
	y = y * x;

	if(x != z){
		y = z - z;

		if(x >= y){
			z = z - z;
			x = x - y;

			if(z > z){
				y = z - z;
				z = z - y;
			}

			x = z - x;

			if(z != y){
				y = y + x;
				x = x * y;
				y = z + z;
			}

		}

		x = y * z;

		if(z != z){
			x = x * x;

			if(y < z){
				x = y - y;
				x = x * x;
			}
			else{
				x = x - z;
				z = z * y;
			}

		}

	}
	else{
		x = z - z;
		x = z + x;

		if(x == z){
			x = z + y;
		}

		z = x + y;
	}

	y = x - z;

	if(z != x){
		x = x - x;
		y = x * y;
	}

	z = x * y;

	if(z < x){
		x = z + y;

		if(y >= z){
			y = y + z;
			z = x * x;
		}

		y = x + z;

		if(y == z){
			z = z * x;
			z = x * x;
			y = y - z;
		}

	}
	else{
		z = y * x;
		z = z * x;
	}


	printf("%d", z);
}
