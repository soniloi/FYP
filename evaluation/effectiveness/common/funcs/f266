/* function auto-generated using seed: 9419290 */
void f266(int x, int y){
	int z = 1;
	y = x * x;

	if(x != x){
		y = z - y;
		y = z * y;

		if(x > y){
			z = y * x;

			if(x > x){
				y = y - y;
			}

			x = z - y;
			z = x - z;
		}
		else{
			y = z + x;

			if(y > x){
				y = x + x;
				y = x * x;
			}
			else{
				x = x * y;
				z = z * z;
			}

		}

	}

	x = z * x;

	if(y <= z){
		y = z - x;

		if(z < z){
			z = z + x;
			y = y - x;
			x = y * y;
		}
		else{
			y = y * z;
		}

	}


	printf("%d", z);
}
