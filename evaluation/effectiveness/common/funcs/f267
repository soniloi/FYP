/* function auto-generated using seed: 8532140 */
void f267(int x, int y){
	int z = 1;
	z = x - z;

	if(y >= x){
		x = z - x;
	}
	else{
		x = z - x;
		y = x * z;

		if(x == z){
			z = z + z;
			z = z * x;
		}
		else{
			z = y - y;
		}

	}


	printf("%d", z);
}
