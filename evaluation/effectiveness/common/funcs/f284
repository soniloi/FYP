/* function auto-generated using seed: 9272088 */
void f284(int x, int y){
	int z = 1;
	z = y + y;

	if(z == x){
		z = x - y;
	}

	z = y + x;

	if(x < y){
		y = z + x;

		if(z == z){
			y = z - y;
			x = x + z;

			if(z == y){
				z = x - z;
				x = z - z;
				x = x - z;
			}
			else{
				z = z - z;
				y = z * y;
				z = y * y;
			}

		}
		else{
			y = y - y;
			z = z * z;
		}

		y = y - y;

		if(y < x){
			x = x * z;
			z = y * x;
			z = y + x;
		}
		else{
			x = z + x;
			y = x + x;

			if(z != y){
				x = z - x;
				z = x + x;
				y = y + y;
			}

		}

	}

	z = y + y;

	if(z < y){
		z = x + x;
	}


	printf("%d", z);
}
