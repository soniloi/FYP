/* function auto-generated using seed: 3937347 */
void f285(int x, int y){
	int z = 1;
	z = x + x;

	if(z != x){
		y = x - y;

		if(y <= y){
			z = x * z;
			z = y - z;

			if(z == x){
				x = y * z;
				x = z - x;
				x = y - x;
			}

			x = z + y;
		}
		else{
			x = x * z;
			z = y + y;
			z = z - z;
		}

		z = y * z;
		x = z * y;

		if(y < z){
			x = x - z;

			if(z != y){
				y = x + x;
				x = y + y;
			}
			else{
				x = y + z;
				y = y + y;
			}

			y = x * z;
		}

	}
	else{
		x = y * x;
	}


	printf("%d", z);
}
