/* function auto-generated using seed: 6321191 */
void f287(int x, int y){
	int z = 1;
	z = x + y;

	if(x <= x){
		y = y + x;

		if(x == y){
			y = x + x;
			x = y * y;
		}

		x = z - x;
		x = z + z;

		if(z == y){
			z = x - x;
		}

	}
	else{
		x = y - y;
		x = x * x;

		if(z > z){
			x = x + y;

			if(x <= z){
				y = x * x;
				x = x * y;
			}

		}
		else{
			x = y - y;
		}

	}


	printf("%d", z);
}
