/* function auto-generated using seed: 5084309 */
void f290(int x, int y){
	int z = 1;
	z = z + x;

	if(x == z){
		y = y + y;
		x = y - z;
	}
	else{
		x = z - x;

		if(y != y){
			z = y - z;
		}
		else{
			x = y * y;

			if(y < y){
				z = z - y;
				y = x - x;
				x = z + x;
			}

		}

		y = z - x;
	}


	printf("%d", z);
}
