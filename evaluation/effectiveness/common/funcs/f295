/* function auto-generated using seed: 8970559 */
void f295(int x, int y){
	int z = 1;
	x = x + x;

	if(y == x){
		x = z - z;
		y = z * x;
		x = y + z;

		if(z != z){
			x = x + y;
		}
		else{
			y = y * z;
			x = y * z;

			if(x < y){
				z = y + z;
				x = z + y;
			}
			else{
				x = y + x;
			}

			z = y * y;

			if(x > x){
				z = x + y;
				z = z + z;
				y = z - x;
			}

		}

	}


	printf("%d", z);
}
