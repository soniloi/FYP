/* function auto-generated using seed: 943363 */
void f30(int x, int y){
	int z = 1;
	z = y + y;

	if(x <= x){
		y = y * y;
		y = y * x;

		if(y >= z){
			x = z + z;
		}

		x = x + x;

		if(z == z){
			x = z - x;
			x = z * x;
		}
		else{
			z = x + y;

			if(y < z){
				x = z + x;
			}
			else{
				x = z - z;
				x = z + y;
				z = y + x;
			}

			y = z - y;
			x = z * y;

			if(y == y){
				z = z - x;
				z = x + x;
				y = z - z;
			}

		}

	}

	y = x - y;

	if(x == z){
		x = z - x;
		x = z - z;

		if(y == z){
			z = z * y;
		}
		else{
			z = x - y;
		}

	}


	printf("%d", z);
}
