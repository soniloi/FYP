/* function auto-generated using seed: 5255300 */
void f303(int x, int y){
	int z = 1;
	x = x * x;

	if(x > x){
		y = y * y;
		z = x * x;

		if(x == y){
			x = x - z;

			if(y == y){
				z = z - x;
			}

		}
		else{
			y = x * y;
		}

	}
	else{
		z = x - z;
	}

	y = y * z;

	if(y >= y){
		x = y + x;

		if(y >= x){
			z = y - x;
			y = z * y;
			x = z * z;
		}

	}


	printf("%d", z);
}
