/* function auto-generated using seed: 4503737 */
void f305(int x, int y){
	int z = 1;
	x = x - z;

	if(x >= y){
		y = z - y;

		if(x <= z){
			z = z - z;

			if(z < y){
				z = z + z;
			}
			else{
				y = y + x;
			}

			y = y * y;

			if(z != z){
				z = x + y;
				x = z + z;
			}
			else{
				x = x * y;
			}

		}

		y = y + x;

		if(y == z){
			z = z * z;
			x = y * z;
		}
		else{
			x = x + y;
			y = y * z;

			if(x >= y){
				y = x * x;
			}
			else{
				x = y - y;
				y = x * z;
				z = y + x;
			}

			z = y + z;

			if(z == y){
				y = z * z;
				x = y - z;
				z = y - y;
			}

		}

	}

	x = y + y;

	if(x > x){
		y = z + x;

		if(z >= x){
			x = x * y;

			if(y <= y){
				y = z + z;
			}

			x = y - y;
			y = z - y;
		}
		else{
			z = x + y;
			x = x * z;
		}

	}


	printf("%d", z);
}
