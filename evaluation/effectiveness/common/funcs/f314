/* function auto-generated using seed: 4497415 */
void f314(int x, int y){
	int z = 1;
	x = x * x;

	if(x != x){
		z = y - x;
		x = x + z;
		y = x - z;

		if(y != x){
			y = y + z;

			if(z == z){
				x = y - x;
				z = z - x;
				x = x - z;
			}

			z = x + y;
		}
		else{
			y = x + z;
			y = z + z;

			if(y != z){
				y = x * x;
				z = x + z;
			}

			y = x * z;

			if(y == x){
				z = y - z;
				z = x + y;
			}
			else{
				y = y - x;
				y = z - y;
			}

		}

	}


	printf("%d", z);
}
