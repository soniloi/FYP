/* function auto-generated using seed: 9041964 */
void f316(int x, int y){
	int z = 1;
	y = x * x;

	if(y != y){
		x = z * y;

		if(z > x){
			z = z - x;
			z = z + x;

			if(y <= y){
				x = y * y;
				y = x - x;
			}

			y = x * x;

			if(x <= x){
				z = z - x;
				y = y * z;
				x = y * x;
			}
			else{
				z = z * x;
			}

		}

		y = z * x;
		z = y * y;
	}
	else{
		y = y - x;
	}

	z = z + y;

	if(y != y){
		y = x + y;

		if(x != z){
			z = x - z;
		}
		else{
			y = y + x;
			y = y * x;
			y = y * x;
		}

		x = x - x;

		if(x > x){
			x = x + x;
			y = z * x;

			if(z < x){
				y = y * z;
			}

			z = x * z;
		}

		y = y + z;
	}
	else{
		x = y + y;
		y = y * y;
		z = z + x;
	}

	z = y - x;

	if(x < z){
		y = x * y;

		if(x == x){
			x = y * y;
			x = z - y;
			x = x * x;
		}
		else{
			z = y - z;
			z = x * y;

			if(y <= z){
				x = x + z;
				z = y - z;
			}

		}

	}
	else{
		x = y - x;
		y = x + x;
	}


	printf("%d", z);
}
