/* function auto-generated using seed: 1640816 */
void f319(int x, int y){
	int z = 1;
	y = z - x;

	if(z == x){
		y = y + x;

		if(y <= x){
			y = x - y;

			if(z != z){
				z = z + y;
				y = y - x;
			}

		}
		else{
			z = y * y;

			if(z > z){
				z = z - z;
				y = y + z;
				z = z - x;
			}

			z = y + x;
		}

		y = x - x;
	}


	printf("%d", z);
}
