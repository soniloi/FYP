/* function auto-generated using seed: 7359927 */
void f329(int x, int y){
	int z = 1;
	x = z * y;

	if(y != x){
		z = z + y;
	}
	else{
		y = x - x;

		if(x <= y){
			y = z + z;

			if(z < y){
				y = x + z;
				z = x - y;
			}

		}
		else{
			x = x * x;

			if(x == y){
				y = y - z;
				z = z * y;
				z = x + z;
			}
			else{
				z = z * y;
			}

			z = z * z;
		}

		z = y + y;
		x = y + x;
	}

	z = z * z;

	if(y == y){
		y = y + z;

		if(z != y){
			z = z - x;
			z = z * z;

			if(z >= z){
				y = x - x;
				y = x - x;
				y = y + y;
			}

		}
		else{
			y = y * y;
		}

		x = z - x;
		x = y - x;
	}


	printf("%d", z);
}
