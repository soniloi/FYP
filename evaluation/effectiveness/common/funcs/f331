/* function auto-generated using seed: 3961200 */
void f331(int x, int y){
	int z = 1;
	y = y * y;

	if(z != z){
		y = z + z;

		if(z < y){
			z = y - x;
		}
		else{
			z = y - z;
			y = z + y;
		}

	}
	else{
		y = y - z;
	}

	z = z - y;

	if(y < z){
		y = y * x;

		if(z <= x){
			y = z + x;

			if(z <= x){
				x = y + x;
				y = x + x;
				x = y - x;
			}

			x = z - x;
			z = y * z;
		}

		z = z * z;
	}


	printf("%d", z);
}
