/* function auto-generated using seed: 9923551 */
void f334(int x, int y){
	int z = 1;
	z = z - x;

	if(y <= x){
		y = y + y;

		if(z < y){
			y = x * x;
		}

		x = x * y;
		x = z + z;

		if(z == z){
			y = x * z;
		}

	}
	else{
		x = y * x;
		z = x - z;
		y = x * y;
	}


	printf("%d", z);
}
