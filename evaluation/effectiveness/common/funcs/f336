/* function auto-generated using seed: 1242771 */
void f336(int x, int y){
	int z = 1;
	y = z * z;

	if(y >= x){
		y = x + y;
	}
	else{
		y = x * z;

		if(x >= z){
			y = z + z;
		}

		z = x + z;

		if(y < y){
			y = y * x;
		}
		else{
			y = x + y;
		}

	}

	z = z - z;

	if(z < y){
		y = y - z;
		x = x * y;

		if(x == z){
			y = y * y;
			x = x * z;
			x = x * z;
		}
		else{
			y = x + z;
			y = z + y;
		}

		z = y * y;
	}


	printf("%d", z);
}
