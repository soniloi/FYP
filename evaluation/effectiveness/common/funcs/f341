/* function auto-generated using seed: 7802890 */
void f341(int x, int y){
	int z = 1;
	z = y * z;

	if(z == z){
		y = x + x;
	}
	else{
		x = x + x;
		y = x - x;

		if(x > x){
			z = x * z;
			y = y * z;
			x = z * y;
		}
		else{
			z = y * z;
		}

		z = z * x;
	}

	x = z * x;

	if(x < y){
		y = z - x;

		if(y < z){
			z = x - y;

			if(z != y){
				x = y - z;
			}

			z = y + x;
		}
		else{
			z = z * z;
		}

		x = y * x;
		y = z + x;

		if(z != z){
			y = y * z;
		}

	}
	else{
		x = x * y;
		y = z + x;
		y = z * x;

		if(x == y){
			x = x + y;
			x = y * z;
		}
		else{
			x = z + z;
			y = z * z;
		}

	}

	y = y + z;

	if(z != x){
		z = x - z;
		z = y * z;

		if(z > z){
			y = x + x;
			y = y + y;
		}

		y = x * y;
	}
	else{
		y = x + y;
	}


	printf("%d", z);
}
