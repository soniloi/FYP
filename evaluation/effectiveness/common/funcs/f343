/* function auto-generated using seed: 2639311 */
void f343(int x, int y){
	int z = 1;
	y = z - y;

	if(y >= z){
		x = z * z;
		x = y - z;

		if(x >= z){
			z = z - y;

			if(z < z){
				x = y + y;
				x = z - x;
				x = x + y;
			}
			else{
				z = z + x;
				z = x + x;
				x = z - z;
			}

		}

	}

	y = x * y;

	if(y != y){
		y = y + y;

		if(x != z){
			z = y + x;
		}

		z = x * x;
	}
	else{
		z = z + x;

		if(z < z){
			x = y * y;

			if(z == x){
				y = z + y;
				z = x - y;
			}

			x = z + z;

			if(y > x){
				y = z + z;
				x = z - y;
			}

			z = x - z;
		}
		else{
			y = y * y;
		}

		x = x - z;

		if(x >= y){
			x = x - z;

			if(x > z){
				x = z + y;
			}

		}

	}


	printf("%d", z);
}
