/* function auto-generated using seed: 8616967 */
void f351(int x, int y){
	int z = 1;
	x = x * y;

	if(z >= y){
		z = z * z;
		x = x + y;
		y = x * y;

		if(x >= y){
			z = z * z;

			if(z <= x){
				z = z * z;
				z = z + y;
			}

			x = y - x;
		}

	}
	else{
		y = y * y;
		y = y - y;
		z = x * x;

		if(y != z){
			x = y * x;
			x = y - z;
		}

	}


	printf("%d", z);
}
