/* function auto-generated using seed: 9395170 */
void f357(int x, int y){
	int z = 1;
	z = x + z;

	if(z != x){
		y = z * z;

		if(z > x){
			z = x * x;
		}
		else{
			z = x + y;
			z = x - y;
			z = y + y;
		}

		x = x * z;

		if(z == y){
			y = z + z;
			y = z - y;

			if(x == z){
				y = y - x;
			}

		}
		else{
			z = x - x;
			y = y + y;

			if(x >= z){
				x = y + z;
				y = x * x;
			}
			else{
				x = x * y;
			}

		}

	}
	else{
		z = z - z;

		if(z == z){
			x = y + x;
			z = z + x;
		}

	}

	y = z - y;

	if(y <= y){
		y = y + x;

		if(y <= y){
			x = z - x;
			z = x * y;

			if(z <= y){
				x = y * x;
				z = x + z;
			}
			else{
				z = z - z;
			}

			y = x + z;
		}
		else{
			x = x * z;
		}

		y = z + x;
	}


	printf("%d", z);
}
