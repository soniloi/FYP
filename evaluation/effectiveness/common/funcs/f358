/* function auto-generated using seed: 2311263 */
void f358(int x, int y){
	int z = 1;
	y = x - y;

	if(x <= z){
		x = z * y;
	}
	else{
		z = z + z;
	}

	z = y * x;

	if(y >= z){
		z = z * y;
		y = z * x;
		y = z - x;
	}
	else{
		z = x + z;

		if(x <= x){
			z = z * z;

			if(z > y){
				z = z * z;
				z = y - x;
			}

		}
		else{
			z = y * y;
		}

	}

	x = x * y;

	if(z >= x){
		y = y - y;
		y = x * y;

		if(y <= z){
			z = z + x;

			if(z != z){
				x = y - x;
			}

			y = y + x;

			if(x >= x){
				z = x - x;
			}
			else{
				y = x - x;
				z = y - z;
			}

		}
		else{
			y = z - x;
		}

		z = x * x;

		if(y <= x){
			z = x * z;

			if(y <= x){
				x = z - z;
				y = y * y;
				y = z * x;
			}

		}
		else{
			z = y * z;
			z = y + x;
		}

	}
	else{
		y = y - z;
		z = y * x;

		if(y > z){
			z = x + z;
			x = x - z;

			if(x != x){
				x = y - x;
			}
			else{
				z = z - z;
			}

			x = x + y;
		}

	}


	printf("%d", z);
}
