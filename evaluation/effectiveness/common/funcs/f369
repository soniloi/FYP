/* function auto-generated using seed: 405208 */
void f369(int x, int y){
	int z = 1;
	y = x - z;

	if(x != x){
		z = y * z;
		x = x + z;
		z = x * y;

		if(x != z){
			z = z - x;
			x = y * z;
			y = x - y;

			if(y == z){
				y = z - y;
				y = z - z;
				x = z * z;
			}

		}
		else{
			x = x * y;

			if(x != y){
				z = x + z;
			}
			else{
				y = z - z;
			}

		}

	}
	else{
		z = y * y;

		if(y < z){
			z = z * z;
		}
		else{
			z = x + x;
			x = x * x;
			y = x - z;
		}

		y = z - x;

		if(z <= x){
			z = x + y;

			if(y >= x){
				z = z + x;
				z = z * y;
			}
			else{
				y = x * z;
			}

		}
		else{
			y = x - x;
			x = x - x;

			if(z > z){
				x = y - z;
			}

		}

		x = z * x;

		if(x <= x){
			x = x * x;
		}
		else{
			y = z * z;
			x = y * y;
		}

	}

	z = x + y;

	if(y == x){
		y = z + z;

		if(x != y){
			y = x * z;
			y = y * x;
		}

		z = y * z;
		y = y - y;
	}


	printf("%d", z);
}
