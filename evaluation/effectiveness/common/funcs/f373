/* function auto-generated using seed: 2325142 */
void f373(int x, int y){
	int z = 1;
	x = y * z;

	if(z > y){
		z = x - z;

		if(x > z){
			z = y - z;
			z = z + y;

			if(x <= z){
				z = y - x;
				x = x - y;
				y = y - z;
			}
			else{
				z = x + x;
				x = x * z;
			}

			y = y + z;

			if(z <= x){
				x = x - y;
				y = y - x;
			}
			else{
				y = z - x;
			}

		}

		x = z * z;
	}


	printf("%d", z);
}
