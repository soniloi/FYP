/* function auto-generated using seed: 8862906 */
void f375(int x, int y){
	int z = 1;
	y = z + y;

	if(y <= z){
		z = x - x;

		if(y > x){
			y = y + z;
			x = y + y;
			y = x * x;
		}
		else{
			y = y + y;
			x = x - z;
		}

		z = z * z;

		if(z == z){
			z = x * x;

			if(z > y){
				x = y - y;
				x = z - x;
				x = x - y;
			}
			else{
				z = z * y;
				y = y + z;
			}

			x = x * z;

			if(x > z){
				y = y * x;
			}

		}

		x = z + y;
	}


	printf("%d", z);
}
