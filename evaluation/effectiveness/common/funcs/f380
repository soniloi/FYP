/* function auto-generated using seed: 5038284 */
void f380(int x, int y){
	int z = 1;
	z = z * y;

	if(x > z){
		z = z * y;

		if(y == y){
			x = z + x;

			if(x >= y){
				x = y * y;
			}
			else{
				y = y * z;
				x = z * y;
				y = y * z;
			}

			y = x - y;

			if(x >= z){
				x = y * z;
				y = z + y;
			}

		}

		x = y + x;

		if(y == x){
			y = x * y;
			y = z + x;
			y = x - x;
		}
		else{
			z = z - x;
			z = z + y;
			y = x - y;
		}

	}

	y = z + y;

	if(z >= x){
		y = y - z;

		if(z < z){
			y = x - y;
		}
		else{
			y = y - x;
			y = x + z;
		}

		x = y + x;

		if(y > y){
			x = z * z;
			z = z + z;
		}

		z = x * z;

		if(x >= y){
			x = z * z;
			z = y - z;

			if(x > z){
				x = y - x;
			}
			else{
				x = y - z;
				z = x * x;
			}

			z = y + x;
		}

	}
	else{
		x = z + z;
		z = y * z;
		z = z + x;
	}


	printf("%d", z);
}
