/* function auto-generated using seed: 9794272 */
void f385(int x, int y){
	int z = 1;
	x = y + y;

	if(y < x){
		y = z * y;
	}

	y = x - x;

	if(x < y){
		z = y * x;

		if(y >= y){
			y = x + z;
			y = z + x;
			x = y + x;
		}
		else{
			z = y - y;
			z = y + y;
		}

	}
	else{
		x = x * y;

		if(y >= x){
			x = x * y;
			z = x + z;

			if(z <= x){
				y = x - y;
				z = y - y;
				x = z + y;
			}

			x = y * y;

			if(x != z){
				x = x - y;
				z = z - x;
				x = x - x;
			}

		}
		else{
			z = y - y;
		}

		y = y - x;
	}

	x = x * y;

	if(x > z){
		x = x + z;
		y = x * x;

		if(z >= y){
			y = y * y;

			if(y > x){
				y = z + y;
			}
			else{
				y = x + z;
			}

		}

		y = y - z;
	}


	printf("%d", z);
}
