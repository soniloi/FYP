/* function auto-generated using seed: 3462349 */
void f387(int x, int y){
	int z = 1;
	z = x * z;

	if(z >= z){
		x = x * y;
		x = z - y;

		if(x < y){
			x = z * x;
		}
		else{
			y = x + x;

			if(z >= x){
				x = y + z;
				y = y - x;
				y = x + y;
			}
			else{
				x = z - z;
			}

			x = y * z;
			y = z - x;
		}

	}
	else{
		x = y + z;

		if(z == x){
			x = z * z;
			z = y * y;
		}
		else{
			z = z * y;
			z = y - z;
		}

		z = x + z;

		if(z >= y){
			x = y + z;
			x = z + y;

			if(x > z){
				y = y + x;
				x = y * x;
				y = x - z;
			}
			else{
				z = x + y;
				x = z * y;
				y = z + z;
			}

			y = y + z;
		}
		else{
			y = y * z;
		}

	}

	x = y * y;

	if(z >= y){
		x = z + z;

		if(y != z){
			z = x + z;

			if(y != z){
				z = y * y;
				z = x - y;
				x = y - z;
			}

			z = x - x;
		}
		else{
			y = x - x;
			y = x - z;
			x = y - z;
		}

	}

	x = y - y;

	if(z <= z){
		x = x + y;

		if(y == x){
			x = y - y;
			x = x * z;
		}

		z = y + y;
		y = x * x;

		if(z <= y){
			z = x + y;
			y = x + z;
			z = x - x;

			if(y <= x){
				y = z - x;
			}

		}

	}


	printf("%d", z);
}
