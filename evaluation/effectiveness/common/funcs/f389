/* function auto-generated using seed: 5832975 */
void f389(int x, int y){
	int z = 1;
	z = x - y;

	if(x >= z){
		z = y - z;

		if(z == x){
			y = x * z;
			z = z - x;
			x = x + y;
		}

		y = x + z;

		if(y <= x){
			x = z * z;
		}
		else{
			x = x + x;

			if(x != y){
				x = z - z;
				z = z + x;
				y = y - y;
			}

			x = x - y;
		}

		x = x * x;

		if(x > y){
			z = z - z;
			z = x * z;

			if(y >= y){
				y = y + z;
				y = y * z;
				z = y - y;
			}
			else{
				z = y + x;
				y = x + z;
			}

		}

	}
	else{
		z = z - x;

		if(y < z){
			x = z + z;
			y = x + x;

			if(y != y){
				y = x + y;
				y = x - z;
				z = z + y;
			}
			else{
				x = x + x;
				x = y - y;
				x = x - z;
			}

		}
		else{
			y = x * z;
		}

		y = x + z;
		z = z + y;
	}

	y = y - x;

	if(x >= y){
		z = y + x;
	}


	printf("%d", z);
}
