/* function auto-generated using seed: 6446081 */
void f399(int x, int y){
	int z = 1;
	x = y * x;

	if(z >= z){
		y = z * z;
		y = y - y;

		if(x <= z){
			z = x * z;
		}
		else{
			x = y + z;
		}

		z = y + y;

		if(x >= y){
			x = x - x;
		}
		else{
			y = x - x;
			x = y - z;
		}

	}


	printf("%d", z);
}
