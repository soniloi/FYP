/* function auto-generated using seed: 6006806 */
void f4(int x, int y){
	int z = 1;
	y = y * x;

	if(y < y){
		z = z - y;
	}
	else{
		z = z - z;
		x = x - z;
		y = y + x;
	}

	y = y + z;

	if(z > y){
		y = x - y;
		z = x - z;
		y = y - z;
	}


	printf("%d", z);
}
