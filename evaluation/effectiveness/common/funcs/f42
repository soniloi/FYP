/* function auto-generated using seed: 7676495 */
void f42(int x, int y){
	int z = 1;
	x = x + y;

	if(z == y){
		y = y * x;
	}
	else{
		z = x * y;

		if(y < x){
			x = z - y;
			z = x + x;
			x = y + z;
		}
		else{
			x = x - x;
		}

	}

	y = z + z;

	if(x != x){
		x = x * y;

		if(y >= x){
			y = z + z;
			y = z - z;
			x = y * x;
		}

	}


	printf("%d", z);
}
