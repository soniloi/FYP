/* function auto-generated using seed: 7529480 */
void f44(int x, int y){
	int z = 1;
	z = z + z;

	if(z >= z){
		z = x + y;
		y = y * z;

		if(y > z){
			y = x + x;
			x = y + z;
			y = z * z;
		}
		else{
			z = y * x;
			x = x + x;
			x = x * x;
		}

		y = y * y;
	}

	y = z * x;

	if(y == x){
		y = z + z;

		if(z == x){
			x = x + x;
			y = x + x;
			x = y - y;
		}
		else{
			y = z * x;
			y = x + z;
			z = y - y;
		}

	}


	printf("%d", z);
}
