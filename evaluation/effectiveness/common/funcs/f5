/* function auto-generated using seed: 1189096 */
void f5(int x, int y){
	int z = 1;
	y = y + x;

	if(y != x){
		y = x + y;

		if(y < y){
			x = x * y;

			if(y < y){
				y = y - x;
				y = x - z;
			}
			else{
				z = y - z;
				x = z - z;
				z = y + z;
			}

			y = x * x;

			if(x == z){
				x = y - z;
				z = y + x;
				y = z - z;
			}

			z = x + y;

			if(y <= y){
				y = x - x;
				z = x - z;
			}

		}
		else{
			y = z + z;
			y = z - y;

			if(y < x){
				x = x + y;
				y = z - x;
			}
			else{
				z = x + x;
			}

		}

	}
	else{
		y = y * z;

		if(x > z){
			y = x + z;
			x = y + y;
			z = x * x;
		}
		else{
			z = x * x;
			y = y * x;

			if(y < x){
				y = z * y;
			}

		}

	}

	x = x - z;

	if(z <= x){
		z = z - z;
		z = z + y;

		if(z != y){
			z = y * x;
		}
		else{
			x = z - x;
			z = x * x;

			if(x < x){
				x = x * y;
			}

			y = y - z;
		}

		z = z - y;
	}
	else{
		x = y * x;

		if(x <= z){
			x = x * z;

			if(x <= y){
				y = x * x;
				x = z * z;
			}
			else{
				z = y - z;
				z = y * y;
			}

			y = z + y;
			z = z + x;
		}

		y = z + z;

		if(y < y){
			y = x + y;

			if(y >= z){
				x = x * z;
				x = y + x;
				x = y + x;
			}

		}

		x = y * x;

		if(z > y){
			z = x - x;
		}
		else{
			y = y - z;
		}

	}

	x = z + x;

	if(y != y){
		y = x * x;
	}
	else{
		y = x * y;
		y = z * y;

		if(y != x){
			z = x + z;
			y = z - z;
			x = x * x;
		}
		else{
			y = y * z;
			z = z - y;

			if(x != z){
				x = x * z;
				x = y * y;
			}
			else{
				y = z + z;
			}

		}

	}


	printf("%d", z);
}
