/* function auto-generated using seed: 8158119 */
void f53(int x, int y){
	int z = 1;
	z = y - x;

	if(y == x){
		z = x + z;

		if(y == z){
			y = y + z;
			x = z * y;
		}

		y = y - x;

		if(z > y){
			y = x + y;
		}

		x = y + y;

		if(z > x){
			y = y - x;
			z = y - z;

			if(z > y){
				z = z - y;
				z = x + x;
			}
			else{
				z = x + y;
				x = x + x;
			}

			x = y + z;
		}
		else{
			z = x * z;

			if(z > z){
				x = x - y;
				x = z + z;
				y = x + y;
			}
			else{
				z = y - y;
			}

			x = z * x;

			if(y != z){
				z = y * z;
			}
			else{
				z = y + y;
			}

		}

	}
	else{
		z = y * z;

		if(z < x){
			z = z + y;
			x = z - z;
			z = y * x;

			if(x >= y){
				z = z - y;
				y = x - z;
			}

		}
		else{
			x = z * z;
			y = z - x;
		}

		z = z + z;
		y = y + y;
	}


	printf("%d", z);
}
