/* function auto-generated using seed: 9198096 */
void f55(int x, int y){
	int z = 1;
	x = y * y;

	if(z > y){
		x = x * x;
		y = y + z;
		y = x - x;

		if(x > x){
			y = y + z;
			y = z * y;
		}
		else{
			z = z + x;

			if(x == x){
				x = y * x;
			}
			else{
				y = y + y;
				x = y * x;
				z = z * x;
			}

			x = x * y;
			x = y + x;

			if(z > x){
				x = x * x;
				x = y * y;
			}
			else{
				x = y * x;
			}

		}

	}
	else{
		x = y - z;

		if(y != x){
			z = y * y;
		}

		y = x * x;
		y = z + z;

		if(z >= x){
			y = y + x;
			z = z - x;
			z = x + z;

			if(y > y){
				x = z * z;
			}

		}

	}

	y = z + y;

	if(y > y){
		x = x + z;
	}

	y = y - y;

	if(z >= x){
		y = x - y;

		if(x >= x){
			z = x + y;
		}
		else{
			x = z - x;
			z = y + x;

			if(y != y){
				x = y * y;
				x = x + y;
				x = z - z;
			}
			else{
				z = y * z;
				z = x + x;
			}

		}

		z = x * y;

		if(y >= z){
			x = y - x;

			if(x > x){
				x = y * y;
				z = x * z;
				y = z + x;
			}
			else{
				z = y + x;
				z = x + z;
				z = y - y;
			}

		}
		else{
			z = x * x;
			x = z * z;
		}

	}


	printf("%d", z);
}
