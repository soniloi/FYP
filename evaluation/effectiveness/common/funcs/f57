/* function auto-generated using seed: 461477 */
void f57(int x, int y){
	int z = 1;
	x = y * z;

	if(x <= z){
		y = z - y;

		if(y == z){
			z = z * y;

			if(x != x){
				x = z - z;
				x = z + y;
				z = z - y;
			}

			y = y - z;

			if(z >= y){
				y = z * x;
				z = x + x;
				y = y + y;
			}

			y = y + y;
		}

		z = x + z;

		if(x > x){
			z = y - x;

			if(x <= z){
				y = z - x;
			}

			y = x - x;

			if(z == y){
				y = x + x;
			}

		}
		else{
			y = x * x;
		}

		z = y * x;
	}
	else{
		z = y - y;
		z = y - x;

		if(x > z){
			x = x + x;
			z = z - y;

			if(z <= y){
				y = x - y;
			}
			else{
				x = z * z;
				x = x * y;
			}

		}

		z = y - y;
	}

	y = x * z;

	if(x > z){
		x = z - x;

		if(x == y){
			x = x * y;
		}
		else{
			x = y * z;
		}

		z = x * z;
	}


	printf("%d", z);
}
