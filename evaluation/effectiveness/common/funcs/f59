/* function auto-generated using seed: 4356283 */
void f59(int x, int y){
	int z = 1;
	z = y - x;

	if(y < z){
		z = x + z;
		y = y + x;

		if(x >= x){
			y = y * y;
			x = x * z;
		}

		y = y - x;
	}

	x = x - y;

	if(y == z){
		y = z - z;

		if(y < z){
			z = y * z;
			z = y * y;
		}
		else{
			y = z - z;
			y = x * y;
		}

	}

	z = y - y;

	if(y == y){
		z = x + y;

		if(y != z){
			y = z + z;
		}
		else{
			y = x - z;

			if(z <= y){
				x = x - y;
				z = x - y;
				x = x + z;
			}

			x = x * z;
			z = z * y;
		}

		x = y * z;

		if(x == x){
			x = y + y;
		}

		y = z + x;
	}
	else{
		z = z + z;
		z = y - z;

		if(z > x){
			y = y - x;
			z = z * y;
		}

	}


	printf("%d", z);
}
