/* function auto-generated using seed: 9426148 */
void f60(int x, int y){
	int z = 1;
	x = y - x;

	if(x > z){
		z = x * x;
	}

	x = z - y;

	if(z >= y){
		y = x * z;
	}
	else{
		x = y * z;

		if(z != x){
			x = x * y;

			if(x != x){
				y = x - y;
			}
			else{
				y = y * y;
				y = y - y;
				z = z + z;
			}

			x = z + y;
			y = x - z;
		}
		else{
			x = x * x;

			if(x < y){
				z = z + y;
			}
			else{
				y = x * y;
				y = z - y;
				x = y * x;
			}

		}

	}


	printf("%d", z);
}
