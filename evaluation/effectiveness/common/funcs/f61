/* function auto-generated using seed: 4521252 */
void f61(int x, int y){
	int z = 1;
	y = z - y;

	if(z != y){
		x = z * x;
		z = y * x;

		if(x == x){
			z = z - z;

			if(y == x){
				y = x - x;
				y = z + x;
			}
			else{
				y = z + z;
				x = z + x;
			}

		}

	}

	y = x + z;

	if(y >= z){
		y = y - x;

		if(z != y){
			y = x - y;
		}
		else{
			y = x - x;
			x = z + x;
		}

	}

	z = x - y;

	if(x != x){
		z = z + x;
		z = x + x;
	}


	printf("%d", z);
}
