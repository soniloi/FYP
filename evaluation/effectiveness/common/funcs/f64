/* function auto-generated using seed: 5603753 */
void f64(int x, int y){
	int z = 1;
	z = x + y;

	if(y >= y){
		z = x * x;

		if(z > x){
			z = x * y;
			z = y - y;
			x = x - x;
		}
		else{
			x = x - y;
			y = z - y;

			if(z < z){
				x = y - x;
				x = x - x;
			}

		}

	}
	else{
		x = z + z;
		x = z - y;
	}

	z = x - y;

	if(z <= y){
		x = y * z;
	}


	printf("%d", z);
}
