/* function auto-generated using seed: 6377539 */
void f65(int x, int y){
	int z = 1;
	z = x + y;

	if(z >= y){
		x = x + z;
	}
	else{
		x = y + y;

		if(y <= x){
			y = x + x;
			z = z - y;
			y = y + z;
		}

		z = x - x;
		y = x * x;

		if(y == y){
			z = z - z;

			if(y != x){
				y = x + z;
				y = x + z;
			}

			x = y - z;

			if(x < x){
				z = z + z;
				z = z - x;
				y = z + y;
			}
			else{
				x = z - z;
				z = x - x;
			}

		}
		else{
			z = z + x;

			if(z == z){
				x = z + y;
				z = z - z;
				x = y + z;
			}
			else{
				z = x + x;
				y = y + z;
			}

			z = y - y;

			if(z > y){
				x = x + x;
				x = z + z;
			}

			y = y * z;
		}

	}


	printf("%d", z);
}
