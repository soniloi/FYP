/* function auto-generated using seed: 2893765 */
void f70(int x, int y){
	int z = 1;
	x = z + z;

	if(z <= y){
		z = z + x;
		x = y - x;

		if(z > x){
			x = z * y;

			if(y <= z){
				z = y + y;
			}

			z = y - z;
		}

		x = y * z;

		if(x == y){
			z = z * z;

			if(y != x){
				x = z - x;
			}
			else{
				y = x * x;
				z = y * z;
			}

			x = y + x;
		}
		else{
			x = x * y;
		}

	}
	else{
		x = y - z;
		z = z + z;
		x = z * z;

		if(y < x){
			x = y - z;
			x = x * y;

			if(y < x){
				x = x - y;
				y = x + x;
				x = y * y;
			}

			y = y - z;

			if(z != z){
				x = x - x;
				y = y * y;
			}
			else{
				x = z - z;
			}

		}

	}

	z = z * x;

	if(x > y){
		z = y + y;

		if(x != x){
			y = x * x;

			if(z == x){
				y = y + x;
			}

			x = x * z;
		}
		else{
			z = x * z;
			x = y * y;

			if(z != x){
				y = z - x;
				z = z + z;
				x = x * z;
			}

		}

		z = x + z;
	}

	x = y * y;

	if(z != z){
		z = x - x;
	}
	else{
		z = y + y;
		y = x - y;
		x = x - x;
	}


	printf("%d", z);
}
