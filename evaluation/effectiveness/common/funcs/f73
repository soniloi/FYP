/* function auto-generated using seed: 4310384 */
void f73(int x, int y){
	int z = 1;
	z = y * x;

	if(x == x){
		z = z + x;

		if(y == z){
			x = z * y;
			y = x * y;

			if(x == x){
				x = x + y;
			}
			else{
				y = x + x;
			}

			x = x * y;
		}

		x = x * x;
		z = x + x;

		if(x < x){
			y = z + y;
			y = x * y;
			z = x + x;
		}

	}
	else{
		y = x * z;

		if(y != y){
			x = x * y;
			x = y + z;
			y = z + z;
		}

		y = x - y;
	}


	printf("%d", z);
}
