/* function auto-generated using seed: 9020984 */
void f83(int x, int y){
	int z = 1;
	x = y * y;

	if(x < z){
		z = x + x;
	}
	else{
		y = y * y;

		if(y < y){
			x = z * y;
			z = z - y;

			if(y < z){
				x = z + y;
				y = y - z;
			}
			else{
				y = y + z;
				y = x * x;
				y = y + x;
			}

			z = y + y;
		}

		y = z * x;
	}

	z = x + x;

	if(z > z){
		z = z - y;
		z = y - z;
	}


	printf("%d", z);
}
