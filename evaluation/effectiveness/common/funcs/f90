/* function auto-generated using seed: 6095437 */
void f90(int x, int y){
	int z = 1;
	x = z + z;

	if(z > y){
		x = x + y;

		if(z != z){
			y = x + z;

			if(x >= y){
				z = x + x;
				y = x + y;
			}
			else{
				x = z - y;
				y = x + y;
				x = y - z;
			}

			z = x - z;
			x = y - z;

			if(x < y){
				z = x * z;
				z = z * x;
				z = x * x;
			}
			else{
				y = y - x;
				x = y - y;
				z = y + z;
			}

		}

		z = x + y;

		if(x != z){
			y = y - y;
			y = y - x;
		}
		else{
			z = x - x;
			y = z - y;
			y = y + z;
		}

	}
	else{
		x = x + z;

		if(z < y){
			y = y * x;
		}

	}


	printf("%d", z);
}
