/* function auto-generated using seed: 5659770 */
void f93(int x, int y){
	int z = 1;
	x = y * y;

	if(y < x){
		y = y * x;

		if(z == z){
			z = x - y;

			if(y <= x){
				x = x - y;
				x = x + y;
				y = y + x;
			}
			else{
				y = x - y;
				z = z - y;
				y = x + y;
			}

		}
		else{
			x = x + x;
			z = x * x;
		}

		y = x * z;
	}

	x = x * x;

	if(y >= y){
		y = y - y;
		x = x + y;
		x = y * z;
	}

	z = z - z;

	if(y == z){
		z = y + y;
		z = y - z;
	}


	printf("%d", z);
}
