/* function auto-generated using seed: 7489620 */
void f103(int x, int y){
	int z = 1;
	x = x * x;

	if(x <= z){
		x = z * y;
		x = y - x;
		x = x + x;

		if(x >= x){
			y = z * y;
			x = x + x;

			if(y > y){
				z = x + y;
				z = y * z;
				y = x + y;
			}
			else{
				x = x + z;
				x = x + y;
				z = x + x;
			}

			x = y - y;
		}

	}

	z = y - z;

	if(z <= y){
		x = y + y;
	}


	printf("%d", z);
}
