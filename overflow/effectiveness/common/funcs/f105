/* function auto-generated using seed: 7154401 */
void f105(int x, int y){
	int z = 1;
	y = x * y;

	if(z < x){
		x = y * z;

		if(z <= z){
			x = x - x;
			y = x * z;

			if(z >= x){
				y = y * y;
				y = z * x;
				z = y * x;
			}

			y = y - x;

			if(y < x){
				y = x - x;
				y = y - x;
			}
			else{
				y = x + y;
				z = y * x;
				y = x * z;
			}

		}

		x = y + x;
		x = y * y;

		if(y >= z){
			z = x - z;
			y = x * y;
			y = z * z;

			if(y < y){
				y = z * y;
				x = y + z;
			}
			else{
				z = y * y;
			}

		}

	}
	else{
		z = x - z;
		y = x * z;

		if(z < y){
			z = y + z;
			y = x - x;

			if(y <= y){
				x = x - x;
				x = y - y;
				x = z + x;
			}
			else{
				x = z + y;
				z = y - z;
			}

		}
		else{
			y = y + y;
			x = x - z;
			x = x * x;
		}

	}

	x = z * x;

	if(z > x){
		y = z + x;
		z = x + x;
		z = x * y;
	}


	printf("%d", z);
}
