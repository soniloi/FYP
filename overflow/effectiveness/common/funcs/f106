/* function auto-generated using seed: 4520037 */
void f106(int x, int y){
	int z = 1;
	y = y * y;

	if(x == y){
		z = z - x;

		if(x <= y){
			z = z * x;

			if(y <= y){
				y = y * y;
			}

			y = y * y;
		}
		else{
			z = x * z;
			z = x + z;
		}

	}

	x = x + y;

	if(y <= x){
		x = z * x;

		if(y < z){
			x = x * z;

			if(z != z){
				y = y * z;
				z = x - x;
			}

			x = x - x;

			if(y >= y){
				z = x * z;
				x = x + x;
				z = x - y;
			}

			y = x * x;
		}
		else{
			z = y * z;

			if(z >= y){
				z = z * z;
				y = y + z;
				y = z + y;
			}

		}

		x = z * x;

		if(x < z){
			z = x * z;
			x = x + z;
			x = x + z;

			if(y == x){
				y = y - z;
				x = z + x;
			}
			else{
				y = z + z;
				z = z - z;
				x = y - y;
			}

		}
		else{
			y = y - y;

			if(x < z){
				y = x + x;
				y = z + x;
				x = z * z;
			}
			else{
				z = x * y;
				x = z - y;
			}

			y = x + z;
			x = y - z;

			if(y < z){
				y = y - z;
			}

		}

		y = x + y;
	}


	printf("%d", z);
}
