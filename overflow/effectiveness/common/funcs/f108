/* function auto-generated using seed: 4920282 */
void f108(int x, int y){
	int z = 1;
	z = z + x;

	if(x != x){
		y = y + z;
		x = x * z;

		if(x >= z){
			x = x * z;
			z = z * x;
		}
		else{
			y = z + y;
		}

	}

	y = z + y;

	if(z <= y){
		y = y * x;

		if(y > y){
			x = x * y;
			x = x - x;

			if(x >= z){
				z = z + y;
				x = z * z;
			}

			z = x + y;
		}
		else{
			x = x + x;

			if(y != x){
				z = y * y;
			}

			y = x * y;

			if(z == y){
				y = z * x;
			}

			y = x + x;
		}

	}


	printf("%d", z);
}
