/* function auto-generated using seed: 7936741 */
void f109(int x, int y){
	int z = 1;
	x = y * y;

	if(x != x){
		y = y + y;
		z = z + z;
		y = z - y;
	}
	else{
		z = x + y;

		if(z != x){
			z = x - y;
			y = z * y;
			y = y - z;
		}
		else{
			x = x - y;
		}

		x = z * y;
		y = x + y;

		if(y < y){
			x = y * x;
		}
		else{
			z = y - x;
			x = y - y;
			x = y + x;
		}

	}


	printf("%d", z);
}
