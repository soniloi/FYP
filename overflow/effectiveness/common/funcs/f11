/* function auto-generated using seed: 9797296 */
void f11(int x, int y){
	int z = 1;
	x = z + y;

	if(x == z){
		z = z - y;

		if(x < x){
			y = y * z;

			if(x >= z){
				x = y + x;
				y = x + z;
			}
			else{
				x = y - y;
				z = z - x;
			}

			y = z + y;

			if(y <= z){
				z = x + z;
			}
			else{
				z = z - y;
			}

		}
		else{
			y = x + x;

			if(z <= z){
				y = z * z;
				z = z * x;
				z = z + y;
			}

		}

	}

	y = z * x;

	if(y < z){
		y = x * z;
		x = x - z;

		if(z != y){
			z = x * x;
			y = y * x;
			y = x + x;
		}

	}

	y = z + z;

	if(y <= z){
		z = y + x;
		x = y * z;
		z = z + x;
	}


	printf("%d", z);
}
