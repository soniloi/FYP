/* function auto-generated using seed: 3954899 */
void f114(int x, int y){
	int z = 1;
	x = x + x;

	if(x > x){
		z = y + y;

		if(x >= x){
			y = x - y;
			z = y - x;
		}

		y = x + z;

		if(y >= x){
			y = y - y;

			if(z >= z){
				z = x + z;
				z = y + y;
				x = x - y;
			}
			else{
				z = z + z;
			}

			z = x * y;
		}

	}
	else{
		y = z + x;
		y = x * y;
	}


	printf("%d", z);
}
