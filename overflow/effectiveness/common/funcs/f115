/* function auto-generated using seed: 6629235 */
void f115(int x, int y){
	int z = 1;
	y = y * z;

	if(z == z){
		x = z - z;
		y = x + x;

		if(x <= y){
			z = y * x;
			x = y + z;
		}

		y = y - z;

		if(z >= z){
			x = x - x;
		}
		else{
			z = z + z;
			y = z * z;
			x = z * x;
		}

	}


	printf("%d", z);
}
