/* function auto-generated using seed: 9728639 */
void f124(int x, int y){
	int z = 1;
	x = y * z;

	if(z <= x){
		y = z * z;
	}
	else{
		y = x + x;
		y = y - y;
		z = z - z;

		if(y > y){
			x = x * z;
		}

	}

	y = y * z;

	if(x == y){
		z = x * x;

		if(z <= y){
			y = y * x;

			if(x < x){
				y = x - x;
			}

		}
		else{
			y = x * z;
			z = z * z;

			if(y < z){
				z = x - x;
			}

			x = y - y;

			if(y <= z){
				y = z + y;
			}

		}

		y = y - y;

		if(y != z){
			z = x * x;
			y = x - y;

			if(y == x){
				z = y * z;
				y = x - x;
			}
			else{
				x = x * y;
			}

		}
		else{
			y = y * z;
			y = y - x;
			x = x * y;
		}

		y = y - x;
	}

	z = y * z;

	if(x > x){
		y = z - x;

		if(y > y){
			x = y * x;
			x = y - y;
			x = x * z;
		}
		else{
			y = y + x;
			z = y + y;
		}

		z = x + x;
	}
	else{
		y = y - y;

		if(y < z){
			y = z + y;
			y = z - x;

			if(z != y){
				z = y + z;
				y = x + y;
			}

		}
		else{
			z = z + z;

			if(x < z){
				y = z * z;
				x = z * y;
				x = y * x;
			}
			else{
				y = x * x;
				z = y - x;
			}

			x = x + x;

			if(z < z){
				x = y * z;
				y = x + x;
			}
			else{
				x = y * z;
			}

		}

		z = y * x;

		if(x < y){
			y = y + y;
		}
		else{
			y = y * y;

			if(x > z){
				x = z + y;
			}
			else{
				x = x * x;
				y = y - y;
				z = z + y;
			}

			y = x + z;
		}

		x = z + z;

		if(x != y){
			y = x - y;
		}
		else{
			y = x * x;
			y = y - y;
		}

	}


	printf("%d", z);
}
