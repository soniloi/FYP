/* function auto-generated using seed: 6653805 */
void f126(int x, int y){
	int z = 1;
	x = y - x;

	if(y > z){
		z = y - z;

		if(x >= z){
			z = x * z;
			x = z * y;
			y = x * x;
		}
		else{
			y = x * x;

			if(x >= x){
				y = x - x;
				x = y + z;
				y = y - x;
			}

			y = x + y;
			y = x - z;

			if(x == x){
				x = y + z;
				z = x - y;
			}
			else{
				y = x - x;
			}

		}

	}
	else{
		y = z - x;
		y = z * x;

		if(x != x){
			z = x + y;
		}

		y = x - y;

		if(y < z){
			z = z * z;
		}

	}

	y = x + x;

	if(x > x){
		z = x - x;

		if(y > z){
			x = y - y;
			z = y * x;
			z = x * x;
		}

		x = y + y;
	}


	printf("%d", z);
}
