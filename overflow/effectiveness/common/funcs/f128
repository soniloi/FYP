/* function auto-generated using seed: 6003208 */
void f128(int x, int y){
	int z = 1;
	z = x + x;

	if(y <= y){
		z = z - y;
		y = y + y;
		x = y + x;
	}

	z = z - z;

	if(x > y){
		z = z + z;

		if(z >= z){
			x = x * x;

			if(z < z){
				x = y + y;
				y = y * x;
				x = z * y;
			}

			z = x + x;

			if(x != x){
				x = y * y;
			}
			else{
				z = y - x;
				x = x + x;
				y = y - z;
			}

			z = x + z;

			if(z <= y){
				x = z * z;
			}

		}

	}


	printf("%d", z);
}
