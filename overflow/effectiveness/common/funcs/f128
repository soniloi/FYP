/* function auto-generated using seed: 6003208 */
void f128(int x, int y){
	int z = 1;
	z = x + x;

	if(y <= y){
		z = z - y;
		y = y + y;
		x = y + x;
	}
	else{
		z = y + x;
	}

	y = y + z;

	if(z == z){
		x = z * x;
	}
	else{
		z = z - z;
		x = y + y;
		y = x * x;

		if(x < y){
			x = z * z;
		}

	}


	printf("%d", z);
}
