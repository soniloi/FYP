/* function auto-generated using seed: 2914218 */
void f129(int x, int y){
	int z = 1;
	z = x - z;

	if(y <= z){
		y = z * z;

		if(z >= z){
			z = y + y;
			z = z - z;
			y = z - z;

			if(z >= z){
				z = z + x;
				y = y * x;
			}
			else{
				x = z + y;
			}

		}
		else{
			z = z - x;

			if(z == x){
				y = x - y;
			}

			x = z + x;
		}

		x = z + z;

		if(z != y){
			z = y * y;
			y = x - x;
			y = z + x;

			if(y == z){
				z = z - y;
				x = x - z;
				z = y + y;
			}
			else{
				x = z + x;
				y = z * y;
			}

		}
		else{
			z = x - z;

			if(z != x){
				z = z + z;
				y = x - x;
				y = y - y;
			}
			else{
				x = y - y;
				x = y + z;
				z = z * z;
			}

			z = z - y;
		}

		z = z + z;
	}
	else{
		z = y + x;
	}


	printf("%d", z);
}
