/* function auto-generated using seed: 1091238 */
void f130(int x, int y){
	int z = 1;
	z = z + y;

	if(z <= z){
		x = x - z;

		if(z >= z){
			x = z * y;

			if(z != z){
				z = z + x;
				y = z + x;
				x = z + x;
			}

			y = x * z;
			y = x + y;

			if(z >= y){
				z = y * y;
				z = z * y;
			}
			else{
				x = y - z;
				y = z * z;
			}

		}
		else{
			z = x * x;
		}

		y = x - z;
		x = y * x;

		if(z != z){
			x = y + y;

			if(z > z){
				z = x - z;
				z = y + x;
				y = z + z;
			}
			else{
				z = x + z;
			}

			y = z * x;
		}
		else{
			x = z * y;
			x = y + y;
			z = z * y;

			if(y != z){
				z = z * z;
			}

		}

	}
	else{
		y = y - z;

		if(z != x){
			x = z - x;
			y = y - z;

			if(x == y){
				x = y * y;
				x = x + x;
				y = x * x;
			}

		}
		else{
			x = z - x;
		}

		z = z - z;

		if(x >= z){
			z = x - z;

			if(z > y){
				z = z - y;
				y = y - z;
			}
			else{
				x = y * z;
				x = x * y;
				y = y - x;
			}

			y = x * x;

			if(y > x){
				z = y * x;
				z = y - y;
			}
			else{
				y = z + y;
				z = z * z;
				y = x - y;
			}

			x = z - y;
		}

		y = z - z;

		if(z == y){
			z = y - y;

			if(z < z){
				x = x * x;
				z = y * x;
			}
			else{
				x = y * z;
				z = x + y;
				x = x * y;
			}

			x = z + x;

			if(z > z){
				x = z * x;
			}

			z = x + z;
		}

	}

	x = z - y;

	if(y <= z){
		z = y * y;

		if(x != y){
			y = x * y;
			y = x * z;

			if(z > x){
				z = z * y;
				y = x + x;
			}
			else{
				x = x * y;
			}

		}

	}


	printf("%d", z);
}
