/* function auto-generated using seed: 9410736 */
void f136(int x, int y){
	int z = 1;
	z = x + x;

	if(y < z){
		y = z - x;

		if(x >= y){
			x = z - x;
		}

		y = y + z;
	}

	x = y + y;

	if(z == y){
		x = y + x;

		if(x == z){
			y = x + x;
			z = z + z;
			x = x + x;
		}
		else{
			y = x + y;
			y = x * z;

			if(z < y){
				z = x * y;
			}
			else{
				z = y + z;
			}

			x = z - x;
		}

		y = y * x;

		if(y < y){
			y = y + x;
			y = x * x;

			if(z > y){
				x = z - z;
				x = z - x;
				y = y - x;
			}

		}

		x = z * y;

		if(z >= x){
			z = y * y;
		}
		else{
			z = z - y;

			if(x == x){
				x = z + x;
			}
			else{
				z = y + y;
			}

			y = x * z;
		}

	}
	else{
		z = y - y;

		if(x < z){
			x = y - x;
		}
		else{
			x = x + x;
			y = z - y;

			if(x != z){
				z = z - y;
			}

		}

	}

	z = x * z;

	if(x != z){
		z = y + y;

		if(y > z){
			z = z * z;

			if(x != z){
				z = x + y;
				x = z - x;
				z = z * y;
			}

			y = z - z;
			x = x - x;

			if(z >= y){
				x = x - y;
			}
			else{
				x = z + z;
				z = z * y;
			}

		}

	}
	else{
		z = z - z;

		if(y > y){
			y = y + x;

			if(z != y){
				z = y - x;
				y = x + z;
			}

			z = z + y;
		}

	}


	printf("%d", z);
}
