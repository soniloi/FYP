/* function auto-generated using seed: 8084130 */
void f139(int x, int y){
	int z = 1;
	x = y - y;

	if(z <= z){
		z = z - y;

		if(x <= y){
			x = z * y;
			z = y * z;
			y = x - y;

			if(z != z){
				x = x + y;
			}

		}
		else{
			z = x * z;
			z = x - y;

			if(y < z){
				x = x + y;
			}
			else{
				y = x * y;
				z = x - x;
			}

			y = x - x;
		}

		x = z + z;
		y = z * x;
	}
	else{
		x = x + x;

		if(z > y){
			x = z + x;

			if(x == z){
				y = z * z;
				x = z - y;
				z = z * x;
			}

		}
		else{
			x = z * z;
		}

		y = x * z;
		y = z - z;
	}


	printf("%d", z);
}
