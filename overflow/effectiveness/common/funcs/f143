/* function auto-generated using seed: 3380171 */
void f143(int x, int y){
	int z = 1;
	z = z - z;

	if(z == y){
		y = z + z;

		if(y <= x){
			z = z * y;

			if(y >= y){
				y = y * y;

				if(x <= z){
					z = y - x;
				}

			}
			else{
				y = x * z;
			}

			x = x - z;
		}

		x = y * x;
	}
	else{
		x = y * z;
		y = z * z;

		if(y < x){
			y = x + x;
			x = z + z;
			z = x - x;
		}

		z = y - x;
	}


	printf("%d", z);
}
