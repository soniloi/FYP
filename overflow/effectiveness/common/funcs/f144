/* function auto-generated using seed: 7287201 */
void f144(int x, int y){
	int z = 1;
	x = x + z;

	if(y > x){
		z = x - z;
	}

	z = y + y;

	if(z == y){
		z = x * y;
		y = z - z;
	}
	else{
		z = z - z;
		x = z - y;
	}

	x = y + z;

	if(z > x){
		x = x - y;
	}
	else{
		z = z * y;
	}


	printf("%d", z);
}
