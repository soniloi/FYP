/* function auto-generated using seed: 7287201 */
void f144(int x, int y){
	int z = 1;
	x = x + z;

	if(y > x){
		z = x - z;
	}

	z = y + y;

	if(z == y){
		z = x * y;
		y = z - z;
	}

	y = z + y;

	if(x <= z){
		y = y * y;

		if(z >= z){
			x = z * x;
			y = x * z;

			if(z <= y){
				z = z + x;
				z = z + z;
				y = z + x;
			}

		}
		else{
			x = z + x;

			if(x > z){
				y = z * z;
			}
			else{
				y = x * z;
				z = x + y;
			}

			x = x * y;

			if(x > y){
				z = z + z;
				y = y - z;
			}

			z = y - x;
		}

		y = z + y;

		if(x < x){
			y = y * x;
			x = z * z;
		}
		else{
			x = z * z;

			if(z == x){
				y = y + x;
				x = x - y;
				y = y - x;
			}

			x = y + x;
		}

	}
	else{
		x = z + x;

		if(z != y){
			y = x + z;
			x = x * x;
		}

	}


	printf("%d", z);
}
