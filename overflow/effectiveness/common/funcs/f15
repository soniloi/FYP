/* function auto-generated using seed: 8708321 */
void f15(int x, int y){
	int z = 1;
	z = y + x;

	if(x < y){
		z = z - x;

		if(x == y){
			y = y * x;

			if(x > z){
				z = x - z;
				x = z * z;
				x = y + x;
			}

			x = x * y;
		}

		x = z * z;
	}

	y = z * z;

	if(x < y){
		z = x + z;
	}


	printf("%d", z);
}
