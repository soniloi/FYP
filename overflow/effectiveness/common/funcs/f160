/* function auto-generated using seed: 7914670 */
void f160(int x, int y){
	int z = 1;
	y = x + z;

	if(x != x){
		z = z * z;
	}
	else{
		y = z * y;
	}

	z = x + z;

	if(y == y){
		y = y * x;
	}
	else{
		x = z + y;
		z = y * z;

		if(y < z){
			y = x * z;
			z = y * x;

			if(z >= y){
				x = y * y;
				z = x + y;
			}

		}
		else{
			z = z - x;
			z = y + z;
		}

	}


	printf("%d", z);
}
