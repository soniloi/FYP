/* function auto-generated using seed: 9367925 */
void f162(int x, int y){
	int z = 1;
	y = z * y;

	if(y != y){
		z = x - x;

		if(y < x){
			x = y - y;
			x = y + y;

			if(z >= x){
				z = y * x;
			}
			else{
				z = z * x;
				x = z + z;
			}

			z = z - x;
		}

		y = z + x;
	}


	printf("%d", z);
}
