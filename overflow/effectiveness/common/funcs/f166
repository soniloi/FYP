/* function auto-generated using seed: 8293257 */
void f166(int x, int y){
	int z = 1;
	z = z + z;

	if(y < z){
		y = z + z;
	}
	else{
		z = x - x;
		y = y + y;
	}

	z = x + x;

	if(x >= z){
		x = z - z;

		if(y > z){
			z = z - x;
		}
		else{
			z = y + z;

			if(x <= z){
				z = z + z;
				z = x * x;
				x = y * z;
			}

			x = y + x;
			x = x * x;

			if(x >= x){
				z = x * x;
				x = z - y;
				x = z - y;
			}
			else{
				y = y + y;
				y = y * y;
				x = y * y;
			}

		}

	}

	x = x * y;

	if(y > y){
		z = z - x;

		if(x > x){
			y = y * y;
		}
		else{
			z = z - x;
			x = x - z;

			if(y != x){
				x = z - x;
				y = y * z;
				z = x + y;
			}
			else{
				z = y + y;
				y = y - x;
			}

			x = z * x;
		}

		z = z + z;

		if(z > x){
			z = x + z;
		}
		else{
			z = z + x;
			z = x * y;

			if(y > y){
				z = z + y;
				x = y - x;
				y = x + x;
			}
			else{
				x = x * x;
				z = z * x;
				x = y * z;
			}

		}

	}
	else{
		z = y * y;

		if(z >= y){
			x = z + y;

			if(x > y){
				x = z - z;
				z = y * x;
			}

			z = z * y;

			if(z <= z){
				z = x - z;
			}

			z = x + x;
		}
		else{
			y = x - x;
		}

		x = z + x;
	}


	printf("%d", z);
}
