/* function auto-generated using seed: 8983633 */
void f167(int x, int y){
	int z = 1;
	z = z - z;

	if(y == y){
		z = x + x;

		if(z <= y){
			x = z + y;
			x = y * y;

			if(z > y){
				y = x - y;
				x = z + z;
			}

			y = y * x;

			if(z > z){
				x = y + z;
				x = z - x;
			}
			else{
				y = z * y;
				z = y + x;
			}

		}

		x = z * y;

		if(x < z){
			x = z - x;

			if(y <= y){
				y = x * z;
			}

			y = x * x;

			if(y != x){
				y = x + y;
			}
			else{
				y = z + z;
			}

		}
		else{
			x = z * y;

			if(x <= y){
				z = x + y;
				x = x * z;
			}
			else{
				y = y - x;
				y = x + x;
				x = z + x;
			}

			y = z * y;
			y = z + z;

			if(x != z){
				y = y * x;
				z = z * y;
			}
			else{
				y = x * y;
				x = y * z;
				x = x * z;
			}

		}

		x = y * z;

		if(x > x){
			z = y - y;
			z = x + z;
			x = y * x;

			if(y == z){
				x = z + y;
			}

		}

	}
	else{
		x = z + x;

		if(y != x){
			y = z + z;
			y = z - y;
		}

		x = y + x;
	}

	z = z * x;

	if(x >= x){
		x = z + z;

		if(y > z){
			y = z - y;

			if(x < y){
				x = x * y;
				x = y + x;
				x = z * z;
			}
			else{
				x = x - x;
				z = x * x;
				x = x + z;
			}

			y = x + x;
			z = z * x;
		}
		else{
			x = y + y;

			if(y > y){
				z = z - y;
			}
			else{
				x = z + y;
				x = z + x;
				z = x + z;
			}

			x = y * y;
		}

		z = z * z;
		z = z * y;

		if(z != z){
			y = z + y;
		}
		else{
			y = y - z;
		}

	}


	printf("%d", z);
}
