/* function auto-generated using seed: 4536574 */
void f177(int x, int y){
	int z = 1;
	z = y - x;

	if(y < x){
		y = x * z;
		x = y + x;
	}
	else{
		y = y + z;

		if(z <= x){
			z = x + x;
			z = x * x;
		}
		else{
			y = x - z;

			if(x < z){
				z = z - x;
			}

		}

	}

	y = y - z;

	if(y > y){
		x = x - y;

		if(x >= y){
			y = y * x;
		}

		x = x - x;
	}


	printf("%d", z);
}
