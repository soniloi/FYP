/* function auto-generated using seed: 3933011 */
void f181(int x, int y){
	int z = 1;
	x = z + y;

	if(x != y){
		z = y * y;

		if(z != z){
			y = x * x;
			y = z * y;
			z = y + z;
		}

		y = x - x;

		if(z > y){
			y = y + y;
			y = x * z;
			z = y - z;
		}
		else{
			x = y + z;

			if(x == y){
				y = z - z;
			}
			else{
				z = y + x;
			}

			z = y - y;
		}

	}


	printf("%d", z);
}
