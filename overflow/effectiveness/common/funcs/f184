/* function auto-generated using seed: 2802801 */
void f184(int x, int y){
	int z = 1;
	y = z + x;

	if(z > y){
		z = y - x;

		if(z != y){
			y = y + z;
			z = y * z;

			if(y > z){
				y = x + y;
			}

			x = z + x;

			if(y != z){
				y = z + x;
				z = z + z;
			}

		}
		else{
			z = y + x;

			if(z != z){
				y = y - z;
				y = y - z;
			}

		}

		x = z * z;

		if(z >= x){
			z = x + z;

			if(x >= y){
				y = z - x;
			}
			else{
				x = x + x;
			}

			z = z - z;
			z = y + y;
		}

	}


	printf("%d", z);
}
