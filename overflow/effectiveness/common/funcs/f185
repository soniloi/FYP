/* function auto-generated using seed: 8247520 */
void f185(int x, int y){
	int z = 1;
	x = x + y;

	if(z >= z){
		y = z * x;
	}

	x = z + y;

	if(y <= z){
		y = z + x;

		if(z == z){
			y = x - x;
			z = y - z;
			z = x + y;
		}

	}


	printf("%d", z);
}
