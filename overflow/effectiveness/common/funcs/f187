/* function auto-generated using seed: 5323572 */
void f187(int x, int y){
	int z = 1;
	y = x + y;

	if(z <= x){
		x = z + x;
		z = z * y;
	}

	z = x + z;

	if(x == y){
		z = y * y;

		if(x > x){
			y = y + x;
			z = z - x;
			z = y * z;

			if(y > z){
				y = z * y;
				x = x * z;
				x = z * y;
			}

		}
		else{
			z = x + x;
		}

		z = z * y;
	}
	else{
		y = x + y;
		x = y - z;

		if(z < x){
			x = x - x;
			z = y - x;

			if(y > y){
				y = x - z;
				y = y + y;
			}
			else{
				x = x - x;
			}

			z = z + z;

			if(y == x){
				z = x - y;
				x = x - x;
			}

		}
		else{
			y = x * y;
			y = y * x;
		}

	}

	x = y * y;

	if(y > y){
		y = z + z;
	}
	else{
		z = z - x;

		if(y > z){
			z = z - x;

			if(z <= z){
				z = y + z;
				z = z * x;
				z = x * x;
			}
			else{
				y = y * y;
				y = x - y;
			}

			z = y - x;
			z = z * x;

			if(y < x){
				z = x * z;
				y = x * z;
			}

		}
		else{
			z = z + y;
			x = z - x;
			z = y + y;
		}

		z = x - x;
		z = z + x;
	}


	printf("%d", z);
}
