/* function auto-generated using seed: 7151329 */
void f193(int x, int y){
	int z = 1;
	y = z + z;

	if(y != y){
		x = x - y;
		y = y * y;
		x = x + y;
	}

	x = x + z;

	if(y != y){
		x = y + y;
	}
	else{
		y = y - z;
		x = y - y;

		if(x != z){
			y = y * x;

			if(x <= y){
				x = z * x;
			}
			else{
				x = x * y;
				y = z + z;
				x = x + y;
			}

			z = x * x;
		}

	}

	x = y + z;

	if(y < y){
		x = y + z;
	}


	printf("%d", z);
}
