/* function auto-generated using seed: 7151329 */
void f193(int x, int y){
	int z = 1;
	y = z + z;

	if(y != y){
		x = x - y;
		y = y * y;
		x = x + y;
	}

	x = x + z;

	if(y != y){
		x = y + y;
	}
	else{
		y = y - z;
		x = y - y;

		if(x != z){
			y = y * x;

			if(x <= y){
				x = z * x;
			}
			else{
				x = x * x;
				y = x - z;
			}

			z = z * x;
		}

	}

	y = y + x;

	if(x > z){
		y = z + y;
		y = y + x;

		if(z != x){
			x = z - x;

			if(x == y){
				y = y * z;
				x = z * x;
				x = x - y;
			}
			else{
				x = x + x;
				z = z - y;
			}

		}

		x = x - x;

		if(x <= z){
			y = z - z;
			x = y * x;

			if(x >= y){
				z = x + x;
				x = y - y;
			}

		}

	}


	printf("%d", z);
}
