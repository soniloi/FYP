/* function auto-generated using seed: 8800345 */
void f198(int x, int y){
	int z = 1;
	y = x * y;

	if(y >= x){
		y = z * x;
		x = z + y;
	}
	else{
		x = y + z;
		x = y + y;
		y = x * z;
	}

	x = y * x;

	if(y != x){
		z = z - y;
	}
	else{
		y = y - z;

		if(z <= y){
			z = y + z;
			x = y - y;
		}
		else{
			z = z * x;

			if(y <= z){
				x = z - x;
				x = z * y;
			}

			y = x * y;
			x = z + x;
		}

		z = z * y;

		if(z < z){
			y = y + x;

			if(y <= y){
				y = y + x;
				z = x * z;
			}

			y = x * z;
		}

	}


	printf("%d", z);
}
