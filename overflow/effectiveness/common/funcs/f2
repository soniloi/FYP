/* function auto-generated using seed: 5343372 */
void f2(int x, int y){
	int z = 1;
	y = x * z;

	if(x == z){
		z = z * x;
		z = x + x;
	}
	else{
		z = x + x;
	}

	x = z * y;

	if(x == x){
		z = z * y;
	}

	x = y + y;

	if(z <= x){
		y = y + z;

		if(x <= x){
			z = y * x;

			if(x > z){
				z = z + x;
			}

			y = x - x;
		}
		else{
			y = y + z;
			z = y + x;

			if(x >= x){
				z = y - x;
			}

		}

		x = x + y;

		if(x <= x){
			x = z * z;
			y = y + y;

			if(x < x){
				x = z * z;
			}

			y = y * z;
		}
		else{
			y = z - y;
			x = x * y;
			x = x - y;
		}

		z = z + y;

		if(x != z){
			y = y * z;
			y = x - y;

			if(z > x){
				z = z + z;
			}

			y = z + x;
		}

	}
	else{
		x = x * y;

		if(z <= x){
			x = y + z;
		}

	}


	printf("%d", z);
}
