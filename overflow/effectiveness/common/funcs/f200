/* function auto-generated using seed: 1335706 */
void f200(int x, int y){
	int z = 1;
	y = y * y;

	if(z >= x){
		y = z * z;
		y = x - z;
		z = x - y;

		if(x == x){
			y = z + x;
			z = z - x;

			if(z == y){
				x = x - y;
				z = z * z;
			}
			else{
				z = y + z;
			}

		}
		else{
			z = z + x;
			z = x + y;

			if(y < z){
				y = y + x;
				y = x * y;
				y = z * y;
			}
			else{
				x = z - y;
				x = z + y;
			}

			z = y * x;

			if(y <= x){
				y = y + z;
				y = x - x;
			}
			else{
				z = z * z;
				z = y * x;
				z = x * y;
			}

		}

	}

	z = x + y;

	if(y != y){
		y = y * z;

		if(z > z){
			x = y - x;

			if(x == x){
				y = y + x;
				x = z * x;
				y = x - y;
			}

			y = y * x;
		}
		else{
			z = y - z;
		}

		z = y - z;

		if(z > z){
			x = z + y;
			y = z + z;
			y = z - z;
		}

	}
	else{
		x = z * y;

		if(x == y){
			x = z - x;

			if(x != x){
				y = z - x;
			}
			else{
				z = z + x;
				z = x + z;
				x = x - x;
			}

			y = z - x;
			x = y + y;
		}
		else{
			y = z + y;
			x = x + z;
			y = z - y;
		}

	}

	x = x - y;

	if(x == z){
		x = y + y;
		x = z - y;

		if(x >= y){
			x = x + y;

			if(z > y){
				x = x + y;
				y = z * x;
			}
			else{
				z = z + y;
				x = x + z;
				x = z + x;
			}

			x = x - z;
			x = z - z;
		}

		x = z + z;

		if(z <= z){
			z = z * z;
			y = z * y;

			if(z != y){
				z = z * z;
			}
			else{
				y = x * y;
				x = x + z;
				z = y * z;
			}

		}
		else{
			x = y - x;

			if(x >= x){
				x = z * y;
			}

			y = z * x;
		}

	}
	else{
		y = y * y;
		y = y * y;
		x = x + z;
	}


	printf("%d", z);
}
