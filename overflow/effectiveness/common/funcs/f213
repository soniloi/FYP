/* function auto-generated using seed: 2664684 */
void f213(int x, int y){
	int z = 1;
	x = z + z;

	if(z == y){
		x = x - z;

		if(z != y){
			y = y + y;

			if(x == y){
				z = y + z;
				z = z + y;
				z = y - y;
			}

			x = z * y;
		}

		y = x * z;

		if(y != y){
			y = x + y;
			z = y - x;
			z = z - x;
		}
		else{
			z = y - y;
			x = x + x;
		}

	}


	printf("%d", z);
}
