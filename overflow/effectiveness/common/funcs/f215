/* function auto-generated using seed: 9768890 */
void f215(int x, int y){
	int z = 1;
	z = z - z;

	if(y < x){
		z = y + z;

		if(x >= y){
			z = x * z;
		}

	}
	else{
		z = z * y;

		if(y >= z){
			x = y - z;
		}
		else{
			x = z + y;
			x = y * y;
		}

		y = z * y;

		if(z <= y){
			z = x + y;
		}
		else{
			x = y * z;
			z = z + y;
		}

		x = y * x;
	}


	printf("%d", z);
}
