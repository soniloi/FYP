/* function auto-generated using seed: 9768890 */
void f215(int x, int y){
	int z = 1;
	z = z - z;

	if(y < x){
		z = y + z;

		if(x >= y){
			z = x * z;
		}

		y = x + z;
	}
	else{
		y = x + z;
	}

	y = y + x;

	if(y >= z){
		y = y * y;
		y = y - z;
	}


	printf("%d", z);
}
