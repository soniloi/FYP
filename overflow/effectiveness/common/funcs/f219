/* function auto-generated using seed: 3820878 */
void f219(int x, int y){
	int z = 1;
	y = z + z;

	if(y != y){
		z = z + x;

		if(z == x){
			x = x - y;

			if(y < x){
				z = x + z;
			}

			y = z + z;

			if(x == z){
				y = z - x;
			}
			else{
				x = y * x;
				y = y * z;
			}

		}

		z = x * x;
		z = x - x;
	}
	else{
		y = x * z;
	}

	z = x - z;

	if(x <= y){
		z = x - z;
		x = z + x;

		if(y != z){
			y = y + y;
			x = x * z;
		}

	}


	printf("%d", z);
}
