/* function auto-generated using seed: 331989 */
void f222(int x, int y){
	int z = 1;
	y = y + z;

	if(x > z){
		x = z * z;
		x = x * x;
	}

	y = z - x;

	if(x >= y){
		z = y * z;
		y = y - y;
		x = x + x;

		if(z > y){
			z = x * y;
			y = x * x;
			x = y - y;
		}
		else{
			y = x * z;
			y = z * y;

			if(x != z){
				x = y * z;
				x = x + x;
			}
			else{
				z = z * y;
				y = z - x;
				z = y + z;
			}

			z = x * x;
		}

	}
	else{
		x = y * y;

		if(y == y){
			x = y - y;

			if(y <= y){
				z = z * z;
				z = y + y;
			}

		}

		z = y - z;

		if(y >= z){
			y = y + z;

			if(z == y){
				x = y * x;
				z = y - y;
			}

			z = x - x;
			y = y - z;
		}

	}


	printf("%d", z);
}
