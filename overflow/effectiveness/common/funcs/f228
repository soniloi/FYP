/* function auto-generated using seed: 3906094 */
void f228(int x, int y){
	int z = 1;
	x = z + z;

	if(x >= x){
		z = z * z;

		if(x <= y){
			y = x + y;
		}
		else{
			y = x * x;
		}

	}

	z = z + z;

	if(x <= z){
		x = z * x;
	}
	else{
		z = x * x;
		z = z - z;
		y = x * y;
	}


	printf("%d", z);
}
