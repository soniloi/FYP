/* function auto-generated using seed: 8273444 */
void f23(int x, int y){
	int z = 1;
	y = z + x;

	if(y > z){
		z = z * z;
		x = y - y;
	}

	z = z + x;

	if(y != x){
		x = x - x;

		if(z <= y){
			y = y + z;
			z = z * x;
		}

		z = z - x;

		if(x >= x){
			z = x * z;
		}
		else{
			z = z - x;
			z = y - y;

			if(z < z){
				y = z * y;
			}

			z = x - z;

			if(x <= z){
				y = z * x;
				y = z * x;
			}

		}

		y = y + y;

		if(y == z){
			x = z + y;

			if(y <= x){
				y = y * x;
			}

			z = y - z;
			x = z + y;

			if(z < y){
				y = z * x;
				x = y - x;
				z = y * z;
			}

		}

	}
	else{
		x = x - z;
	}

	z = z * y;

	if(x < z){
		x = x + y;
		z = y * y;
		z = x * z;
	}
	else{
		z = x - z;

		if(x >= y){
			y = z - x;
			y = z + y;
			x = z + y;
		}

	}


	printf("%d", z);
}
