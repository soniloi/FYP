/* function auto-generated using seed: 4954309 */
void f234(int x, int y){
	int z = 1;
	z = y * z;

	if(y <= y){
		x = y - y;
		z = z * y;

		if(z >= y){
			x = x - z;
			y = x - y;
		}

		x = y * x;

		if(x != y){
			z = z * x;

			if(y == z){
				x = y * y;
				x = z * z;
			}
			else{
				x = z - z;
			}

			y = x * y;

			if(z > y){
				y = y - z;
			}

			z = y * x;
		}

	}
	else{
		z = y - x;
	}

	y = y + z;

	if(y == z){
		z = z * x;

		if(z != y){
			y = y * z;

			if(y >= y){
				z = y * z;
				x = y + y;
				x = y + z;
			}

			z = z + z;
		}

		x = x + x;

		if(y > z){
			x = x * x;
			x = z - y;
			x = z - y;
		}

	}

	y = x + x;

	if(z >= x){
		x = x + x;

		if(x >= x){
			x = z + z;

			if(x > y){
				y = z + x;
				z = y - x;
				y = x - y;
			}

			x = z + x;
			y = x - x;

			if(x <= y){
				z = x - x;
				y = y + z;
				z = y + x;
			}

		}
		else{
			z = x * y;

			if(z < x){
				y = x + z;
				z = x - z;
				z = z + y;
			}
			else{
				x = z + y;
				y = y * x;
				z = x - z;
			}

		}

	}
	else{
		y = x * x;
		z = x - z;

		if(z < y){
			z = y - y;

			if(y != x){
				z = z * x;
				x = x * x;
			}

		}

		y = x * z;
	}


	printf("%d", z);
}
