/* function auto-generated using seed: 1179122 */
void f237(int x, int y){
	int z = 1;
	z = z * z;

	if(y != y){
		z = y - z;
		y = y + x;

		if(y > z){
			z = y * x;
			y = z * z;
			y = x * y;
		}

	}

	x = z + x;

	if(z != x){
		x = x + y;

		if(x == y){
			x = y + x;
			x = y * z;

			if(y == y){
				z = z + x;

				if(z <= y){
					z = x - x;
				}

				x = z + y;
				z = y + y;
			}

		}

	}


	printf("%d", z);
}
