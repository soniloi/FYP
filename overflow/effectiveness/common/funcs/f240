/* function auto-generated using seed: 3204120 */
void f240(int x, int y){
	int z = 1;
	y = z - x;

	if(y < x){
		y = y * z;
	}

	x = z + x;

	if(x < y){
		y = x + y;

		if(x < y){
			z = z + y;
			z = z + x;

			if(y == z){
				x = z - y;
			}
			else{
				y = y - y;
			}

			z = x + z;
		}

		x = y * x;

		if(z <= x){
			x = x - z;
		}

		y = x - z;
	}
	else{
		y = x + z;

		if(z > z){
			y = x + z;
			y = x - y;
		}

		z = y - y;
		y = x * z;
	}


	printf("%d", z);
}
