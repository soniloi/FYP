/* function auto-generated using seed: 2628705 */
void f245(int x, int y){
	int z = 1;
	x = y * y;

	if(z <= x){
		z = z + y;

		if(z == z){
			x = x - z;
			y = x + z;
		}
		else{
			z = x * x;

			if(z >= x){
				z = y - y;
				y = x + y;
			}

			z = x + y;

			if(y > y){
				x = z * z;
				x = x * x;
				z = z - z;
			}
			else{
				z = y * y;
			}

			x = z - y;
		}

	}
	else{
		x = z * x;

		if(y > y){
			z = z * z;
		}
		else{
			y = z - y;

			if(y > x){
				z = x + z;
				x = x * y;
				y = y + x;
			}
			else{
				x = z + y;
				y = y - x;
				y = x * x;
			}

		}

		y = x + z;
	}


	printf("%d", z);
}
