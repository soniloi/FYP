/* function auto-generated using seed: 3298142 */
void f249(int x, int y){
	int z = 1;
	z = x * z;

	if(x < y){
		y = y + z;
	}
	else{
		z = z * x;
		y = z - z;
	}

	x = x * x;

	if(z > y){
		y = z * y;
	}
	else{
		x = y - x;

		if(y >= x){
			x = y * z;

			if(y > y){
				y = z - y;
				z = x + z;
			}
			else{
				x = z + x;
			}

			y = y * x;
		}
		else{
			x = x * z;
		}

		z = y - z;
	}


	printf("%d", z);
}
