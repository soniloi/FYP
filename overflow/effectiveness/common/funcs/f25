/* function auto-generated using seed: 7708067 */
void f25(int x, int y){
	int z = 1;
	z = y + x;

	if(x > y){
		z = y * x;

		if(y >= x){
			x = y + y;
			x = y - y;
			y = y + x;
		}
		else{
			x = y * z;
			z = y + x;
		}

		x = x * z;

		if(y <= z){
			y = z - z;
		}

		x = y - y;

		if(y > z){
			z = y + x;
			y = y - y;
			z = y + x;
		}

	}


	printf("%d", z);
}
