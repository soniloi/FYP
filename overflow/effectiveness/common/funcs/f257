/* function auto-generated using seed: 8659287 */
void f257(int x, int y){
	int z = 1;
	z = y * x;

	if(z > z){
		x = y * y;
		z = x + y;

		if(x <= y){
			z = y * z;
			y = z * y;

			if(z >= y){
				z = z - z;
				z = x - x;
				z = x - y;
			}
			else{
				z = y - x;
				y = y * y;
			}

		}

	}


	printf("%d", z);
}
