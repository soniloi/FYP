/* function auto-generated using seed: 9908359 */
void f263(int x, int y){
	int z = 1;
	x = x - z;

	if(x == z){
		z = y * z;

		if(z != x){
			z = z * x;

			if(z == z){
				z = z * z;
			}
			else{
				x = y - z;
				x = z + x;
				y = x - y;
			}

			y = z + x;
		}

		z = x * y;
	}
	else{
		z = y - y;
	}


	printf("%d", z);
}
