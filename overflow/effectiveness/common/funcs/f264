/* function auto-generated using seed: 3111623 */
void f264(int x, int y){
	int z = 1;
	y = z + x;

	if(z >= z){
		x = z - y;
		x = z - z;
		y = y * x;

		if(y < y){
			y = y - x;
		}
		else{
			z = z + x;
			x = x - z;
			z = y - x;

			if(z > z){
				z = z - x;
				x = x + y;
				x = z * z;
			}

		}

	}
	else{
		y = y * z;
		x = x * y;
	}


	printf("%d", z);
}
