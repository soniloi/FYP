/* function auto-generated using seed: 9892232 */
void f270(int x, int y){
	int z = 1;
	x = z * x;

	if(z != y){
		y = y + y;

		if(z != z){
			x = x + z;
			z = z + z;
		}

	}
	else{
		y = x * z;
		z = y * y;
	}

	z = z + z;

	if(y < y){
		z = z - z;

		if(x != y){
			z = x - y;
			x = y + z;

			if(y != z){
				z = y - z;
			}

			y = y - x;
		}
		else{
			y = y - z;
			y = y - x;
		}

	}


	printf("%d", z);
}
