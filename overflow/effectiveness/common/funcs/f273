/* function auto-generated using seed: 5067994 */
void f273(int x, int y){
	int z = 1;
	z = x * y;

	if(y != x){
		z = x - y;
	}

	x = x + x;

	if(z >= z){
		z = x + y;
		y = y + x;

		if(y != y){
			y = x * x;
		}
		else{
			z = y * x;

			if(z < z){
				x = x - z;
				y = y - x;
				y = z * y;
			}
			else{
				z = x * x;
				z = x - x;
				z = y * x;
			}

			x = y - z;

			if(x <= y){
				y = y * z;
				x = x * x;
			}

		}

		y = x * z;
	}

	y = y + x;

	if(z <= y){
		y = y - x;

		if(x >= x){
			z = y * x;

			if(z <= z){
				z = z * y;
				z = y - x;
				y = x + y;
			}

			z = y + y;

			if(x != y){
				y = z * z;
			}
			else{
				z = y - y;
				x = z * y;
				z = z + y;
			}

		}
		else{
			y = z * y;
			x = x * z;
		}

		y = x * z;

		if(y == z){
			x = z + x;
			y = z + x;
			z = z * y;
		}
		else{
			x = y + x;
			z = y * y;
		}

		y = y + z;

		if(x != x){
			z = z * z;
			x = x * z;

			if(x != y){
				y = x + z;
			}
			else{
				y = x - y;
				x = x * z;
			}

			z = y * x;

			if(x != x){
				x = x + z;
				y = y + y;
				z = x - z;
			}

		}
		else{
			y = x - z;
			z = x * z;
			y = z - z;

			if(z == x){
				z = z + y;
			}
			else{
				y = x * z;
				x = x + x;
				z = y - x;
			}

		}

	}
	else{
		x = z + y;
		y = x - x;

		if(z != x){
			x = y * x;
			y = x + x;

			if(y > z){
				x = x + x;
				x = y - z;
				z = z + x;
			}

			x = z - y;
		}

		z = x - x;
	}


	printf("%d", z);
}
