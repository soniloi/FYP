/* function auto-generated using seed: 2939514 */
void f274(int x, int y){
	int z = 1;
	x = z + z;

	if(x >= z){
		z = y * y;
		y = x * y;

		if(x <= x){
			y = x - z;
			x = z * y;
			z = y + z;

			if(z == z){
				z = y - z;
				y = x * z;
			}

		}
		else{
			z = z - x;
		}

		y = y * x;

		if(x >= x){
			z = x + z;
		}
		else{
			x = x * z;
			z = z + x;
			x = y * z;
		}

	}

	y = x - x;

	if(y == x){
		y = z + y;

		if(y <= z){
			y = y + y;
			y = y * z;

			if(z <= z){
				y = x * x;
				y = x - y;
				y = y * z;
			}

			y = y + x;
		}
		else{
			y = y + z;
			y = x + x;
		}

		y = y + z;

		if(z < y){
			x = x - x;

			if(z > z){
				y = y + z;
				z = z - z;
				x = y + z;
			}

			z = y + z;

			if(z < y){
				z = x + y;
			}

		}
		else{
			z = x * y;
			y = y * z;

			if(y == y){
				y = z * y;
			}

			y = z - z;
		}

	}

	y = x * y;

	if(z == y){
		z = x * y;

		if(y < z){
			y = y - z;

			if(y != z){
				y = x - z;
				z = y + z;
			}
			else{
				y = y * z;
				x = y * x;
				z = z + z;
			}

		}
		else{
			x = x - z;
			z = x * z;
		}

	}


	printf("%d", z);
}
