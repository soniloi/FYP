/* function auto-generated using seed: 8406569 */
void f278(int x, int y){
	int z = 1;
	x = y - z;

	if(y == y){
		z = z * y;
		z = z - y;

		if(z != y){
			x = x * y;
			y = y * z;

			if(x == y){
				y = x * z;
				y = x + z;
			}

			z = y - z;

			if(y < y){
				z = y - z;
				x = x * x;
			}

		}
		else{
			y = x - x;
			x = x + z;

			if(z != y){
				y = y * y;
				z = z * z;
				z = y * y;
			}
			else{
				z = z - y;
				y = x + y;
				z = x + y;
			}

		}

	}

	z = z + y;

	if(y < y){
		y = z - z;

		if(z <= y){
			y = x * y;
		}
		else{
			x = y - x;
			z = y - x;
			y = y + z;

			if(z > y){
				z = z - y;
				z = y * x;
			}

		}

		y = y + x;

		if(z < z){
			z = x + z;
		}

	}


	printf("%d", z);
}
