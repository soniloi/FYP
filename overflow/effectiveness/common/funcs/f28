/* function auto-generated using seed: 2379913 */
void f28(int x, int y){
	int z = 1;
	z = z * y;

	if(y <= z){
		z = z + z;
		x = y + y;

		if(x > y){
			y = y - y;
			z = y - z;
			y = x + z;
		}

		z = y - z;
	}
	else{
		z = z - z;

		if(z <= x){
			x = y - x;

			if(x > x){
				y = x + y;
				z = y + x;
				z = y - z;
			}

		}

		y = y - y;
	}

	z = y + z;

	if(y > x){
		y = x - x;

		if(z > y){
			x = x - x;

			if(z < y){
				y = y + y;
				x = y + z;
			}
			else{
				x = z - y;
				z = x * y;
			}

		}

	}
	else{
		z = x * y;
		z = x + z;
		x = x - x;
	}

	z = y * y;

	if(y < y){
		y = y + x;
		z = z - x;
	}
	else{
		y = x * z;

		if(x > y){
			x = y + y;

			if(y > z){
				x = y + z;
				z = y * x;
			}
			else{
				x = y + y;
				x = y + y;
				x = x * x;
			}

			x = y * z;
			z = y + z;
		}
		else{
			x = z - x;

			if(z == x){
				x = z * y;
				y = x * y;
			}
			else{
				x = x * z;
				y = x * z;
			}

			x = y * x;
		}

		x = x - y;
		z = z - x;
	}


	printf("%d", z);
}
