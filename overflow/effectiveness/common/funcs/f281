/* function auto-generated using seed: 4057227 */
void f281(int x, int y){
	int z = 1;
	z = y + y;

	if(x != x){
		x = z - z;

		if(x <= y){
			y = z * z;
			y = z * z;
		}

		y = x * y;
		x = z + x;

		if(z != z){
			x = x - x;
			z = z * y;

			if(x > z){
				x = y - y;
				z = y + y;
			}
			else{
				z = z * y;
				y = z * y;
			}

		}
		else{
			y = x + z;
			x = y + z;
		}

	}
	else{
		x = x - x;

		if(z != x){
			x = x * z;
			y = y - z;

			if(z >= y){
				x = z + z;
			}
			else{
				z = y - y;
			}

		}

		z = z + z;

		if(x >= z){
			z = y * y;
			z = z * z;
		}
		else{
			z = z - x;
			y = z - z;
		}

	}

	y = z * y;

	if(y < y){
		z = y * y;

		if(y > y){
			z = z - z;
		}

		z = y + z;
		z = y - y;

		if(x < y){
			z = x + x;

			if(z == y){
				y = x * x;
			}
			else{
				x = x + x;
				z = y + y;
			}

			z = z - x;
			z = z * y;
		}
		else{
			x = z - x;
			y = x * z;

			if(z == x){
				z = z + z;
			}

		}

	}

	z = x + z;

	if(y > x){
		x = z - y;

		if(y < x){
			z = z - x;

			if(z < y){
				y = z + z;
			}

		}

		y = x + y;
	}
	else{
		y = x - x;

		if(x == y){
			y = y * x;
			y = y - x;
		}

		y = x - y;
	}


	printf("%d", z);
}
