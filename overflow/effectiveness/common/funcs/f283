/* function auto-generated using seed: 6984579 */
void f283(int x, int y){
	int z = 1;
	x = x - y;

	if(z == y){
		x = z * z;
	}
	else{
		y = z * y;
	}

	z = z * z;

	if(y != x){
		z = z * x;

		if(z <= z){
			x = z + x;
		}

		z = y + x;
		z = z * y;

		if(z <= y){
			x = y * x;
		}
		else{
			x = y - x;

			if(z >= z){
				z = y - x;
			}

			z = x * y;
			x = z + z;
		}

	}


	printf("%d", z);
}
