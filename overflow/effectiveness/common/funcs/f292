/* function auto-generated using seed: 5027904 */
void f292(int x, int y){
	int z = 1;
	x = x + z;

	if(x == x){
		z = y * z;

		if(z <= y){
			x = x + z;

			if(y != x){
				z = y * y;
			}
			else{
				x = y * z;
			}

			z = x - y;
		}

		z = z - y;
	}
	else{
		x = z - z;

		if(y <= z){
			x = x - x;
		}

		z = x - z;
	}

	z = y * y;

	if(x < x){
		z = x + z;
	}

	x = z + x;

	if(x == z){
		x = y - x;
	}


	printf("%d", z);
}
