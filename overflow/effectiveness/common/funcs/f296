/* function auto-generated using seed: 5954130 */
void f296(int x, int y){
	int z = 1;
	y = z + y;

	if(y > z){
		z = x - y;
		y = y + y;

		if(z < x){
			y = y - z;
		}

		y = y * y;

		if(x > z){
			z = x - z;
			x = z + x;
		}
		else{
			y = y - y;
			x = y - x;
			x = x + x;
		}

	}

	y = x + y;

	if(x == x){
		z = y + z;

		if(x != z){
			y = x * x;
			z = y * y;
		}
		else{
			z = z - z;
			y = x - z;
		}

	}

	x = y + x;

	if(y == z){
		z = z * x;

		if(x == y){
			z = x * z;

			if(y < y){
				y = z - x;
				y = y * x;
			}

			z = x + y;
		}

		x = x * y;

		if(z >= z){
			y = y - z;

			if(x > x){
				x = y + z;
				z = y - x;
			}
			else{
				z = y - z;
				y = y * z;
			}

			z = z - y;
		}

	}


	printf("%d", z);
}
