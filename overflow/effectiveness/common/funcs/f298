/* function auto-generated using seed: 4629000 */
void f298(int x, int y){
	int z = 1;
	y = z - z;

	if(y >= y){
		y = y * x;

		if(z > y){
			z = y - x;
		}

	}

	y = z + y;

	if(y < x){
		z = y - x;

		if(x == z){
			z = z * x;
			x = x - z;

			if(x > z){
				y = z * y;
				y = z + x;
				z = z * y;
			}
			else{
				x = x * y;
				x = x * y;
				x = y * x;
			}

			z = y * z;
		}

		x = z - y;

		if(z < y){
			y = x + y;
		}

	}

	y = z - z;

	if(y < y){
		y = z + z;

		if(z >= x){
			x = z * x;
			x = z + z;
			y = z + y;
		}

		x = y * z;

		if(x >= x){
			y = x * z;
			z = x + z;
			z = y - z;

			if(z != y){
				y = y + y;
			}

		}

	}


	printf("%d", z);
}
