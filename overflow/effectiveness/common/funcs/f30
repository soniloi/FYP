/* function auto-generated using seed: 943363 */
void f30(int x, int y){
	int z = 1;
	z = y + y;

	if(x <= x){
		y = y * y;
		y = y * x;

		if(y >= z){
			x = z + z;
		}
		else{
			x = z * y;

			if(z == y){
				z = y * x;
				z = x * y;
				x = z * z;
			}

			y = y + z;
			z = z * x;
		}

		x = z - z;
	}

	z = y + y;

	if(x < z){
		y = y * z;
		y = z - z;
	}
	else{
		z = y * z;
	}


	printf("%d", z);
}
