/* function auto-generated using seed: 6613755 */
void f302(int x, int y){
	int z = 1;
	x = x + x;

	if(z > z){
		y = z - z;

		if(x > z){
			z = x * z;
		}
		else{
			y = x * z;
			x = x + y;

			if(y == y){
				x = y * x;
			}
			else{
				z = x - y;
				z = z - z;
				y = y * y;
			}

			y = y - z;
		}

		x = z + z;

		if(y >= z){
			x = x - x;

			if(z != x){
				y = y - y;
				x = x - y;
				x = z * x;
			}

		}

		y = x - y;

		if(x >= z){
			y = x - y;

			if(y >= x){
				x = x + y;
				y = x + y;
				z = z * z;
			}
			else{
				y = y * x;
			}

			z = y * z;
		}
		else{
			x = x + y;
			x = y + x;
		}

	}
	else{
		z = z + x;

		if(x == z){
			y = y + z;

			if(y <= z){
				x = z + y;
				x = x + z;
			}
			else{
				x = z + z;
			}

		}

		y = y - x;

		if(x <= x){
			z = x - y;

			if(z == x){
				z = x - z;
				x = y + z;
				x = y + x;
			}

			y = x - y;

			if(z >= z){
				y = x - y;
			}
			else{
				y = y * z;
				z = y + y;
			}

		}

		z = x + z;

		if(y > z){
			z = y - z;
			x = x * x;
			x = y + y;
		}

	}


	printf("%d", z);
}
