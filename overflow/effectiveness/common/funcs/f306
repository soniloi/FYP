/* function auto-generated using seed: 861894 */
void f306(int x, int y){
	int z = 1;
	y = z * x;

	if(x >= x){
		y = y + x;

		if(y <= z){
			y = y + y;
			x = x * z;
		}

		x = x + x;

		if(y <= y){
			y = z + x;
			x = y + x;

			if(y > x){
				z = y + y;
			}

			y = y + y;
		}
		else{
			x = y - y;
			z = x * y;
		}

		x = z - z;

		if(x > x){
			z = x + y;
			x = y + y;

			if(y >= y){
				x = z + x;
				x = z * y;
				y = y + y;
			}
			else{
				y = y + x;
				y = x - z;
				z = z * z;
			}

		}
		else{
			x = x - y;

			if(x <= y){
				x = y + x;
				z = y - y;
				z = y * y;
			}
			else{
				y = y - x;
			}

		}

	}
	else{
		z = z * x;

		if(x == y){
			z = y - y;

			if(x != y){
				z = z - z;
				z = x * z;
			}

			y = z - x;
			z = y * y;
		}

		x = z - z;
		y = y * x;
	}

	y = z * y;

	if(z != x){
		x = z * x;

		if(z >= x){
			z = z + y;
			y = y + z;
		}
		else{
			x = y * y;

			if(z >= z){
				x = x + z;
			}
			else{
				x = x * x;
				x = x + y;
				y = y * z;
			}

			z = z + z;
			y = z + z;

			if(x > z){
				z = z + z;
				y = y + y;
				y = y * y;
			}

		}

		x = x + z;

		if(z != z){
			y = y + y;
			y = x - z;
			y = z - z;
		}

		x = y + x;

		if(y >= x){
			z = z * z;

			if(y > x){
				x = z + z;
			}
			else{
				y = y + x;
				y = x - x;
				y = y * x;
			}

		}
		else{
			x = y * y;

			if(z != y){
				z = y + y;
			}
			else{
				x = y - z;
			}

			x = y - y;
			y = y * x;
		}

	}

	z = y * z;

	if(x == z){
		x = y + y;

		if(y != x){
			y = z * z;
			y = z + x;
			z = x + x;
		}

		x = y - y;
		x = y + y;
	}


	printf("%d", z);
}
