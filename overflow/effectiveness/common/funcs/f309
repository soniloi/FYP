/* function auto-generated using seed: 8636270 */
void f309(int x, int y){
	int z = 1;
	y = y + x;

	if(z >= z){
		x = z - x;

		if(x < z){
			y = y + y;
			z = y - x;

			if(y != y){
				z = y - x;
				x = y - z;
			}
			else{
				z = z - x;
				y = x - x;
			}

		}

		z = y - x;
	}
	else{
		x = y + x;

		if(y > y){
			x = x + x;

			if(y != y){
				x = z + y;
				y = x + z;
				y = x * y;
			}
			else{
				y = y + y;
			}

			y = y - x;
			y = z - y;

			if(z != x){
				y = z + z;
			}

		}

		x = z + x;

		if(x < x){
			z = z * x;

			if(z != y){
				x = z + z;
			}

		}

		y = y - x;

		if(x < z){
			y = z - y;
			x = x * y;
		}

	}


	printf("%d", z);
}
