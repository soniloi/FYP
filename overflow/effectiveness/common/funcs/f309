/* function auto-generated using seed: 8636270 */
void f309(int x, int y){
	int z = 1;
	y = y + x;

	if(z >= z){
		x = z - x;

		if(x < z){
			y = y + y;
			z = y - x;

			if(y != y){
				z = y - x;
				y = y + x;
			}

		}

		y = x - x;
	}
	else{
		z = y - x;
	}


	printf("%d", z);
}
