/* function auto-generated using seed: 8710661 */
void f31(int x, int y){
	int z = 1;
	y = x + x;

	if(z > x){
		y = y * z;
	}
	else{
		x = z - z;
		y = y * x;

		if(x > x){
			x = x * z;

			if(x >= x){
				x = z * x;
				z = y * y;
			}
			else{
				x = y - y;
				z = z + z;
			}

			x = z - x;
			z = z - x;
		}
		else{
			x = x * z;
			x = z + x;
			x = z - x;
		}

	}

	x = y * z;

	if(z == y){
		x = y + z;
	}

	x = x - y;

	if(y == z){
		y = z * z;

		if(z < y){
			y = x * y;
		}
		else{
			z = x + x;
			x = x * y;
			x = y + x;
		}

		z = x * z;

		if(z == z){
			x = x - z;

			if(z > z){
				y = z + x;
				y = y + x;
				y = z + x;
			}
			else{
				y = y - y;
				y = y + z;
				y = z + z;
			}

		}
		else{
			z = z * x;
			z = z - z;

			if(z != x){
				x = z * x;
			}

		}

		z = z * y;
	}
	else{
		z = x + x;

		if(z < x){
			z = x + x;
			x = y + y;
			x = z * y;
		}
		else{
			x = y - z;

			if(x != y){
				z = y * y;
			}

			x = z + z;
		}

		z = x - z;
	}


	printf("%d", z);
}
