/* function auto-generated using seed: 1553289 */
void f311(int x, int y){
	int z = 1;
	z = y - y;

	if(z != z){
		y = z + y;

		if(y < z){
			z = y * x;
			x = x - x;
		}

		y = x + x;
	}
	else{
		x = x + z;
		z = z - x;

		if(z <= z){
			y = z * y;
			y = z * y;
		}

		z = z - x;

		if(x <= z){
			z = z * x;

			if(x < z){
				z = x - x;
			}

		}

	}

	y = y * y;

	if(y == y){
		x = z + z;

		if(z != y){
			y = x + x;
		}

		z = z - x;

		if(y > z){
			z = z - z;
			z = z - z;

			if(x != y){
				z = x * y;
				y = z + y;
				x = z - x;
			}
			else{
				y = z - y;
			}

		}

		x = z + z;

		if(x >= y){
			x = x - z;
		}
		else{
			z = y * x;
			y = y * z;
		}

	}
	else{
		y = z + y;

		if(y != x){
			y = z - x;
			z = z - y;
		}
		else{
			y = x + y;

			if(z >= z){
				y = z + x;
			}
			else{
				y = y * z;
				y = z * x;
				z = y - y;
			}

		}

	}


	printf("%d", z);
}
