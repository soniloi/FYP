/* function auto-generated using seed: 721141 */
void f317(int x, int y){
	int z = 1;
	x = y + y;

	if(x <= x){
		y = y - z;

		if(z >= y){
			z = z - z;
			z = x * z;
		}
		else{
			x = x + y;

			if(x <= z){
				y = z + z;
				y = x - y;
			}

		}

		y = z * y;
	}
	else{
		x = z - y;
		x = x + z;

		if(x == x){
			y = y * x;
			y = x + z;
			x = y * z;

			if(y >= z){
				x = x - y;
			}

		}
		else{
			x = y + z;
			y = y - z;

			if(x != x){
				y = y * z;
				z = y * y;
			}
			else{
				y = z - x;
				y = x - y;
			}

			y = x * y;
		}

		z = z * z;
	}


	printf("%d", z);
}
