/* function auto-generated using seed: 5743005 */
void f32(int x, int y){
	int z = 1;
	y = z + z;

	if(y > x){
		y = z - x;
		x = y - y;
	}

	y = y * z;

	if(y == z){
		y = y * y;

		if(z > y){
			x = y + z;

			if(z != y){
				x = y - z;
				x = x - z;
				y = x - z;
			}
			else{
				z = x * y;
				z = x - x;
				y = y - x;
			}

			x = z - y;

			if(z >= x){
				y = x - y;
				y = y - z;
			}

		}
		else{
			x = x * z;
		}

		x = z * x;

		if(x > z){
			z = y + y;
			y = y * x;
		}
		else{
			x = y + z;
			x = x * y;
			z = x + x;
		}

	}


	printf("%d", z);
}
