/* function auto-generated using seed: 4587085 */
void f324(int x, int y){
	int z = 1;
	y = x * x;

	if(z < x){
		z = z * z;
		z = x - z;

		if(x > x){
			z = x - y;
			z = z - y;

			if(y == z){
				y = y - z;
			}
			else{
				z = y * z;
				y = y + y;
			}

		}

		y = x - y;
	}
	else{
		x = z + z;
	}

	z = y - y;

	if(z >= x){
		y = x * z;
	}
	else{
		y = x + x;

		if(x == x){
			x = y + y;

			if(x == z){
				z = y - z;
				z = z + x;
			}

		}
		else{
			x = z + y;
			y = z + z;
			x = y + z;
		}

		x = x + y;

		if(x >= z){
			y = x - y;
		}
		else{
			x = z + z;
			z = x - z;
			y = z * y;
		}

	}

	z = x * x;

	if(y < y){
		x = z * x;
		z = x * x;
		z = y + z;
	}


	printf("%d", z);
}
