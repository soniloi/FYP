/* function auto-generated using seed: 850242 */
void f327(int x, int y){
	int z = 1;
	z = x * z;

	if(y < x){
		y = z + y;
		x = z * z;

		if(z < x){
			z = x * z;
			y = z * y;
		}

		x = x - z;
	}
	else{
		z = x * z;

		if(z < x){
			z = x - y;
		}

		z = z + z;
	}


	printf("%d", z);
}
