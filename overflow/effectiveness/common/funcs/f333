/* function auto-generated using seed: 963591 */
void f333(int x, int y){
	int z = 1;
	x = x - z;

	if(x != y){
		z = x + z;

		if(x == y){
			y = z - y;
			y = z + y;
			z = z - y;
			x = z + x;
		}
		else{
			z = y * x;
		}

		x = x + x;
		y = y * y;
		y = x - z;
		z = y + z;

		if(y >= y){
			y = x * x;
			y = z + x;
			x = z - z;

			if(z == y){
				z = z - x;

				if(x >= y){
					y = z + x;
					z = z * y;
					y = y * y;
					z = y + y;
				}

			}

			x = y - y;
			x = y + x;
			z = x + z;
		}
		else{
			x = x - y;
			x = y + x;
			y = z * y;
		}

		z = x - x;

		if(y < z){
			z = y - y;

			if(x >= z){
				x = z * z;
				x = x + z;
				y = y + z;

				if(x != z){
					x = x + y;
					z = z * z;
					z = z + z;
				}
				else{
					z = y * z;
					z = z - x;
				}

				y = y - x;

				if(y <= z){
					z = y + z;
					y = y + x;

					if(y <= y){
						z = y * z;
						x = y - x;
					}

					x = x + z;
					z = y + x;

					if(x > z){
						z = x * z;
						y = z - x;
						x = x + y;
					}
					else{
						y = y - x;
						z = z + x;
					}

					z = y + x;
				}

				y = y + z;
				x = y + y;
			}

			y = x + x;
			z = x + x;

			if(y != z){
				x = y * x;
				x = z - x;
				y = y - y;

				if(z != z){
					x = x + x;

					if(x <= y){
						y = y - x;
						z = z + z;
					}
					else{
						z = y * z;
					}

					x = z - z;
					x = z - z;
					y = y + x;
				}
				else{
					y = y + x;
				}

				y = z * y;
				y = z - y;
			}

			y = x * x;
			x = x * x;

			if(z == x){
				x = z - x;
				x = z + y;
				y = z * z;

				if(y <= x){
					y = y + y;
					z = y + x;
				}
				else{
					y = x * z;
					x = x - y;

					if(z != z){
						y = x + x;
						z = y + y;
						z = z + y;
						x = z - x;
						x = x * y;
					}

				}

				y = y * x;
				y = z - y;
			}
			else{
				x = x * z;
				x = y + y;

				if(y == z){
					z = y - x;
					z = y * z;

					if(y == y){
						y = z * z;
						x = z + y;
						z = y * z;
						z = x + z;
						y = x * z;
					}
					else{
						y = z - x;
						z = y * y;
						z = x - y;
						z = y - y;
						z = y + z;
					}

					x = z - x;
				}

			}

		}
		else{
			y = y * z;
			y = z * y;
		}

	}

	x = y * x;

	if(z <= z){
		y = y + z;

		if(x > x){
			x = z - y;
			y = y - y;
			z = z - y;
			z = z + y;
			z = y * x;
		}
		else{
			z = x - z;

			if(z == y){
				y = y - y;
				z = x * y;

				if(z > x){
					x = z * y;
				}
				else{
					y = x * y;
					y = x - y;

					if(y == z){
						z = x - y;
						z = z * x;
						y = z * x;
					}

					y = z * z;
				}

				y = x + y;
				y = y - z;
				x = x + z;

				if(z >= x){
					z = y * y;
					y = x + z;
					x = x + y;
					z = y - x;
					z = x + y;
				}
				else{
					x = y * z;
					z = y + y;

					if(y != y){
						x = y - y;
					}

					z = y - z;
					z = y + z;
					y = z * z;
				}

				z = z + x;
			}

			z = z - x;
			x = x + x;
			y = y + z;
			z = x + x;
		}

		x = x - y;

		if(z == x){
			y = z * y;
			x = z * x;

			if(x != y){
				x = z * y;
				y = z - y;
				x = z - z;

				if(y == y){
					x = z * z;

					if(x == z){
						y = z * z;
					}

					z = y * y;
					z = x - z;
				}
				else{
					x = y - z;
				}

				y = y + x;
				x = y * y;
			}

			y = x - y;

			if(y <= z){
				z = x - x;
				x = x + y;
				y = z - y;
				z = x * y;
			}

			x = x - y;

			if(z != y){
				x = y * y;
			}

			x = z + y;
		}

	}
	else{
		z = y - y;
	}

	z = y - x;

	if(z > x){
		z = x - x;
		x = z + y;

		if(z <= y){
			x = z * y;
			x = y * z;
		}

		x = x * y;
		z = z + z;
		x = y + y;

		if(y <= x){
			z = y - x;
			y = y + z;
		}
		else{
			x = y - x;

			if(y == y){
				x = y - x;

				if(y <= y){
					x = y + x;

					if(y == x){
						y = x * x;
						y = x + x;
						z = z * y;
						y = y * y;
						x = x * x;
						y = z - x;
					}

					x = x * x;
					z = y * y;

					if(z >= y){
						y = y + z;
						y = x - z;
					}

					z = z + x;
					z = x - y;

					if(x > z){
						z = x + x;
					}

				}

				y = z + y;

				if(z <= y){
					y = x * x;
					y = y - z;

					if(y != z){
						x = y + y;
						z = x + z;
						x = y - z;
						z = y * y;
					}

					x = z - z;
					x = z + x;
					y = z * z;
					x = z + z;
				}
				else{
					y = z * z;
				}

			}

			y = x * x;

			if(z < z){
				y = z + x;

				if(z == z){
					y = z + z;
					x = z + y;
					y = z * y;
					y = z - x;
					z = y - x;
				}
				else{
					y = y * x;
					z = z + x;
					y = y * x;
					z = x * z;
					z = y + z;

					if(x >= z){
						x = x - x;
						z = x * y;
						z = x * y;
						z = y + y;
						z = y - y;
					}

					x = x + y;
				}

				y = z * x;
				z = y * y;
				z = z + x;
			}

			y = y * x;

			if(x > x){
				x = z + z;
				y = z - z;
				y = x * x;

				if(z == x){
					y = y * x;
					y = y + z;
					x = y + y;
					x = z + z;
					y = x * y;
					z = x * y;
				}

				z = z - y;
				z = z * x;

				if(x != y){
					z = y - z;
					x = x * y;
					x = y + y;
					x = z * y;
					y = x - x;
				}
				else{
					x = z + y;
					y = x - y;
				}

			}
			else{
				x = z + z;
			}

		}

	}
	else{
		z = y + y;

		if(x != z){
			y = y * z;
			z = z + z;
		}
		else{
			z = x + y;
			y = z * x;
			z = x * z;
			x = z - x;
			x = x - y;
		}

	}


	printf("%d", z);
}
