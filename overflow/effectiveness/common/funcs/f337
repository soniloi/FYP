/* function auto-generated using seed: 4873152 */
void f337(int x, int y){
	int z = 1;
	z = x * y;

	if(x > x){
		z = y - z;

		if(y > x){
			x = y + z;
			y = z * z;

			if(z != x){
				x = y + x;
				y = x + z;
			}
			else{
				z = z - x;
				y = z - z;
				x = y * x;
			}

		}

	}

	x = z + y;

	if(x <= x){
		z = z + x;

		if(y >= x){
			y = z - z;

			if(z != y){
				z = z + z;
			}
			else{
				x = z - x;
			}

		}
		else{
			x = z + x;

			if(z <= y){
				x = z + x;
				x = x + y;
				y = y * z;
			}
			else{
				y = y - y;
				y = y + z;
			}

		}

	}

	z = z + z;

	if(x > x){
		z = z - x;
		x = z * y;

		if(y < z){
			z = x + x;

			if(y < y){
				z = y * z;
				y = x * x;
			}

		}

	}


	printf("%d", z);
}
