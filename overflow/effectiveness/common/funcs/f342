/* function auto-generated using seed: 8435670 */
void f342(int x, int y){
	int z = 1;
	x = x + z;

	if(y > x){
		x = y - z;

		if(z <= x){
			z = z - z;
			y = x - z;

			if(z != z){
				y = y * z;
				y = x * x;
			}

			z = x - x;
		}
		else{
			z = x - y;
		}

		z = z + y;
	}
	else{
		z = z + x;
		y = y * z;

		if(z >= x){
			y = z * x;
		}

	}


	printf("%d", z);
}
