/* function auto-generated using seed: 5049080 */
void f347(int x, int y){
	int z = 1;
	z = y * z;

	if(y > z){
		y = y * y;

		if(x < y){
			y = y - z;

			if(x < y){
				x = y + x;
				z = y - z;
				z = x * x;
			}
			else{
				z = x - x;
			}

		}

		x = x - z;

		if(z == y){
			x = y + y;
			x = z * x;
			x = z + z;

			if(y < x){
				y = z - y;
				y = y - x;
			}

		}

		z = y * z;

		if(z <= y){
			x = x - x;
			y = y * y;
			x = z - x;
		}
		else{
			x = z - y;
			z = z - x;

			if(y >= z){
				x = y + y;
				x = y - y;
				y = y - z;
			}

			y = y - z;

			if(x == y){
				z = y + y;
				z = z * x;
				y = z + y;
			}

		}

	}
	else{
		x = y + y;

		if(z <= z){
			z = z - y;
			x = y - z;

			if(y <= z){
				z = x + y;
			}
			else{
				y = y + z;
				z = y * x;
			}

			z = z - x;

			if(y != x){
				y = x * x;
				z = y * z;
				x = z + z;
			}
			else{
				x = x + x;
				y = x * y;
				z = z * z;
			}

		}

	}

	x = z + y;

	if(z < z){
		y = x * z;

		if(y >= x){
			y = x - y;
			z = x * x;
		}

		x = x + y;
	}

	z = z * y;

	if(y < x){
		y = x + x;
	}
	else{
		x = x + y;

		if(z <= z){
			x = x + z;
			x = x - z;
		}

		z = x * z;

		if(x == z){
			z = z - x;
			z = x * z;
		}
		else{
			y = z - x;

			if(y < y){
				z = x * x;
			}
			else{
				x = x * y;
			}

			z = y - x;
			z = x * z;

			if(y <= z){
				z = x - y;
				z = x - x;
				z = y * z;
			}

		}

	}


	printf("%d", z);
}
