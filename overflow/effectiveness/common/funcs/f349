/* function auto-generated using seed: 4637933 */
void f349(int x, int y){
	int z = 1;
	y = z - y;

	if(y > z){
		z = x - z;

		if(y < z){
			z = z - z;
			x = y - x;
		}

	}
	else{
		z = z * x;
		y = x - z;

		if(y < z){
			y = z - x;
			x = y + x;
		}

		x = x + x;

		if(z >= z){
			z = x * x;
			y = z + x;

			if(z <= y){
				x = x + x;
			}

		}
		else{
			y = y - x;
		}

	}


	printf("%d", z);
}
