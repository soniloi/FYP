/* function auto-generated using seed: 4829750 */
void f35(int x, int y){
	int z = 1;
	z = y - x;

	if(x == y){
		z = z + y;
		x = z - y;

		if(z <= x){
			y = y + y;

			if(y > x){
				y = y * x;
			}

		}

	}
	else{
		z = y - x;

		if(x > z){
			y = y + y;
		}
		else{
			z = x * y;
			z = z + x;
		}

		y = x - x;

		if(y < x){
			x = x - z;
		}

		x = z * z;
	}

	x = z + y;

	if(x < x){
		x = x - x;

		if(x > y){
			z = z + z;
			z = z * x;

			if(y < x){
				z = x * y;
			}

			z = y * y;
		}
		else{
			z = z - x;

			if(y <= x){
				x = x + x;
			}
			else{
				x = z - z;
				y = z + x;
			}

			x = x * z;
			z = y - x;
		}

	}

	z = y * y;

	if(y > x){
		z = y * x;
		z = y + y;
	}


	printf("%d", z);
}
