/* function auto-generated using seed: 9600655 */
void f353(int x, int y){
	int z = 1;
	y = y - z;

	if(z != z){
		z = y + z;
		z = y * z;
		z = z + x;
	}

	x = z * y;

	if(z != z){
		z = x * x;
		y = z + z;

		if(x >= z){
			z = y - x;
			z = y * y;
		}
		else{
			z = z - z;
		}

	}


	printf("%d", z);
}
