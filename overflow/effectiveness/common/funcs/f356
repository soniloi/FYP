/* function auto-generated using seed: 2772616 */
void f356(int x, int y){
	int z = 1;
	y = z * y;

	if(x != z){
		x = y - x;
	}

	z = x + z;

	if(z >= y){
		z = y - x;

		if(z == x){
			z = z - y;
			z = x + x;

			if(z != x){
				y = x * x;
			}

			z = z * z;
		}

		x = x - x;

		if(z == x){
			x = x * z;
			y = z + z;
		}

	}
	else{
		x = z - z;
		x = y * x;

		if(y != x){
			x = y + z;

			if(y != x){
				y = z + z;
				z = x + x;
				y = y * y;
			}

			z = x + z;
		}

	}


	printf("%d", z);
}
