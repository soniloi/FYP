/* function auto-generated using seed: 1443513 */
void f362(int x, int y){
	int z = 1;
	y = x + z;

	if(z != y){
		z = y * y;
	}
	else{
		y = x - y;
		y = y - y;
	}

	x = x * z;

	if(y != z){
		y = x + z;

		if(z == z){
			z = z * z;
			y = z + z;
			z = x - y;
		}

		z = x * y;

		if(x >= y){
			z = y * z;
			x = z + z;
		}

	}


	printf("%d", z);
}
