/* function auto-generated using seed: 490621 */
void f367(int x, int y){
	int z = 1;
	x = z - y;

	if(x >= y){
		y = z - z;

		if(x != z){
			x = z - x;
		}

		z = z - y;

		if(y <= y){
			y = y + y;
		}
		else{
			z = y + x;
		}

	}

	y = z + z;

	if(x <= y){
		x = z - y;
		x = y * y;

		if(y == z){
			z = z - x;
			x = x * y;
			x = x - y;

			if(x > z){
				x = z * y;
				x = x + x;
			}
			else{
				y = z * z;
				z = y * z;
			}

		}

	}
	else{
		x = x * x;
		x = x - y;
		z = y - x;
	}

	y = x + x;

	if(z < x){
		x = y + x;

		if(y < x){
			y = z + x;
			y = z + x;

			if(z <= y){
				z = z * x;
				z = x - y;
			}
			else{
				x = z - y;
			}

			z = z - z;
		}
		else{
			y = x * x;

			if(z >= y){
				y = y - z;
				z = x * x;
				y = z * z;
			}

			x = z - z;

			if(x <= x){
				y = z + z;
				z = y - x;
			}

		}

	}
	else{
		x = x + y;
		y = z - x;

		if(z < x){
			z = z * z;

			if(y <= z){
				x = z - z;
				y = z * y;
				y = x * z;
			}
			else{
				y = x - y;
				x = y + z;
				y = x - z;
			}

		}
		else{
			y = z + x;
			y = z + z;

			if(x > z){
				x = z * y;
			}
			else{
				y = x - y;
				y = z + y;
				x = y - x;
			}

		}

		y = y - z;

		if(x > z){
			z = z - x;

			if(x < z){
				z = x - z;
				x = x * y;
				y = y + z;
			}

			x = z - y;
		}

	}


	printf("%d", z);
}
