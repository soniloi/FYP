/* function auto-generated using seed: 7325926 */
void f368(int x, int y){
	int z = 1;
	x = y + x;

	if(z != z){
		x = y * z;
		y = y * x;
	}
	else{
		x = y + z;
		y = y - z;
		x = y * x;
	}

	y = z + x;

	if(x == x){
		y = x * z;

		if(z <= x){
			z = y - y;

			if(y < y){
				z = x * y;
				y = y + z;
			}

			x = y - z;

			if(z == x){
				y = y + z;
			}

		}

		y = y - y;

		if(y >= z){
			x = x - x;
		}
		else{
			z = x - x;
			y = x - x;

			if(x > y){
				x = z * x;
			}
			else{
				y = x * z;
				y = z + z;
				x = y - x;
			}

		}

		y = z - y;
	}

	x = y - y;

	if(z <= z){
		x = y * z;

		if(z != y){
			z = z + z;
		}

		z = y + x;
		z = z - x;

		if(z > y){
			z = y + x;
		}
		else{
			x = y * y;

			if(z >= x){
				z = y + y;
			}

		}

	}


	printf("%d", z);
}
