/* function auto-generated using seed: 3160987 */
void f37(int x, int y){
	int z = 1;
	z = z * y;

	if(z > x){
		z = x * y;
		y = x - z;
		z = x + z;
	}

	z = x - x;

	if(z >= x){
		x = z * x;

		if(y > x){
			y = x * y;
		}
		else{
			y = z * z;
			x = x + x;

			if(y != x){
				z = y + x;
				y = z - y;
				x = z * z;
			}
			else{
				z = z - x;
				y = y - x;
			}

		}

	}
	else{
		x = y - y;

		if(z == z){
			y = y - z;
		}

		y = z + z;

		if(y >= z){
			x = z - z;
		}

		y = y - y;

		if(x < z){
			z = z + y;
			z = x * y;
			z = x - z;
		}
		else{
			z = z - x;
			x = y - x;
		}

	}

	y = z * x;

	if(x < z){
		z = x * x;
		z = y * x;
		x = y - x;
	}


	printf("%d", z);
}
