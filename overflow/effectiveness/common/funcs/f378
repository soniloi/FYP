/* function auto-generated using seed: 8844257 */
void f378(int x, int y){
	int z = 1;
	y = y * y;

	if(x != x){
		z = x * y;

		if(x >= x){
			y = z - x;
		}
		else{
			y = y * x;
			y = x - z;
		}

	}
	else{
		z = x - x;
		y = y * x;

		if(y >= z){
			z = x + x;
			z = y - z;
			y = x * z;
		}

		x = z - y;

		if(y == y){
			y = y * z;
			x = z + z;
			y = z - y;
		}

	}

	z = z + y;

	if(y >= x){
		z = x - x;
	}


	printf("%d", z);
}
