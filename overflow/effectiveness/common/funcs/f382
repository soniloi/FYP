/* function auto-generated using seed: 9376320 */
void f382(int x, int y){
	int z = 1;
	z = z * y;

	if(y >= x){
		x = z * y;
		x = x + x;
		x = y * y;
	}
	else{
		y = y - z;

		if(x == y){
			x = y - x;
			y = x + y;
			z = y - y;
		}
		else{
			y = x * z;
			y = y - y;
		}

		z = y + x;

		if(y > y){
			x = z - x;

			if(z == z){
				z = x - y;
				z = z + z;
				y = z * z;
			}

			y = y * y;
		}

		y = x + z;
	}

	y = y * x;

	if(y >= y){
		x = z * y;
		x = x + x;
	}


	printf("%d", z);
}
