/* function auto-generated using seed: 5491288 */
void f390(int x, int y){
	int z = 1;
	z = z + z;

	if(y == y){
		x = z + z;

		if(x != z){
			y = x + x;
			z = z * x;

			if(z == z){
				z = y + z;
				y = y + y;
				x = z * y;
			}

			x = x + x;
		}
		else{
			x = x + x;
			z = z * x;
		}

		y = z - x;

		if(y < y){
			z = x * z;
			z = y * y;
		}
		else{
			x = x + z;

			if(x > y){
				y = z * z;
				y = y - z;
			}

		}

		y = z * z;
	}


	printf("%d", z);
}
