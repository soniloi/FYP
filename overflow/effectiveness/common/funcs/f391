/* function auto-generated using seed: 8006127 */
void f391(int x, int y){
	int z = 1;
	z = x - y;

	if(x <= x){
		x = x - x;

		if(z < x){
			x = x + z;

			if(y >= x){
				z = y + z;
				z = x - y;
				z = z + z;
			}
			else{
				y = y + x;
				z = x + y;
			}

			z = z - z;
			y = y + z;
		}
		else{
			z = z * y;

			if(z == x){
				z = x * y;
				z = y * z;
			}
			else{
				z = y + x;
				y = z + x;
			}

		}

		z = z + x;
	}

	x = y * x;

	if(z > z){
		y = z * y;
		z = y + x;
	}


	printf("%d", z);
}
