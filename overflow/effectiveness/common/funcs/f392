/* function auto-generated using seed: 6226817 */
void f392(int x, int y){
	int z = 1;
	x = x * z;

	if(x < x){
		y = y * z;

		if(x < x){
			x = x - x;

			if(y != x){
				x = z + x;
			}

			z = z - z;

			if(x <= x){
				z = y - z;
				z = y + x;
			}
			else{
				x = x * z;
			}

		}

		x = y * z;
	}
	else{
		z = z * y;

		if(y != z){
			y = z - z;

			if(z == y){
				z = y + x;
				y = x + x;
			}
			else{
				z = y * y;
			}

		}

		y = x - z;

		if(x < x){
			x = y * z;
		}
		else{
			x = z - x;
			y = x * y;
		}

	}


	printf("%d", z);
}
