/* function auto-generated using seed: 5505179 */
void f395(int x, int y){
	int z = 1;
	z = z * x;

	if(y > z){
		x = z - z;

		if(z >= y){
			x = z * z;
			y = y + z;
			z = y + y;

			if(z != z){
				x = x + z;
				x = y * x;
			}

		}
		else{
			y = z - y;

			if(y != x){
				z = z + y;
			}
			else{
				y = z + z;
				x = x - x;
			}

			x = x * y;

			if(z < y){
				z = y * y;
			}
			else{
				z = z - y;
				z = y - y;
			}

		}

	}


	printf("%d", z);
}
