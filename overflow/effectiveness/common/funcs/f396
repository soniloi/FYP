/* function auto-generated using seed: 4514587 */
void f396(int x, int y){
	int z = 1;
	y = y * y;

	if(x == y){
		x = z * z;

		if(z < z){
			z = z * z;
			z = z - y;
			z = z + y;

			if(x >= x){
				y = x - z;
				z = z + y;
				z = z + z;
			}
			else{
				y = z * x;
			}

		}
		else{
			z = x * x;

			if(z <= z){
				x = z * z;
				x = z * z;
			}
			else{
				x = y + x;
				y = x * z;
				y = y * y;
			}

			x = z + z;
		}

	}


	printf("%d", z);
}
