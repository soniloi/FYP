/* function auto-generated using seed: 5890482 */
void f400(int x, int y){
	int z = 1;
	x = y - z;

	if(z != x){
		z = z * x;

		if(x >= z){
			z = z * x;

			if(y <= y){
				y = x * x;
			}
			else{
				z = z - z;
				x = x + z;
			}

			z = y - z;
			z = x - z;
		}

	}


	printf("%d", z);
}
