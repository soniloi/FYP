/* function auto-generated using seed: 101079 */
void f48(int x, int y){
	int z = 1;
	z = z - z;

	if(y <= z){
		z = y - y;

		if(z != y){
			y = x + x;

			if(y == x){
				y = y + x;
				z = z - x;
			}
			else{
				x = y * y;
				z = z - x;
				z = x - x;
			}

			x = y * x;
		}
		else{
			z = y * z;

			if(y >= z){
				y = x - z;
			}
			else{
				x = y + z;
				z = x + x;
			}

			x = x + x;

			if(z == z){
				x = x + x;
			}

		}

		y = x - z;
		z = x - x;

		if(z < x){
			y = x + x;
			x = x - x;
			x = z * x;
		}
		else{
			z = z - y;
		}

	}

	z = y - y;

	if(x > x){
		z = z + x;
		z = z * y;

		if(z >= z){
			x = x * y;
			y = x - x;

			if(x != x){
				z = x + z;
				y = x + x;
				z = y - y;
			}

		}
		else{
			y = x * y;
			x = y - x;

			if(y != z){
				x = x - z;
				z = x * x;
				x = y - x;
			}
			else{
				x = z - x;
				z = x * x;
			}

			z = y * x;

			if(z >= y){
				x = x * x;
				x = y * x;
				z = x * z;
			}
			else{
				x = y * z;
			}

		}

	}
	else{
		x = x - y;

		if(y < x){
			z = x - y;

			if(z != x){
				y = z * y;
				x = z + y;
			}
			else{
				x = x + z;
				z = y + x;
				y = x * x;
			}

			z = x * y;
			y = z * z;
		}
		else{
			y = z - y;
			z = z * x;
			x = z - x;

			if(y < z){
				x = y - z;
				y = y + y;
			}

		}

	}


	printf("%d", z);
}
