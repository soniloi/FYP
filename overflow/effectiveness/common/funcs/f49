/* function auto-generated using seed: 6532075 */
void f49(int x, int y){
	int z = 1;
	y = x * x;

	if(y > z){
		x = y - z;

		if(z < x){
			z = x + z;

			if(y != x){
				x = x - z;
			}

			z = y - z;

			if(x >= z){
				z = x + x;
			}

		}

		x = x + x;

		if(z <= z){
			z = x - z;

			if(x < z){
				z = x - x;

				if(y != z){
					y = z - x;
					z = y - y;
					y = z + y;
					z = y - x;

					if(z <= y){
						y = z + z;
						x = y * y;
						y = z - y;
						y = z - y;
						x = z * y;
						y = y + x;
					}
					else{
						z = x * y;
						z = y - z;
						z = x + x;
						z = y * x;
						y = y * z;
						x = x + z;
					}

					y = x * y;
					z = x - y;

					if(z > x){
						y = y + x;
						x = z + x;
						z = y + y;
					}

				}
				else{
					z = y + z;
					y = z - x;
					y = z * z;
					y = x * z;

					if(x >= x){
						z = z * y;
						x = z - z;
					}

					x = y * x;
				}

				y = z - z;
				x = z - x;
				z = x + x;
				x = x - x;
			}

			x = x - z;
			x = z + y;
			y = y + y;
			x = x - x;
			z = z + x;
		}

	}
	else{
		y = x + z;

		if(y < x){
			y = y * z;
			z = y - z;

			if(z >= x){
				y = x + x;
			}
			else{
				x = y - x;

				if(z < x){
					y = x - x;
					z = y + x;
					y = y + y;

					if(z != y){
						x = z + y;
						y = y * x;
						z = z - x;
						y = z + z;
						y = y - y;
						x = x - y;
					}
					else{
						z = z + y;
						x = z - z;
					}

					x = x * z;
					y = z - z;

					if(y >= z){
						z = y - x;
					}
					else{
						x = y + y;
						z = y + y;
						z = z - x;
						x = y - y;
					}

					x = x + x;

					if(y <= y){
						x = y - x;
						x = z + x;
					}
					else{
						z = y * x;
						x = y + y;
						x = y - x;
						x = y * y;
						x = x + y;
					}

				}
				else{
					x = y - x;
				}

				y = y * y;
				x = x * y;
				z = z - y;
				y = x + z;
				x = x - x;
			}

			z = x * x;

			if(x != y){
				y = y + y;
				z = x - y;
				z = y - y;
				y = x * z;
			}
			else{
				z = x - y;
				x = y + x;
				y = x - z;
			}

			y = z + y;
			x = x - x;
			z = z * x;

			if(x <= x){
				z = y + y;
				z = z + z;
				x = x * z;
				y = x - x;
				y = z * z;

				if(y == x){
					z = y + y;
					x = z * x;
					y = x * z;
				}

			}

		}

	}

	y = z + z;

	if(z != y){
		x = z * x;

		if(z != y){
			x = x + z;

			if(y == z){
				y = z - y;
				z = y + x;
				y = x + z;
				y = z * y;
				x = y * z;
			}
			else{
				z = z - z;

				if(y < y){
					y = z - x;
					x = z * y;
					z = z - z;
				}

				y = x - y;

				if(y == z){
					z = z + y;

					if(y == z){
						y = z * z;
						z = y - z;
						x = x * z;
						x = x - y;
						y = y + z;
					}
					else{
						z = x + y;
					}

					x = x - y;
				}

				z = x * y;

				if(y == x){
					x = z + x;
					y = z * z;
					y = z + y;
					y = x * x;

					if(x <= x){
						y = x - x;
						x = x * x;
						x = y * x;
						y = z - y;
						x = y - y;
					}
					else{
						x = z * x;
						y = z + z;
					}

					x = y + x;
					y = x + y;

					if(z == x){
						x = z - y;
						x = z - x;
						x = x - y;
					}

				}
				else{
					y = y * z;
					z = x - x;
					z = y - x;
					y = x * x;
					y = y - x;
					y = x * y;
				}

				z = y + x;

				if(x == x){
					x = x * z;

					if(x != y){
						x = x * y;
						y = y * x;
						z = z + z;
						x = z * x;
						z = y * z;
					}
					else{
						y = x + z;
						z = x - y;
						x = z + y;
						x = x - z;
						y = z * x;
						x = y * y;
					}

					x = z + x;

					if(x != x){
						x = y - y;
						x = y * y;
						x = x + z;
						y = z + z;
						y = z + x;
						z = y + x;
					}
					else{
						x = y - z;
						x = z + x;
						z = x * z;
						z = z - x;
					}

					x = y - z;
					z = x + y;
					y = y * z;
				}
				else{
					z = x + y;

					if(z != z){
						x = z * y;
					}
					else{
						x = y + y;
						z = y * x;
						z = y - y;
					}

					x = z + x;

					if(z == x){
						x = z * x;
						z = z + x;
					}
					else{
						y = z + z;
						z = x - y;
					}

					x = z + y;
				}

				x = z + y;
				x = y - x;
			}

		}

		z = z + y;

		if(z > x){
			y = z - x;
			z = z * x;
			y = z - z;

			if(x != y){
				z = x - z;

				if(z != z){
					z = y - x;
					x = z + z;

					if(z != z){
						y = y - y;
						y = y - z;
						z = x * y;
						y = x - z;
						y = x * z;
					}

					x = y + y;
					y = x * y;
				}

				z = z + y;
				y = x * y;
				z = z * x;
				x = x - z;

				if(z <= y){
					x = x - x;
					x = y + y;
				}

			}

		}

		x = x - z;
		x = x * z;
		y = y + y;
	}
	else{
		x = y - x;

		if(z >= x){
			x = y + y;
		}

		z = x - z;
	}

	z = x + z;

	if(y == y){
		z = y * y;

		if(x == x){
			z = y + z;
			y = x - z;

			if(y == y){
				x = y - z;
				z = x - x;
			}

			x = z * y;
			z = y + y;
		}
		else{
			x = z + x;
			z = x * z;
			z = x + y;
			x = z - x;
		}

		x = z - y;

		if(z != y){
			z = x - z;
			y = z - y;

			if(x == y){
				x = y - z;
				x = x * y;
			}
			else{
				y = y - x;

				if(z > y){
					z = x - y;
					z = x * y;
				}
				else{
					x = y + z;
					z = x - z;
					x = y * z;
					x = x - x;
				}

				x = x - y;
				z = x - z;
				y = y - y;
				x = y - z;
				x = y + y;
			}

			z = y - z;
		}
		else{
			z = y * x;
			y = y - x;
			z = y * z;
		}

	}


	printf("%d", z);
}
