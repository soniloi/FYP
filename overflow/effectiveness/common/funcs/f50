/* function auto-generated using seed: 8626596 */
void f50(int x, int y){
	int z = 1;
	y = x - z;

	if(z != x){
		x = x + z;

		if(y >= y){
			z = x * x;
		}

		z = x - x;
		y = z * x;
	}


	printf("%d", z);
}
