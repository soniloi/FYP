/* function auto-generated using seed: 248380 */
void f52(int x, int y){
	int z = 1;
	y = y * y;

	if(z < y){
		x = y * z;

		if(z != x){
			x = z * z;

			if(z == z){
				z = y * x;
				z = y - x;
				x = y + z;
			}
			else{
				z = y - x;
				x = z * x;
				y = y - z;
			}

			y = y + x;

			if(y >= x){
				z = y - z;
				x = y - z;
			}

		}
		else{
			x = z - z;

			if(z == y){
				x = y - x;
				y = z + z;
			}

		}

	}
	else{
		x = z - y;
	}


	printf("%d", z);
}
