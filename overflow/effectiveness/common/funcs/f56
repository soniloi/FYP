/* function auto-generated using seed: 4751950 */
void f56(int x, int y){
	int z = 1;
	y = z + x;

	if(x < x){
		y = x * x;

		if(x > x){
			z = y - x;
			x = x + x;

			if(y < y){
				z = z + x;
				z = z * y;
				y = x + y;
			}

		}
		else{
			y = z - z;

			if(z != y){
				z = z + y;
			}
			else{
				z = x - z;
			}

			z = y - y;
		}

	}
	else{
		z = y * x;
		y = x * z;
	}


	printf("%d", z);
}
