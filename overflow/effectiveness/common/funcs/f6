/* function auto-generated using seed: 8600185 */
void f6(int x, int y){
	int z = 1;
	y = y - z;

	if(z < z){
		x = z * x;

		if(x <= y){
			y = x * x;
			x = z + z;

			if(x <= y){
				y = y * z;
				y = y + x;
				y = y * x;
			}
			else{
				z = z * x;
				y = z * z;
			}

			x = z + x;
		}
		else{
			x = y + z;
			y = y * z;
			z = x * y;
		}

		x = y + x;
		y = y + z;
	}

	y = z + x;

	if(x >= z){
		x = z * y;
	}


	printf("%d", z);
}
