/* function auto-generated using seed: 1089458 */
void f62(int x, int y){
	int z = 1;
	x = z + z;

	if(y >= y){
		z = y - z;
		x = x + x;

		if(x > x){
			z = x + y;
			y = y + y;

			if(y > y){
				y = x - x;
			}

			z = y + y;
		}

	}

	x = x * y;

	if(y != z){
		y = x + y;
	}


	printf("%d", z);
}
