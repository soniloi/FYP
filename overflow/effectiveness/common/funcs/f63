/* function auto-generated using seed: 8831475 */
void f63(int x, int y){
	int z = 1;
	x = z + y;

	if(y < y){
		x = z - z;
	}
	else{
		y = x - z;

		if(z >= y){
			y = y * y;
			z = x * x;
			x = z + x;

			if(y < x){
				x = z * x;
				x = y * x;
				y = y - y;
			}

		}
		else{
			z = z * y;
			x = z + x;
		}

		x = x * z;
	}


	printf("%d", z);
}
