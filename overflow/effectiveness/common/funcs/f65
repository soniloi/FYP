/* function auto-generated using seed: 6377539 */
void f65(int x, int y){
	int z = 1;
	z = x + y;

	if(z >= y){
		x = x + z;
	}
	else{
		x = y + y;

		if(y <= x){
			y = x + x;
			z = z - y;
			y = y + z;
		}

		z = x - x;
		y = x * x;

		if(y == y){
			z = z - z;

			if(y != x){
				y = x + z;
				x = z + y;
			}
			else{
				z = z * y;
				x = z + z;
			}

			z = z - x;
		}

	}


	printf("%d", z);
}
