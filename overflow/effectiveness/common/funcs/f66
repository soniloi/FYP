/* function auto-generated using seed: 5746318 */
void f66(int x, int y){
	int z = 1;
	x = x * x;

	if(x != z){
		z = x + z;
		y = y + y;
	}
	else{
		x = y + y;
		y = y - y;

		if(x == y){
			x = y * y;
		}
		else{
			x = x * x;
			z = z + x;
		}

		y = z * z;
	}

	z = y - z;

	if(x >= z){
		y = z * z;
	}

	z = x - y;

	if(x > z){
		z = z + y;
		y = x + z;
	}


	printf("%d", z);
}
