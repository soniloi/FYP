/* function auto-generated using seed: 8470192 */
void f68(int x, int y){
	int z = 1;
	x = y - z;

	if(y <= z){
		x = z + z;

		if(y < x){
			y = x - y;
			x = z * y;

			if(z <= x){
				y = x - z;
				y = y * x;
				z = x - x;
			}
			else{
				z = y * y;
				z = x + x;
			}

		}

		x = x * y;

		if(z != z){
			y = x * x;
			z = z - z;
			x = y + x;

			if(x >= z){
				z = x * x;
			}
			else{
				x = z - z;
			}

		}
		else{
			x = x * x;
			y = y + z;

			if(y <= y){
				x = x - x;
				x = y - y;
			}
			else{
				z = z * y;
				z = z + z;
				y = z + x;
			}

			z = y - x;
		}

	}

	y = x + z;

	if(x < y){
		y = y + y;
	}

	y = x + x;

	if(z >= y){
		y = x - y;
		x = x + x;
		y = z - y;

		if(z > x){
			y = z - x;

			if(y <= x){
				z = y + x;
				x = y * z;
			}

			y = x * z;
			z = z * y;
		}
		else{
			x = z - z;
			z = y + y;

			if(x != y){
				y = x - z;
				z = x * x;
				x = y + x;
			}
			else{
				z = y * x;
			}

			y = x * y;
		}

	}


	printf("%d", z);
}
