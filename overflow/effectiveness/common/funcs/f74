/* function auto-generated using seed: 4780910 */
void f74(int x, int y){
	int z = 1;
	z = z + y;

	if(y >= y){
		z = x * y;

		if(z == x){
			z = x + z;

			if(z == x){
				y = x * y;
				x = x * x;
				x = x * x;
			}

			z = x + z;
			x = y * y;
		}
		else{
			z = x - x;
			z = x * y;

			if(y == y){
				z = x * z;
			}
			else{
				x = z + x;
				x = x * y;
				z = x * x;
			}

			z = y - x;
		}

		z = y * x;

		if(z >= x){
			z = z + z;

			if(z > x){
				x = z + x;
				x = z - z;
				z = x * z;
			}
			else{
				y = z + z;
				z = y + y;
			}

		}
		else{
			z = y - z;
		}

		x = z + x;

		if(x != z){
			z = y + x;
			z = x * y;
			y = x - x;
		}

	}

	x = x - y;

	if(y <= x){
		y = y - y;

		if(y <= z){
			z = x + x;
			x = x + z;

			if(x != y){
				z = y - y;
			}
			else{
				z = x + z;
				x = y + z;
			}

		}
		else{
			x = y + x;
			x = x * x;
		}

		x = z - z;
		y = x + x;

		if(x == x){
			y = y * x;

			if(z >= y){
				z = z * x;
			}
			else{
				z = y * y;
				z = y - y;
				y = x + x;
			}

			x = z * y;

			if(x < y){
				y = z * z;
			}

		}
		else{
			y = x * y;

			if(z < x){
				x = y * z;
				x = x * z;
				y = z + z;
			}

		}

	}

	z = z * z;

	if(x == y){
		y = z - y;

		if(x > z){
			y = y * y;
			x = z + x;
		}
		else{
			z = x * z;
		}

		z = x + z;
		y = y + y;
	}


	printf("%d", z);
}
