/* function auto-generated using seed: 6846973 */
void f75(int x, int y){
	int z = 1;
	y = y * z;

	if(x == z){
		z = y * z;
		x = y * y;
	}

	y = z - x;

	if(z == z){
		x = y - y;

		if(x != y){
			y = y * z;
			z = z * z;
			y = y - x;
		}

		z = z * x;
		x = z * y;
	}
	else{
		x = x * z;

		if(z >= y){
			x = z - x;
			z = x + y;
			x = z * y;
		}
		else{
			z = y * x;
			x = z - z;
			x = z + y;
		}

		y = x + x;

		if(y <= y){
			z = z + x;

			if(x > z){
				y = x - x;
				y = x + x;
			}

		}

	}


	printf("%d", z);
}
