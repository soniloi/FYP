/* function auto-generated using seed: 2781002 */
void f76(int x, int y){
	int z = 1;
	x = x * x;

	if(z == z){
		y = x + z;

		if(x <= x){
			y = z + y;
			x = x - y;
			z = y * y;

			if(z == x){
				y = x - z;
				z = z + x;
				x = x - y;
				x = y + y;

				if(z > y){
					x = x + x;

					if(y < x){
						y = z * y;
						y = x + x;
						y = z + z;
					}
					else{
						x = x * z;
						y = z * z;
						z = x + z;
						z = z * z;
						x = y * y;
						z = y * y;
					}

					x = y + z;
					z = z + x;
					z = y - z;
				}
				else{
					y = z - x;
				}

			}
			else{
				z = x - y;
				z = y * y;
			}

			x = z + y;
		}

	}
	else{
		y = x - y;

		if(y == z){
			y = y * z;
			z = x - y;

			if(z >= x){
				z = y + y;
				x = x * z;
			}

			z = y - x;

			if(z == x){
				x = x * y;
				x = y * z;
				z = z + y;

				if(x > x){
					y = x - y;

					if(y < z){
						y = y + x;
						z = y * z;
						y = x + y;
						z = z + y;
						z = z * z;
						x = x - z;
					}

					y = y * y;
					y = y + z;
					x = z + x;
					y = x - x;
				}
				else{
					x = z + x;
					x = y + x;
					y = x * x;
					z = x - x;

					if(y != z){
						z = z * y;
						z = z * y;
						y = x * x;
						z = y - y;
						z = y * z;
						y = x + z;
					}
					else{
						z = z + y;
						x = z * y;
						y = y - x;
						x = z + x;
						y = z - x;
						z = z + z;
					}

				}

			}

			y = z - x;
			x = y + y;

			if(y <= x){
				z = y + x;
				z = x - z;

				if(y >= x){
					z = z - y;

					if(x <= y){
						y = z + z;
						y = y + z;
						y = x + z;
						z = y * y;
					}

					y = x - y;

					if(y < y){
						y = x * x;
						x = z + x;
						z = z - z;
						x = z + z;
					}
					else{
						y = x + y;
					}

					x = y + y;
					y = y - z;
					y = z - z;
				}
				else{
					y = z - z;
					z = x - z;
					y = x + y;
					y = x * y;

					if(x >= z){
						y = z * z;
						z = x * z;
						z = x + y;
						y = y - x;
					}
					else{
						z = x * z;
						z = x - y;
						y = x * z;
						z = z + z;
					}

				}

				y = x + y;

				if(x <= x){
					y = y - x;
				}
				else{
					x = y - z;

					if(x >= y){
						x = z - y;
						z = y + z;
						z = z * y;
						y = z - y;
						y = x * x;
					}

					z = z * x;

					if(x != y){
						x = y * y;
					}
					else{
						y = z * z;
						x = z * x;
						z = x - z;
					}

				}

				y = z - y;
				z = z + y;
				z = x + z;

				if(x == y){
					z = z * y;
					x = x - x;

					if(z < y){
						x = x - y;
						z = y - x;
						y = z - z;
					}
					else{
						z = x + y;
						z = x * z;
					}

					x = y - y;
					x = y - y;

					if(y != x){
						z = z * z;
						x = z + y;
						y = z + y;
						y = x - x;
						y = x - x;
					}
					else{
						z = z * z;
						z = y + x;
						x = z - x;
					}

				}

			}

		}
		else{
			z = z + x;

			if(y < y){
				y = y * z;
			}
			else{
				z = z - z;
				x = y - x;
				x = x - z;

				if(y <= y){
					z = y * z;
					y = x * y;
					z = x * y;
				}

				z = y * y;
				z = x * z;
			}

			y = y + z;
			y = x * y;
			x = x * z;
			y = x * x;
			y = y + z;
		}

		y = y - y;
		x = z + y;

		if(x != z){
			y = y - y;
			x = y + x;
			z = x - y;

			if(z <= y){
				y = y + x;

				if(z != z){
					z = x * z;
					y = y - x;
					x = y - z;

					if(x == y){
						y = x - y;
						z = x + y;
						z = y - y;
						y = y + z;
						x = y + y;
						z = z - z;
					}

					y = x - z;
				}

			}
			else{
				y = y * y;
				z = y * z;
				x = x - z;

				if(x < z){
					y = y + y;
					y = x * z;
					y = x + x;
					z = y * z;

					if(z == x){
						z = z + y;
						z = x * z;
					}

					y = y + y;

					if(y == x){
						x = y + y;
						y = y * y;
						z = x + y;
					}

				}

				z = z - x;
			}

			x = x - x;
			x = z + y;
			y = y + x;

			if(x > x){
				y = y + z;
				x = x * y;
				x = y - z;
				y = y + x;

				if(y > z){
					y = z - y;

					if(z < z){
						z = z * y;
						z = z + y;
					}

					x = z + y;

					if(y >= z){
						z = y - z;
						y = x * x;
						y = x * z;
						z = z + x;
						x = y - z;
					}

					z = x + y;
					x = z + x;
					x = x + y;
					x = x + x;
				}

				z = z * x;

				if(x <= x){
					x = x + z;
					z = y * y;
					z = x + x;

					if(z >= x){
						x = x + z;
						y = y * x;
						z = x - z;
						y = y - z;
						z = y - y;
					}
					else{
						y = x + y;
						x = y + z;
					}

				}

			}

		}
		else{
			x = z - y;
			y = x * y;
			x = x * y;
			z = y * y;
			y = z - x;
		}

		y = x * z;
	}

	z = y - z;

	if(z <= z){
		x = y - z;
		z = y + x;
		z = z * z;

		if(x == z){
			z = x - x;
			z = x - x;
			z = x * y;
			z = z * y;
			z = x * z;
		}

		z = y * x;

		if(y > x){
			x = z * z;

			if(y == z){
				z = y - z;
			}

		}

		x = x + z;

		if(x == y){
			y = z * y;
			z = z + y;
		}
		else{
			y = y - y;
			z = x - z;

			if(x >= z){
				y = x * z;
				x = x - x;

				if(y <= x){
					x = x - x;
					z = z * y;
					z = z + z;
				}

				y = y + y;
				x = z - y;
				y = x + z;
			}

			x = x * y;

			if(z < z){
				y = z - y;

				if(x < y){
					z = z + y;
					x = y + y;
					z = z + y;
					y = x - y;
					x = y - x;
					y = z + z;
				}

				x = z - x;

				if(y != x){
					y = x + z;
					z = x - z;

					if(x >= x){
						y = z + y;
						x = z * z;
						x = y * z;
						x = z + z;
						z = x - z;
					}

					x = x * x;

					if(z <= z){
						x = z - y;
						z = x + z;
						z = y * y;
						y = y - x;
						x = x - y;
					}

					z = y - x;

					if(y == z){
						x = y * y;
						z = x + z;
					}
					else{
						x = x + z;
						z = x * x;
						z = y - z;
						y = x + z;
					}

				}
				else{
					z = y * x;
					x = z * x;
					y = z - x;
					z = x - y;
					z = x - z;

					if(y >= z){
						z = z * y;
						z = z - x;
					}

				}

				x = y - z;

				if(x < y){
					x = z + x;
				}

				y = x - x;

				if(z <= z){
					x = x * y;
					z = y - y;
					y = x * y;
					x = z - x;
					x = y - z;
				}
				else{
					y = y * z;
					y = z - y;
					x = y - x;
				}

				x = z + y;
			}

			z = x * x;
		}

		z = y + x;
	}


	printf("%d", z);
}
