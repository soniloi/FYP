/* function auto-generated using seed: 924466 */
void f82(int x, int y){
	int z = 1;
	x = z + z;

	if(y == z){
		z = y + x;

		if(z < x){
			x = z * y;
			y = z + z;
			z = x + y;
		}

	}
	else{
		z = x * z;

		if(y >= z){
			y = y - x;
			x = z * y;

			if(x > y){
				x = x + y;
			}
			else{
				x = x + x;
			}

			x = x - z;
		}

		y = z + z;
		x = y + y;
	}


	printf("%d", z);
}
