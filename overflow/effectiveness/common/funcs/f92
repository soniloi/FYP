/* function auto-generated using seed: 1238186 */
void f92(int x, int y){
	int z = 1;
	z = y - z;

	if(x != x){
		z = x + z;
		x = x - x;

		if(y > z){
			y = x - z;
			x = z * z;
		}
		else{
			y = y + x;
			z = y - z;
			y = y * y;
		}

	}
	else{
		z = y - y;
		y = x - x;

		if(x == y){
			z = z + z;

			if(y == x){
				z = y * z;
				z = x * z;
				z = z - x;
			}
			else{
				x = x * x;
				y = z * y;
			}

			z = x + x;

			if(z != z){
				y = z + z;
			}
			else{
				y = z - x;
			}

		}

	}

	x = z + y;

	if(z == y){
		y = z + y;

		if(x > y){
			y = z - y;
		}

		y = y * z;
	}


	printf("%d", z);
}
