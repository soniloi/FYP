/* function auto-generated using seed: 1447872 */
void f99(int x, int y){
	int z = 1;
	z = z * z;

	if(y != z){
		x = y + y;
		z = z - x;

		if(z != z){
			z = y + y;
		}
		else{
			y = x - z;
			z = y * y;
			y = x * z;
		}

	}
	else{
		z = z + z;
	}

	x = z * z;

	if(x != z){
		x = y + z;

		if(z < x){
			x = x - y;
		}

		z = y - y;

		if(y == x){
			z = x * x;

			if(z > y){
				z = y * y;
				z = y + y;
			}
			else{
				y = y + x;
				z = z - x;
			}

			z = x * x;
		}

		y = x + y;
	}


	printf("%d", z);
}
