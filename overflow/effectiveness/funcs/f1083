/* function auto-generated using seed: 4520269 */
void f1083(int x, int y){
	int z = 1;
	y = x * z;

	if(x >= z){
		x = x + x;

		if(y > z){
			y = y * z;
			y = x - z;

			if(z == x){
				y = y - y;
			}

			x = y + z;

			if(y > z){
				x = x + x;
				z = y * z;
				y = x * z;
			}
			else{
				y = y - x;
			}

			x = y + x;

			if(x != y){
				y = z - x;

				if(x != z){
					y = x * x;
				}

				z = z * x;
				x = z * y;
				y = x + y;

				if(y > x){
					x = y + z;
					z = y * x;

					if(x > z){
						y = y + y;
						x = z * x;
						y = x - y;
						z = y + y;
						x = x - z;
						x = y - x;
					}
					else{
						x = z + y;
						z = x + z;
						y = z + z;
						y = y - y;
						y = y + x;
						z = z * y;
					}

					z = x - z;

					if(y != y){
						y = x + y;
						z = y + z;
						z = x - y;
						z = y - y;
						x = x + x;
					}

					y = z + x;
					y = x * z;
					x = x * x;

					if(z == x){
						y = y - z;
						x = y + y;
						z = y * y;
						y = z - z;
						y = z * z;
						y = z + x;
					}

				}
				else{
					z = x * y;
					x = z - y;
					y = z * x;
					x = z * y;
					x = y * x;
					y = x * x;
				}

			}
			else{
				x = z - z;
				y = x * z;
				y = z + y;
			}

			z = z * y;

			if(y <= z){
				y = y - y;
				x = y + z;
			}

			y = x + y;
		}

		y = z + z;

		if(x <= x){
			z = y + y;
			x = y * z;

			if(x < y){
				y = x * y;
				z = z + y;

				if(x != x){
					x = x * x;
					x = x + y;

					if(z > y){
						y = y + x;
						z = x + x;
						x = y + z;
						x = x + x;
						y = y * y;
						y = x + y;
					}

					x = x + y;
				}
				else{
					z = y + x;

					if(z != y){
						z = x * z;
					}
					else{
						y = x * z;
						y = z * y;
						z = y * z;
						x = z + y;
						x = x - x;
					}

					y = y - y;

					if(x >= y){
						z = x - y;
						x = y - z;
						x = x * x;
					}
					else{
						y = x + x;
						z = z * y;
						z = y + z;
						z = z * x;
						z = z * y;
					}

					x = z - y;
					y = x + y;
					z = y + y;
					y = y * z;
				}

				x = z * x;
				x = x + x;
			}

		}

	}

	y = z - x;

	if(y < x){
		x = y - y;

		if(z == z){
			x = y - x;

			if(x >= y){
				x = z * y;
				z = z + y;
				z = y + y;
				z = y * y;
			}
			else{
				y = z * z;
				z = z * y;

				if(x <= x){
					x = z + y;

					if(x > x){
						y = z + y;
						y = x - x;
						y = y * x;
						y = z * z;
						y = x * z;
						x = y - y;
					}

					x = y + y;

					if(x >= y){
						z = z - x;
						y = x + z;
						z = x - z;
						x = y * y;
						z = y - z;
					}
					else{
						x = x * x;
						z = x - x;
						y = z + y;
						z = x + y;
					}

					z = x + x;
					y = y * y;
					x = x - y;
					z = x - x;
				}

			}

		}

		z = z * z;

		if(x == y){
			y = x * x;
			y = y - y;
			x = x + x;
			z = x * z;
			z = z - z;

			if(x == z){
				y = y * y;

				if(y >= y){
					z = y + y;
					x = z - z;

					if(z > z){
						x = x + x;
						y = x * z;
						z = x + z;
						z = z + x;
						y = x + y;
					}

					x = x + x;

					if(y > z){
						z = y * z;
						y = y * x;
						y = x - x;
						x = z + z;
						y = y - x;
					}
					else{
						y = y - x;
						z = z * z;
						z = x + y;
						y = x * z;
					}

				}
				else{
					y = y - y;
					y = y * y;

					if(x <= z){
						x = z + y;
					}
					else{
						y = x - z;
						y = x + x;
						x = y * z;
						x = y - z;
						x = y * y;
						z = y * z;
					}

					x = z - z;
				}

				z = x * x;
				z = x * x;
				x = y - z;
			}
			else{
				y = z * y;
				y = x + z;

				if(y == x){
					y = z - z;
					x = x - y;
					y = z - x;
				}

				z = y * x;
				z = y - y;
				x = z + y;

				if(y < z){
					z = y - z;
					y = y - z;
				}
				else{
					z = x - x;
					x = z - z;
				}

			}

		}
		else{
			x = z - y;
		}

		x = y + z;
		y = x + x;

		if(x > z){
			x = y - z;

			if(x > z){
				z = x + x;

				if(z <= y){
					x = z - z;
					y = z + y;
					y = x * y;

					if(x > z){
						x = z - x;
						x = x - z;
						z = x - y;
						z = z * z;
					}
					else{
						z = y - x;
						x = y - y;
					}

				}

				x = z * y;
				x = y - y;
				y = z * z;
			}
			else{
				x = x * z;
				y = x * y;

				if(z < x){
					x = z * z;
					x = y + z;
					z = x + z;
				}

			}

			x = z * x;
			x = z * x;

			if(y <= x){
				x = x - y;

				if(x >= x){
					z = x + x;

					if(x < y){
						z = z * z;
						x = x + x;
						y = z * x;
						x = y - y;
						y = z + y;
					}

					z = y * z;
					x = y - y;
					z = z * y;
					y = x - y;
					z = x + z;
				}
				else{
					x = y * z;
					y = x * x;

					if(y > z){
						z = y - z;
						z = y + z;
						z = x - z;
						x = y - x;
						z = y - y;
						z = z + z;
					}
					else{
						x = y - x;
					}

					y = x + x;
					y = y + z;
				}

			}

		}

		y = z * y;

		if(x > z){
			y = x * y;

			if(x < y){
				y = z * z;
				x = x + y;
			}
			else{
				y = z * y;
			}

			y = x + x;
		}

		y = z * x;
	}

	x = y - y;

	if(z == x){
		x = y - x;
		y = x - y;
		y = z * y;

		if(y > y){
			y = x + x;

			if(z == x){
				x = z - x;
				x = z + z;
				y = y + x;
			}

			z = x - y;
			y = z - y;

			if(z > x){
				x = x + x;
				z = x + x;
				z = z - z;
			}

		}

	}


	printf("%d", z);
}
