/* function auto-generated using seed: 7052749 */
void f1144(int x, int y){
	int z = 1;
	y = x * x;

	if(y != y){
		z = x * y;
		y = z + z;

		if(x != z){
			x = x + y;

			if(x <= y){
				x = x + y;

				if(y >= x){
					x = x * y;
				}

				y = x * x;
				z = z * z;
			}

			x = z + y;
		}
		else{
			z = z + y;
			x = z - y;
		}

		y = y * y;
	}
	else{
		x = z + z;
		x = z * y;
		x = x * z;

		if(y >= z){
			y = x - x;
			x = z * z;
		}

		z = z - y;
	}


	printf("%d", z);
}
