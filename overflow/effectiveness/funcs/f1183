/* function auto-generated using seed: 522452 */
void f1183(int x, int y){
	int z = 1;
	z = z - x;

	if(z != x){
		y = x - y;
		x = x * y;

		if(y < y){
			y = x + y;
			x = z + y;
			y = z - x;

			if(y > x){
				x = x + z;
			}

		}
		else{
			x = z - x;
			y = x - y;
			z = x - y;

			if(y == y){
				z = x - x;
				z = z + x;
				z = x * x;

				if(y != x){
					y = z - y;

					if(y > y){
						z = z - z;
					}

					y = y * z;

					if(y >= z){
						z = z - x;
						y = z + z;
						y = y + y;
						x = x + x;
						y = x - x;
					}

					z = y - y;
				}

				x = z + x;
				z = z * z;
				z = z * y;

				if(z == z){
					x = y + z;

					if(z != x){
						z = x * y;
						y = y + x;
					}
					else{
						x = x * y;
					}

					y = z + y;
				}

			}

			x = x * x;
		}

	}
	else{
		x = y + y;

		if(x < z){
			y = x - z;
		}
		else{
			z = y - y;
			z = x + x;

			if(y == z){
				x = z - x;

				if(x > x){
					y = z + y;
					x = y * z;
					z = z * y;
					x = x - x;

					if(y <= x){
						y = z - y;
						x = y - z;
						z = z + z;
						y = y + x;
					}
					else{
						x = z - x;
					}

					x = x * x;
					x = y - x;
				}

			}
			else{
				x = z + y;

				if(y <= z){
					x = y - y;
					y = z * y;
					y = y - y;
					y = y + z;
					z = z - y;
					y = z - x;

					if(y >= y){
						x = z + z;
						y = y + z;
						z = x - x;
						y = z - y;
						x = z + z;
						x = y - y;
					}

				}

			}

			x = z * y;

			if(x < y){
				x = z * x;
				y = x * y;
				y = z + z;
				z = z - x;

				if(z < z){
					z = z - z;
					x = x - x;
					z = y - z;
				}
				else{
					z = x - y;
					z = x * x;
					y = y + z;
				}

			}
			else{
				y = z * z;
			}

			x = y + x;
		}

		z = x - x;

		if(z < y){
			z = z - z;

			if(z != x){
				z = z + x;
				x = x - y;
				y = z * x;

				if(x != y){
					z = y - y;

					if(z >= x){
						y = y - y;
						z = x * x;
					}

					y = y * x;
					x = x - z;
					x = x - z;

					if(z <= y){
						x = x * y;
						y = y + y;
						y = y - z;
						x = y + y;
						x = x - x;
						x = z + y;
					}
					else{
						y = x + z;
						z = y * z;
						y = z + z;
					}

					z = y - x;
				}

			}

			y = x * y;
			y = x - z;

			if(y < x){
				x = z + y;
				y = y - x;
				y = z * y;
				y = x + y;
				x = z - x;
				z = z + x;
			}

			z = x - y;

			if(y != y){
				z = x * y;
				x = z * z;
				y = x + z;
				x = x + y;
			}
			else{
				x = x * z;

				if(y > z){
					y = x - y;

					if(z > x){
						z = y * x;
					}
					else{
						z = y - x;
						z = y - x;
					}

					z = y * z;
					x = y * x;
					z = x + x;

					if(z <= z){
						y = z + z;
						x = y * z;
						y = z + x;
						x = y * y;
						x = y * y;
					}

					x = y - z;
				}
				else{
					y = y * z;
				}

				x = x - z;
			}

			y = y + z;
			z = y * z;
		}
		else{
			x = x * x;

			if(z <= y){
				y = z - y;
				y = x + x;
				z = y - y;
				x = y - x;
			}
			else{
				y = z + x;
				z = x - x;
			}

			z = y * y;

			if(x <= x){
				y = y * y;

				if(y >= z){
					z = z - z;

					if(z < z){
						z = x - x;
						y = y * y;
						x = z - x;
					}

					z = z * x;
					z = z * x;
					y = x + z;
					y = y - z;
				}

				z = x + x;
				z = z * y;
				x = z - x;
			}
			else{
				z = y - z;
			}

			z = z - y;
		}

		y = z - y;
		x = z * y;

		if(z >= z){
			x = z - x;
			z = x - z;

			if(z <= y){
				y = x - z;
				y = z * x;
				y = z + y;
				z = x - y;
				y = x * y;
				x = x - y;
			}
			else{
				y = x - x;
			}

			x = z + z;
			z = z * x;
			y = z + x;
		}

	}

	y = y - z;

	if(y > x){
		y = z * x;

		if(y > x){
			z = x * z;

			if(y >= x){
				x = z + y;
				y = y - x;
				x = x - z;
				z = y * y;
				x = z + y;
			}
			else{
				z = z * y;

				if(x == x){
					y = y - z;
					y = x * z;
					y = x + x;
					x = x * x;
					z = z - z;
					z = x + y;
				}

			}

			z = y - y;

			if(x != z){
				z = z - z;
				y = z - x;
				y = x - x;
				z = z * z;
				x = z + y;
				y = y * z;

				if(x > x){
					x = y + y;
					z = z * x;

					if(x > x){
						z = z - z;
						y = z * z;
						z = x * z;
					}
					else{
						z = x + z;
						y = z - y;
					}

					x = x + y;
					x = y + x;

					if(y <= y){
						z = z + x;
						y = x * y;
						x = z - z;
					}
					else{
						y = y - z;
						z = y + y;
					}

				}

			}

			y = x + z;

			if(z != y){
				y = z + y;

				if(x >= y){
					y = z * z;
					x = y + y;
					x = y - z;

					if(x > y){
						z = z * y;
						y = z * x;
					}
					else{
						z = x * x;
						y = x * x;
					}

				}
				else{
					x = y + y;
					x = x * z;
				}

				z = z + z;
				x = z + x;
				y = x * z;
				y = x * y;
			}

			x = y + y;

			if(x == z){
				y = x - x;

				if(z > x){
					y = x - y;

					if(z > x){
						z = x + z;
						y = y * z;
					}
					else{
						z = y - y;
					}

					z = y + z;
					y = x - y;
				}

				z = x - x;
				x = y + y;
				y = y * x;
				x = z + x;
				z = y + z;
			}
			else{
				x = z - z;
				y = x * z;
				y = x - y;
			}

		}

		x = y * y;

		if(z < x){
			y = x * y;
			z = x - x;
			x = z + y;
			x = z * z;
			z = y - y;
			y = x * x;
		}
		else{
			x = y + x;

			if(z != y){
				y = y + z;
				x = y + y;
				y = x + x;
				z = y + z;
				x = y + y;

				if(x >= y){
					z = z - x;
					y = x - z;
					y = y * y;

					if(y >= z){
						y = z * z;
						x = y + z;
						y = z - z;
					}
					else{
						z = z + z;
						x = x * z;
					}

					x = x - x;
				}
				else{
					x = y * y;

					if(x == z){
						y = y - x;
						x = z - y;
						x = z + y;
						y = z + x;
						y = z + z;
					}
					else{
						y = x - y;
						z = z - x;
						y = y * x;
						z = x * z;
						x = z - x;
						z = z - z;
					}

					x = y + x;
					y = z * y;
					x = x + z;
					y = z + y;
				}

			}
			else{
				y = x + x;
				x = y + x;

				if(z != z){
					x = z * y;
					x = y - x;
					y = y * y;

					if(z > y){
						z = x * z;
						y = y - x;
						x = z + y;
						y = z * x;
						x = y - y;
					}

					y = x + x;
					y = y - z;
				}

			}

			x = z + y;
			z = z - y;

			if(z >= z){
				z = x * y;
				z = y * x;
				z = x + x;
				z = z + y;
			}
			else{
				x = z - y;
				x = z + z;

				if(x != y){
					z = x + x;
					y = x + y;
					x = x - x;

					if(x == z){
						z = x * x;
						y = x * z;
						x = y - y;
						y = y - y;
						x = x - z;
					}

					z = y + y;
					x = y * x;
				}
				else{
					x = z - y;
				}

				z = z - x;
				x = z + z;
				y = x * x;
			}

		}

		y = y + z;

		if(y > y){
			z = z - y;
		}

		x = z + z;

		if(x <= x){
			z = z + z;
		}
		else{
			z = z - x;
		}

		z = x + z;

		if(z > z){
			x = y * y;
		}
		else{
			y = z * z;
		}

		y = y * y;
	}

	y = y + z;

	if(y >= y){
		x = y - x;

		if(z <= z){
			x = y + y;

			if(z >= y){
				y = x * y;
				z = y - x;
			}

			z = z * x;
		}
		else{
			z = x - y;
			z = z + x;
			x = y * y;
		}

	}
	else{
		x = x - z;

		if(y < x){
			z = z + z;

			if(z != x){
				z = x - y;
				z = y * y;
				y = z - y;

				if(z != x){
					z = z * x;
					z = z - z;
					x = x + z;
				}

				y = x - y;
				y = x * y;
				z = x * z;
			}

			x = x * z;
			y = y - y;
			x = y - z;

			if(z >= y){
				z = y + x;

				if(y >= z){
					x = z - x;
					y = z * y;
				}

				y = x - z;
				x = y * z;
			}

			z = x - z;
		}

		x = x + y;
	}


	printf("%d", z);
}
