/* function auto-generated using seed: 7349208 */
void f125(int x, int y){
	int z = 1;
	x = x + z;

	if(y == x){
		y = z + x;
		y = y * y;
		x = x - z;

		if(y > z){
			x = y * y;
			z = z - y;
			y = y - x;
		}
		else{
			z = x + z;
			z = z - x;

			if(y >= x){
				y = x * y;
			}

			z = z + x;
		}

	}
	else{
		z = z + z;
		y = z - z;

		if(z == x){
			x = y + z;
			y = z - y;
			z = y - x;
			x = y + x;
		}

		z = y - z;
		y = z - y;
	}


	printf("%d", z);
}
