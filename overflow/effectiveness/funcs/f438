/* function auto-generated using seed: 4730547 */
void f438(int x, int y){
	int z = 1;
	z = x * y;

	if(y > z){
		y = y - y;
		z = x + y;

		if(x < z){
			y = z - x;
			x = z + y;
			y = y * z;
			y = y * z;
			x = x - y;

			if(z < y){
				z = z - z;
				x = x - z;
				z = y * x;
			}

			y = x - y;
		}
		else{
			z = x * z;
			x = x + z;
			z = z * y;
			y = y - x;
		}

		y = z - z;
		z = x + y;
		x = x + y;
	}
	else{
		z = z * y;
		y = z + z;

		if(z != z){
			x = z * y;

			if(y >= z){
				z = x + z;
				y = x - z;

				if(z <= y){
					y = x * x;
					z = y - x;
					x = z - y;
				}
				else{
					z = z * y;
					y = z - z;
					x = y - z;

					if(x != x){
						z = x * z;
						y = x - x;
						y = z * z;
						z = x - y;
						x = x * y;
						x = z - z;
					}
					else{
						x = y - x;
						y = y * x;
						z = y + x;
						x = z * x;
					}

				}

				z = z + z;
				y = z * x;

				if(y != z){
					x = z + y;

					if(z > x){
						y = y - y;
						y = x - x;
						x = x + y;
						y = z * y;
					}
					else{
						x = z * x;
						z = x + y;
						z = z * x;
						x = z - y;
					}

					y = z - x;
					x = z - z;
				}

				z = z + x;
				y = z - z;
			}
			else{
				y = z * y;
				z = x + y;
				x = z - x;

				if(z < y){
					z = x - x;
					x = z * y;
					z = x * y;
					y = y * y;
				}

				y = y - z;
			}

		}

		y = y * x;

		if(x != y){
			z = z - z;

			if(y != y){
				z = y + z;
				z = y * x;
				x = y - y;

				if(z != y){
					z = z * x;
				}
				else{
					y = y + z;
					z = y + y;

					if(x > z){
						y = x - y;
						y = z - z;
						x = x - z;
						y = x + x;
						z = y + y;
					}

					x = x + x;
					x = y + z;
					x = x - y;
				}

				x = y - z;
				y = y * z;

				if(z >= y){
					y = y * z;
				}

			}

		}

	}

	y = z - y;

	if(y >= y){
		z = y - y;
		z = y - x;

		if(z > y){
			y = y - y;
			x = y + y;
			x = y - x;

			if(y > x){
				z = y + y;
				z = y * y;
			}

			z = x - z;
			y = y - y;
			z = x * y;

			if(z > z){
				y = z + x;
				x = y + z;

				if(y >= x){
					x = z - x;
				}

				y = x - z;
				x = y * x;
				z = x + x;

				if(z != z){
					x = x - x;
					y = z - y;

					if(y > y){
						x = x - x;
						z = y - y;
						x = x * z;
						x = z + x;
						x = x * y;
					}
					else{
						z = y * y;
						x = z * y;
					}

					x = y * z;
					y = z + x;
				}

				z = x - y;
			}
			else{
				x = z - y;

				if(z < z){
					y = x - x;
					z = y + z;
					x = x + z;
				}

				y = z + x;

				if(y < z){
					x = x + y;
					x = y + x;
					x = y * z;
					x = x - y;
					x = x - y;
				}
				else{
					z = z + y;

					if(x != y){
						z = z * x;
						z = z * z;
						z = y * z;
					}

					x = y * x;
					x = x - z;
					x = x - x;

					if(x >= z){
						z = y * x;
						z = y - y;
						x = x + y;
						y = x * z;
					}

					z = y + y;
				}

				z = y * z;
				y = y + y;

				if(z >= z){
					x = y * z;
					y = z - x;
					x = x * z;
				}

				y = y - y;

				if(y != z){
					y = z * x;
					y = z + x;
					x = z * x;
					z = y + x;
					z = y - y;
				}
				else{
					z = z - y;
					z = y * x;

					if(z >= z){
						x = x * y;
						y = z * z;
						z = y + y;
						z = y - x;
						y = x + z;
						z = y * x;
					}

				}

				z = z - z;
			}

		}

	}
	else{
		x = y - z;

		if(z == z){
			y = z * y;

			if(z <= y){
				y = x - y;
			}

			x = x - x;
			x = z + y;

			if(x <= x){
				y = z * y;
				x = z - y;
				y = x * x;
				z = x - x;
				x = x + x;
			}

		}

		x = z + y;

		if(x == y){
			x = x + z;
			z = z * z;

			if(z >= x){
				z = z * z;
				z = y + z;
				x = z + y;
				z = x + z;
				y = x * z;

				if(y <= z){
					x = z * x;
				}

				x = x - x;
			}
			else{
				y = z + z;

				if(z >= z){
					z = z * y;
					z = z - y;
					y = z + x;
				}

			}

		}

		z = y - x;
		x = y * x;

		if(y <= y){
			z = z - z;
			z = x + x;
		}

	}

	x = y + y;

	if(y <= z){
		y = z * x;
		y = x - y;
	}

	y = x - z;

	if(y != y){
		x = y * x;
		x = z - y;

		if(x > y){
			z = z * z;
			x = x * x;
			y = z + z;
			x = x * z;

			if(z <= x){
				x = y * x;
				y = x * z;
				y = x - x;
				y = z + y;
				x = y * x;
				x = z + y;
			}
			else{
				y = x + x;

				if(z <= y){
					y = x + y;
					y = z * y;
				}

				y = z + y;
				z = x * z;

				if(y >= z){
					z = z * y;
					y = y - x;

					if(z >= x){
						z = x - x;
					}

					z = x - x;
					x = y + x;
				}
				else{
					z = z - x;
					x = z - z;
					z = x * x;
					x = x + x;
					x = y - x;
					x = z * x;
				}

				z = z + x;
			}

		}

		y = z - z;
	}
	else{
		y = y + y;

		if(y != z){
			z = y - y;
			y = z + y;
			y = y * x;
		}

		z = x + x;

		if(y > z){
			x = y * y;
			y = z + y;
			x = z * x;

			if(y > z){
				x = z * y;
				x = y + z;
				z = z * x;

				if(y >= z){
					x = x + y;
					x = z * y;
				}
				else{
					z = x + y;
					y = y * y;
					z = x + y;

					if(y <= x){
						z = y + x;
						z = y * z;
					}
					else{
						z = x + y;
						y = z * x;
						x = y + x;
						z = z - x;
						y = x * y;
					}

					z = y + y;
					z = x * z;
					x = y - x;

					if(x != x){
						z = y * z;
						z = x - y;
						z = x - z;
						z = x - z;
					}

				}

				x = z + y;
				y = z - z;
				x = y - y;
			}
			else{
				x = y - x;
				x = y + y;
				z = y * x;
				z = z - z;
				z = z * z;
			}

			z = z + z;
			x = y + x;
		}
		else{
			x = z + y;
			x = x - x;
			x = x - y;
			z = y - y;
		}

	}

	x = x * x;

	if(y > z){
		z = y * y;
		x = y + x;

		if(z > x){
			y = x - y;
			z = x + z;

			if(z == z){
				x = y * x;
				x = z * y;

				if(z == x){
					z = y * x;
					x = z - y;

					if(z >= x){
						z = y + z;
						z = y * y;
						x = x - y;
						z = y + z;
						z = y - y;
						x = x * x;
					}
					else{
						z = y - y;
						z = x + y;
						x = z * x;
						y = x * y;
					}

					x = z * z;
				}

				x = y + z;
				z = y * z;
				y = z * y;
				y = x + x;
			}
			else{
				x = y - z;
			}

		}

	}


	printf("%d", z);
}
