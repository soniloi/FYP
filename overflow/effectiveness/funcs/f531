/* function auto-generated using seed: 1121514 */
void f531(int x, int y){
	int z = 1;
	z = z + x;

	if(z != z){
		z = y - z;
		x = z * x;
	}

	x = y * y;

	if(x > y){
		z = x + y;

		if(y == y){
			y = z + z;
			y = z + z;
			x = y * z;
		}

		z = y * z;
	}
	else{
		z = y + z;

		if(x == z){
			y = y + y;
			x = x * x;
			x = x + z;
		}

		y = y - y;

		if(y != y){
			x = z - x;
			z = y + z;
			z = y + x;
			y = z - y;

			if(x >= y){
				y = y * y;
				z = z - y;
				z = z * z;

				if(z < y){
					y = y * z;
					z = z * x;
					y = y - z;
				}

				y = z * z;

				if(y == y){
					y = z * x;
					x = y + x;
					z = x + x;
					z = x * y;
					x = x + x;

					if(z != z){
						y = x - x;
						x = z * z;
						x = y + z;
					}

					z = z * y;
				}
				else{
					y = z - y;
					z = x * y;
				}

				z = y + x;
				y = y * x;
			}

			z = z + y;
		}
		else{
			z = y - x;
			y = y * x;
		}

		x = y + x;

		if(z <= y){
			z = z + z;

			if(y < x){
				x = y * x;
				x = z + z;

				if(x < z){
					z = z - y;
					y = x - z;
					x = y + z;

					if(z < z){
						z = x - y;
						z = z + y;
					}
					else{
						z = x + y;
						x = y + x;
						y = x - x;
					}

					x = y + z;
					z = x - x;
					z = z - z;
				}

				z = y + x;

				if(z <= z){
					y = x - x;
					y = y - y;
					x = z * z;
					z = x + x;
				}
				else{
					y = x - y;

					if(z > z){
						x = z + x;
						y = x + y;
						x = x - x;
						y = z + x;
						z = y * z;
						y = y + z;
					}
					else{
						z = x * z;
					}

					x = x * x;
					y = y * z;

					if(x != y){
						x = y + y;
						z = z - x;
						z = z + y;
						x = y * x;
					}

					y = y * z;
					z = z * x;
				}

			}

			x = z - z;
			z = z + x;
			x = y * y;

			if(y < y){
				y = z + x;
				y = x + y;
				z = z + z;
				y = z - y;

				if(y != z){
					x = z + y;
					z = z * z;

					if(y <= y){
						z = x - z;
						y = z * z;
						x = x - z;
						z = y * x;
						y = z - x;
						z = x * y;
					}
					else{
						z = y - z;
						y = x + x;
						y = z * z;
					}

					z = z * x;
				}
				else{
					y = z - x;

					if(y >= z){
						x = z * z;
						z = y - x;
						y = z * z;
					}

					x = y + z;
					y = z + z;
				}

				y = y * x;
			}
			else{
				z = x + x;

				if(x >= z){
					z = z + z;
					y = x - y;
					z = x - z;
					y = y - y;
					z = y * y;

					if(z <= x){
						x = y + z;
						y = x + z;
						x = x - x;
						z = x + z;
						z = x + z;
						y = z * x;
					}

					y = z + z;
				}

			}

			z = y + y;
			y = x * y;
		}
		else{
			x = y * y;
			y = z - x;
			y = y + y;
		}

		z = x + y;
		z = x + z;

		if(x > x){
			z = y + y;

			if(z < z){
				z = x + x;
			}
			else{
				y = y - z;
				x = x - z;
				x = x * z;

				if(x > y){
					x = y - z;
				}

				x = x - z;
				x = x * y;

				if(x < y){
					y = y - z;
					y = y * x;
					z = x - y;
					x = z + y;
					y = z - z;
					z = x + z;
				}

				y = z - x;
			}

			z = x + x;

			if(z >= x){
				x = z + x;
				z = x + z;
			}
			else{
				z = x * x;
				y = z - x;

				if(y <= z){
					y = y - z;

					if(x > y){
						x = x * y;
						x = z - x;
						z = y - x;
						y = z * z;
						z = y - x;
					}

					y = z - y;
					z = y * x;
				}

				x = z * x;
				x = x - x;
			}

			z = z - x;

			if(x <= y){
				y = x - z;
				y = y * y;
				x = z - y;

				if(x < x){
					z = z * x;
					y = y + x;
				}
				else{
					z = z * x;
					x = z * x;
					z = z - y;
					x = z * y;
					z = y + y;
				}

				y = y - x;

				if(x < z){
					y = x + y;
				}
				else{
					y = z - x;
					y = x + y;
					y = y + z;

					if(x < x){
						y = x - y;
						y = z * y;
						z = y + y;
						z = z * y;
					}
					else{
						y = y * x;
						x = z + z;
						y = z + z;
					}

				}

			}
			else{
				x = y * x;
			}

			y = z - x;
			y = y - x;
		}
		else{
			y = z + x;
			z = z * x;

			if(y < y){
				x = x + y;
				z = y + y;
				z = x - z;
				x = y * x;
				x = y + y;
				y = y + x;
			}
			else{
				x = y - z;
			}

			x = z + x;

			if(y >= z){
				x = y + x;

				if(x == z){
					x = y - x;

					if(x >= z){
						x = z - y;
						x = z - y;
						y = y + y;
						x = x * x;
						z = y - y;
					}

					y = y + z;

					if(x > z){
						y = y + z;
					}
					else{
						y = y + y;
						x = x * x;
						z = z * y;
						z = y * x;
					}

					z = z - x;

					if(z < x){
						z = z + x;
						y = y * x;
						x = z * y;
					}

				}
				else{
					z = x * x;

					if(z < z){
						z = x - z;
						z = z * y;
						x = z + y;
						x = y * x;
						z = x - x;
					}

				}

				z = z * z;
			}
			else{
				y = x * z;
				y = y - y;

				if(x > y){
					x = y * y;
					z = y - x;

					if(z >= y){
						y = y - z;
					}
					else{
						z = y * x;
					}

					y = x * x;
					z = x + z;
					y = x + y;
					y = z - y;
				}
				else{
					x = y - y;
					x = z * z;
					y = y + x;

					if(z != x){
						y = x * y;
						z = x * x;
						y = y - z;
						y = y + x;
						z = z * x;
					}
					else{
						y = x - z;
						y = x * z;
						x = x - z;
						y = z * x;
						y = z - x;
						x = y * y;
					}

					y = y * y;
					y = x * y;
					z = y + x;
				}

			}

			y = y - x;

			if(x != y){
				x = x * z;

				if(z > y){
					y = x - x;
				}

				z = x + x;
				z = z - y;

				if(y >= x){
					z = y * z;

					if(x > z){
						x = z + z;
						z = x + z;
						z = x * x;
						z = z + y;
					}
					else{
						x = x + z;
						x = y * y;
						y = x - z;
						z = y - x;
					}

				}
				else{
					x = y - x;

					if(z != z){
						x = z + x;
						x = y - y;
					}

					z = y + y;
				}

				z = y + z;
			}
			else{
				y = z + x;
			}

		}

		y = x - y;
	}


	printf("%d", z);
}
