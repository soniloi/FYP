/* function auto-generated using seed: 8017154 */
void f532(int x, int y){
	int z = 1;
	z = x - z;

	if(y == x){
		x = z - z;

		if(y >= x){
			y = z - x;

			if(z == z){
				z = z + x;

				if(x >= z){
					x = y + y;
				}

				y = x * x;
				z = z + y;
			}

			y = y - z;
			x = x + x;
			y = x - z;
		}
		else{
			y = y + x;
			y = z - x;
		}

		z = x * y;
		z = x + x;
		z = z * z;
		x = x * z;
	}
	else{
		y = x + z;
		y = y + y;
		z = x + x;

		if(y >= y){
			z = y - x;

			if(x == x){
				x = z * y;
				x = x + x;
				z = z - z;
				y = y - z;

				if(z <= z){
					y = x - y;

					if(x <= y){
						x = y - x;
						y = y - z;
						y = y + y;
						y = y + x;
						y = z - y;
					}

					z = z * z;
				}
				else{
					y = x - z;
					z = z * z;
					y = z * x;
				}

				x = z * x;
				y = z + y;
			}

			x = x + x;
		}

		y = y * y;
		y = x + z;

		if(y == z){
			y = x * y;
			z = y + y;

			if(y != x){
				y = x - y;
				z = z + y;
			}
			else{
				z = z + z;
				x = x - x;
				x = y * z;
				x = x * z;

				if(z >= y){
					x = z * x;
				}

				x = x + z;
			}

		}
		else{
			z = z + z;
			x = z - x;
		}

	}

	y = x - z;

	if(z != x){
		y = z * x;

		if(z >= y){
			z = x + y;

			if(z <= z){
				y = z - z;
				z = z - y;
			}
			else{
				y = x + y;
				z = z + y;
				y = x - x;
				x = z + y;
				z = z - x;
				x = x * x;
			}

		}

		y = y * z;

		if(y != x){
			x = z - x;
			y = z - y;
			z = z + z;
			x = z * z;

			if(z == y){
				x = x * y;
				z = x + z;
				z = x - y;
				z = x + y;
			}
			else{
				y = z + x;
				x = z + y;
			}

			z = y + x;
		}
		else{
			z = z - z;
			x = x * z;

			if(x == y){
				y = z + z;
				z = x - z;

				if(z == z){
					x = x + y;
					y = x + y;
					x = z + x;
					y = x + x;
					x = y - y;
				}
				else{
					y = y + x;

					if(x >= z){
						y = y - y;
						y = x + x;
						x = z - y;
						y = y * z;
						x = y - x;
					}
					else{
						y = y + x;
						z = z + z;
					}

					x = z - y;
					z = y + x;
				}

				y = z + x;
				z = z - z;

				if(y < y){
					z = x - z;

					if(z > x){
						y = x - z;
					}
					else{
						x = x + z;
						z = x * x;
						x = z * z;
						z = x + y;
						x = x * z;
						y = x * x;
					}

					y = x + x;

					if(x <= y){
						y = z - x;
						z = x + y;
						x = y + z;
					}

					x = z - y;
					y = z * x;
				}

				z = x * z;
			}

			x = z - y;

			if(x <= y){
				z = y * y;
				y = z + y;
				z = z + y;
				z = y - z;

				if(y == z){
					z = x * y;
					z = y * y;
					y = z - y;
					z = x - z;
				}
				else{
					z = y * y;

					if(x != z){
						z = y - z;
						y = z * x;
						z = x + x;
						x = z - y;
						z = y - z;
						z = z * y;
					}

					x = z - z;
					z = y * z;
					x = z - x;
					y = x * x;
					x = y * y;
				}

				y = z - z;

				if(x >= x){
					x = x * z;

					if(y == z){
						x = y * x;
						x = z * x;
						y = x + z;
						z = x + y;
					}

					y = x + x;
					x = x + z;
					x = x + z;
					x = x - y;
				}

				y = x - z;
			}
			else{
				x = x + z;
				z = z + x;
				y = x + y;

				if(z >= y){
					y = z * y;
					z = x * y;
					x = x - y;
				}

			}

			y = z + y;
			z = x * y;

			if(z > y){
				y = y + z;
				y = y + z;

				if(y == x){
					y = x - x;
					y = z + x;
					z = y - y;
					x = z * x;
				}
				else{
					z = x * y;
					y = y - y;
					y = z * x;
					y = z + x;
				}

				x = x + z;

				if(z != z){
					y = y - x;
					y = x * y;
					y = y + z;
					y = y * y;
				}

			}

		}

		y = z + z;

		if(z > z){
			x = x - z;
			z = x * x;

			if(y != x){
				y = y + z;
				x = z + z;
				z = x - x;
			}

			y = x * y;
		}
		else{
			x = y * x;
			y = z - y;
			z = x * x;
			x = z - z;

			if(y == y){
				x = z + x;
				y = z + y;
				x = y * z;
				x = x - y;
				z = x * y;
			}
			else{
				z = x + x;

				if(y < z){
					x = y - x;
					y = z - x;
					x = y + z;
					x = y * y;

					if(z > x){
						x = z + z;
						z = z * x;
						x = x - x;
						z = x - z;
					}

					z = y - z;

					if(x > y){
						x = x + x;
						x = x * y;
						y = z - x;
						x = y * x;
						y = x - x;
					}

				}
				else{
					x = y - z;
					x = y + x;

					if(x >= y){
						z = y + y;
						z = y - y;
					}

				}

			}

			x = x + x;
			y = x * y;

			if(z >= x){
				x = x + x;

				if(x >= x){
					z = y + x;
				}
				else{
					z = y - x;
					x = y - x;
				}

				y = x * y;
				x = z + x;
				z = y * y;
			}
			else{
				x = x + x;
				x = x + x;

				if(z >= x){
					z = y - z;

					if(y != y){
						y = x - y;
						z = x - x;
						z = y + x;
						z = z * x;
						y = y * y;
						y = z + z;
					}

					z = y - z;
					x = z - y;
					x = y - y;
					z = x * z;
					z = y - z;
				}

				z = y - y;
				x = z - z;
			}

		}

		y = x + z;

		if(x != x){
			z = z * x;
		}

	}


	printf("%d", z);
}
