/* function auto-generated using seed: 4356283 */
void f59(int x, int y){
	int z = 1;
	z = y - x;

	if(y < z){
		z = x + z;
		y = y + x;

		if(x >= x){
			y = y * y;
			x = x * z;
			z = y - y;
			y = z * x;
		}
		else{
			y = z + z;
			z = y + z;
			y = z - z;
			x = z * z;
			x = y - x;
		}

		y = z - z;
		y = x * y;
		z = z - y;
	}

	y = z - x;

	if(x != y){
		y = z + z;
		z = z + y;
		x = y * y;

		if(z == x){
			x = x * y;
			z = x - y;
			x = z + y;
		}
		else{
			z = y + z;
			y = y * y;
			z = z * z;
			z = x - z;
			x = z - z;

			if(x >= x){
				z = z + z;
				z = y - z;
				z = y * y;
			}

			y = x * z;

			if(x < x){
				z = z * z;
				z = y + x;
				x = z - z;
			}
			else{
				x = x * z;

				if(x >= z){
					y = y - x;
					x = x + z;
					y = y - z;
				}
				else{
					y = x + z;
					x = y + y;
					y = z - z;
					z = y + x;
				}

			}

		}

	}
	else{
		y = z + x;

		if(x < x){
			x = z + x;
			z = y + y;

			if(x != y){
				x = x * y;
				y = x - z;
				y = x * x;

				if(y != z){
					x = y - x;
				}

				x = x * y;
				z = x - z;
			}
			else{
				y = z - x;
				z = z - y;
			}

			y = z - y;
			x = x - x;

			if(z != y){
				z = y - y;
				z = z - y;

				if(x <= z){
					x = z - z;
				}
				else{
					z = y + y;
					x = x - y;
					z = z - y;

					if(x < y){
						y = z * z;
						x = y * x;
						z = z * x;
						z = y * x;
						z = x * x;
					}
					else{
						z = z * z;
						z = x + y;
					}

					y = x + y;
				}

				x = z - z;
			}

			z = x + y;

			if(y >= z){
				x = z - y;

				if(y > z){
					z = y * y;
					x = x + y;
				}

			}
			else{
				z = x * x;
				x = z + y;
			}

			y = x + x;
		}

	}

	x = z - x;

	if(y <= y){
		z = y - y;
		z = y * y;

		if(y < y){
			y = x * y;

			if(z > x){
				z = z - y;
				x = z - y;
				x = y + z;
				x = y * x;
				z = x * z;

				if(z < y){
					y = z - z;
					z = x * y;
					y = y - y;

					if(x < z){
						x = y + x;
						x = z - y;
						z = y - z;
						z = y + y;
					}

					z = z - y;

					if(z != y){
						z = z - x;
						z = y * y;
						y = z * z;
						x = x * y;
						z = z + z;
					}
					else{
						y = z * x;
					}

					z = x * z;
					z = y - y;
				}
				else{
					y = z - z;
					x = x * y;
					x = x - y;

					if(x >= y){
						x = y + z;
					}
					else{
						y = z + z;
						z = z + y;
						z = z * x;
						x = x * y;
						z = x + x;
						z = y * x;
					}

					y = z + z;
				}

			}
			else{
				y = z + x;

				if(x >= x){
					z = x - y;

					if(y != z){
						z = y * x;
					}

					x = z + y;
					z = x + y;
					y = y - y;
					y = z + z;
					z = z + x;
				}

				x = y * z;

				if(y != z){
					y = x * y;
					z = z * z;
					x = x * z;
					y = y * x;
				}
				else{
					y = x + z;
					z = y + z;
					z = z + x;
					z = x * z;
					z = x + x;
				}

				x = z + y;
				x = y - x;

				if(y <= x){
					x = y * z;
					x = z - z;
				}
				else{
					x = z + z;
					x = y * x;
					y = z * x;
				}

			}

			x = z * y;
			z = z + x;
			z = y + y;
		}
		else{
			y = z + z;

			if(z == x){
				x = z - z;

				if(x < x){
					x = y * z;

					if(z > y){
						y = x - x;
						x = x + y;
						x = y + y;
						z = x + x;
					}

					x = z * x;
				}
				else{
					x = x * x;
				}

				z = y - y;
				x = z + z;
				y = z - z;
			}

			y = z - x;
			y = z - x;

			if(z <= x){
				y = y + x;
				y = z + z;
				z = z * y;
			}

			z = y * z;
			z = x - z;

			if(x <= y){
				y = x * y;
			}

		}

		y = x + z;
	}
	else{
		y = y - y;

		if(x > y){
			z = y * z;
			y = y * y;

			if(x > x){
				x = y - y;
				x = z + z;

				if(z != x){
					y = y * y;
					x = x - z;
					x = y * z;
					x = y + y;
					y = x * x;
					z = x + y;
				}

			}
			else{
				x = y - x;
				y = z - y;
			}

			y = z + z;

			if(x <= z){
				x = x + z;
				z = y * z;
				x = z * y;
				z = x - z;

				if(z < z){
					x = y * z;
					x = z * x;

					if(z < x){
						z = x + x;
						x = y * y;
						z = y - x;
						y = z - z;
						x = z - z;
						y = z + z;
					}
					else{
						z = y + x;
						z = x + x;
						y = x * z;
						z = z - x;
						x = y + y;
						y = z + y;
					}

					z = z + y;
				}
				else{
					x = y + z;

					if(x != z){
						z = x * z;
						x = x * y;
						y = z - z;
						y = z + z;
					}

				}

				y = y + z;
				y = z + x;

				if(z > z){
					z = x - y;
				}
				else{
					z = z - y;
					y = y - z;

					if(z > x){
						y = z * x;
						x = z + y;
						x = z * x;
						x = x - x;
						y = y * z;
						x = z + x;
					}

					y = x * x;
					z = z - y;
				}

			}
			else{
				y = y + z;

				if(y <= z){
					z = x - z;
					y = y + x;
					y = y * x;
				}

				x = y + x;
			}

			y = z - x;

			if(y == y){
				z = x * z;
				x = y + y;
				z = x - z;
				x = x + z;
			}
			else{
				y = x - z;
				x = z + y;
			}

			x = y + z;
		}
		else{
			x = x * x;
			x = z - y;
			x = y * z;
		}

		y = z + x;
		y = x + x;
	}

	z = y * y;

	if(y <= x){
		x = z + x;
		x = x - y;

		if(x < z){
			y = x + z;

			if(y != y){
				x = y * z;
			}

			z = z * z;
		}

		y = x * x;
		y = z + z;

		if(y == x){
			y = z + x;
			z = y - y;
			x = z + y;

			if(z > x){
				z = x + y;
				x = y * z;
			}
			else{
				x = z + x;
				x = y + y;
				x = y - z;
			}

		}

	}
	else{
		x = z - x;
		y = x - y;

		if(z >= x){
			x = x + z;
			z = z + y;

			if(z > y){
				z = z * x;

				if(x == x){
					x = z - x;
					z = x * x;
					z = y + y;
					x = z - z;
				}
				else{
					x = z + y;
				}

				y = x + z;
				y = y * y;
			}
			else{
				x = x * x;
				z = z - x;
				x = y - z;
				x = z - y;
				x = y + x;

				if(z <= y){
					y = y - z;
					x = x - x;

					if(x > z){
						y = y + z;
						z = x + y;
						y = x * y;
					}
					else{
						z = z + y;
					}

				}

			}

			y = x * z;
			z = z + z;

			if(z >= z){
				x = x + z;
				x = y + x;

				if(z != y){
					y = z * x;

					if(z <= z){
						x = z - z;
						y = z + z;
						z = z - y;
						x = y + z;
						z = x * x;
						y = x + y;
					}

				}
				else{
					z = y + y;

					if(y >= z){
						x = y + x;
					}
					else{
						x = x + y;
					}

					y = y - z;
					x = z + z;
					x = y - y;
					y = x * x;
					y = z + z;
				}

				x = x - z;
				y = y - x;
				z = z - z;
				z = z * z;
			}

		}

		y = x * y;
		y = z - z;
		x = y * z;

		if(x <= x){
			x = x * y;

			if(y <= x){
				y = y * y;
				y = x - x;

				if(z <= y){
					y = y - x;
					y = z * z;
				}

				y = z - y;

				if(y > x){
					x = y - x;
					x = y - z;
					y = x * y;
				}

				y = y * z;
				x = x * z;
				y = x + y;
			}
			else{
				y = x - x;

				if(z >= z){
					x = z + x;
					z = y - x;
				}
				else{
					y = z * y;
					x = z + y;

					if(z > z){
						x = y * z;
						x = y - y;
					}

					z = y - z;
					x = z * y;

					if(y < z){
						y = x + z;
						y = x + z;
						x = x + y;
						y = y * x;
						z = x * y;
					}
					else{
						z = x * y;
						x = y + y;
						x = z * x;
					}

				}

				x = x * y;

				if(x <= z){
					z = y * z;
					x = x * y;
					z = y * x;
				}

				y = z * x;

				if(x < z){
					x = z - x;

					if(x != x){
						z = x * z;
						x = x - z;
						z = x * z;
						x = x * z;
						y = z * x;
					}
					else{
						x = x - y;
					}

					y = z + y;
					x = x * y;

					if(x >= y){
						x = x + y;
						x = x + y;
						x = x - y;
						z = y * x;
						z = y + z;
						z = z + z;
					}

					z = z + y;
				}

				y = z + x;
			}

			y = x - x;
		}
		else{
			y = x * y;
			x = y * z;
			y = z * y;
		}

		z = x + y;
	}

	x = x + z;

	if(x == y){
		z = z * x;
		x = x * y;
		y = z * z;
	}


	printf("%d", z);
}
