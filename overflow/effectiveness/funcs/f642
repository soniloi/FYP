/* function auto-generated using seed: 574951 */
void f642(int x, int y){
	int z = 1;
	x = x - y;

	if(z > z){
		z = y + y;
	}
	else{
		z = x - x;
		y = z - z;
		y = x + x;
		y = z + z;
		y = x - x;
		y = z - z;

		if(z == x){
			x = x * z;

			if(y > x){
				z = y * z;
				y = y * x;
			}

		}

	}

	y = y + y;

	if(x != z){
		x = x * x;
	}


	printf("%d", z);
}
