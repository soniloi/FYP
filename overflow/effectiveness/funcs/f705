/* function auto-generated using seed: 7311729 */
void f705(int x, int y){
	int z = 1;
	y = x - x;

	if(x != z){
		z = y * z;
		x = x - y;
		x = x - z;

		if(z > x){
			z = x + x;
			x = y + x;
			x = z + z;
			x = x + z;
		}
		else{
			y = y + x;
			y = x + z;

			if(z <= x){
				z = x + z;
				x = z + x;
				z = y - x;
				x = x * x;

				if(x <= y){
					x = y * y;
					x = z + x;

					if(y >= y){
						x = x * x;
						x = y + y;
						x = x - x;
					}
					else{
						y = y * y;
						x = x + x;
						z = x - z;
					}

					y = z + x;
					z = y + x;
					y = x - y;
					y = x - y;
				}

				y = z * z;
			}

			z = z + y;
			z = y * y;

			if(z == x){
				z = x + x;

				if(y > x){
					y = z + x;
					y = x - x;

					if(y < z){
						y = y - y;
						x = x - y;
						x = z + z;
						y = z * x;
						y = y - y;
					}

					y = z + x;
				}
				else{
					x = y + z;
					x = x + y;
					y = y * y;
					y = x + z;
					y = y - x;
				}

				y = y * x;
				x = x - y;
				z = z - y;
				x = y * x;
				x = z + x;

				if(y > x){
					x = y * x;
					x = x * x;

					if(y <= z){
						x = y - z;
						z = x + y;
						z = y - x;
						y = y - x;
						y = y + z;
					}

					z = y + z;
					z = y - z;

					if(y < x){
						y = z + z;
					}

					y = x * x;
				}

			}
			else{
				y = x - x;
				z = z * y;
				z = y + z;

				if(x > z){
					y = y - z;
				}

				z = x + x;
			}

			y = y + x;

			if(x >= x){
				x = y * z;
				z = y * y;

				if(y <= z){
					z = x * z;
					z = x * z;
					x = z + x;
					y = x + y;
					z = x - y;
				}

				z = x - y;
				x = y * y;
				z = x + x;
				z = y * y;
			}

		}

		y = z - z;
		z = y + z;
	}

	y = y * z;

	if(y <= x){
		y = z * x;
	}
	else{
		y = y * y;

		if(z <= y){
			y = z + z;
			y = y - z;
			x = y - z;

			if(x < z){
				y = x * z;

				if(x != z){
					z = x * y;
					z = z - x;
					z = y + z;
					z = x * y;
					z = x + y;
					y = z - z;
				}
				else{
					x = x + y;
				}

				x = y + x;
				x = y + x;
				y = z * x;
			}

		}
		else{
			y = z + x;
			x = z + y;
			y = y * x;
		}

		z = y - z;
		x = z - x;
	}

	x = z + z;

	if(z < y){
		y = x - z;
		z = x * y;

		if(y <= x){
			x = y - y;
			y = x - x;
			x = z + x;
		}

	}
	else{
		y = y - z;

		if(x < x){
			y = z * x;
			y = x * x;
			z = z + y;
		}

		x = y - y;

		if(z <= y){
			y = x - z;
			y = z * z;

			if(x >= y){
				x = z * x;

				if(x <= y){
					z = y + x;
					y = y * x;
					y = z * x;

					if(z <= y){
						y = x + z;
						z = x - z;
					}

					x = z - z;
				}
				else{
					y = z * y;
					z = y + x;

					if(z >= y){
						x = y - z;
						x = x + x;
						x = z * y;
					}

					y = z - y;

					if(y == z){
						y = y + y;
						y = x * y;
					}

					y = z - y;
				}

			}

			y = y - x;
		}
		else{
			x = z - y;
			x = x * z;

			if(x != z){
				z = z * y;
			}

		}

		x = z * z;
		x = y * y;
		y = x + z;

		if(z <= z){
			y = x - y;
			y = y + y;
			x = z + z;
			y = y + z;

			if(z <= x){
				z = z + z;

				if(x <= x){
					x = x + z;
					y = y * y;

					if(y <= y){
						z = z - x;
						z = x * y;
					}

					z = y + x;
					z = y * y;
				}
				else{
					x = x + z;
					z = y + z;
					x = z * y;
				}

				z = z * y;
				y = y + x;
				y = z * z;
				x = z + x;

				if(z >= x){
					z = x - y;

					if(z < x){
						z = z + y;
						x = x + y;
						z = y * x;
						z = y - x;
						y = x * z;
					}

				}
				else{
					x = y * z;

					if(x != y){
						z = x * y;
						z = x * x;
					}

					x = x * y;
					y = x + z;
					x = x * y;
					x = y * z;
				}

				y = y + z;
			}

			y = z + x;
			z = y + y;
		}

		x = x - x;

		if(x == y){
			y = z + y;

			if(z != z){
				z = z + x;
				z = y + y;
				z = x * y;
				x = x + y;
				x = x + x;
				x = x + z;

				if(y == x){
					x = x + z;

					if(y != z){
						z = x * x;
						y = z + y;
						x = y * y;
					}
					else{
						z = x - z;
						y = z * x;
						z = z + y;
						z = z + x;
						z = x - z;
					}

					z = z * y;
					x = x - y;
					x = y + y;
					z = z + y;
				}

			}

			z = y * z;
			x = x * y;
			y = y + y;

			if(z == z){
				y = x - y;
				x = z * y;
			}

			x = z - x;
			x = z * x;
		}
		else{
			y = y + z;
			z = z * x;
		}

	}

	y = z - z;

	if(x == z){
		z = y - z;

		if(y >= z){
			y = z + x;
			x = y - x;

			if(y == z){
				z = z - z;

				if(x >= z){
					z = y - x;
					y = y - x;

					if(y < y){
						z = z + x;
						y = x * z;
						x = y + x;
					}

				}

			}

			y = x - y;
		}
		else{
			z = y - y;
			z = z + x;
			y = y + z;
		}

		y = y + y;

		if(y >= y){
			z = x + z;
			x = z * x;

			if(x < x){
				y = z * y;
				y = y * x;
				y = z * x;
				z = z + z;
				z = z - x;

				if(z == z){
					z = z * z;

					if(z > x){
						y = x - y;
					}
					else{
						y = z - y;
						z = z - x;
						x = x + y;
					}

					z = y - z;

					if(z < y){
						z = x - x;
						y = z - x;
					}
					else{
						y = z + y;
						y = z + y;
						z = z - y;
						y = z - z;
						y = z * z;
					}

				}
				else{
					y = y + z;
					x = y - y;
				}

			}

		}

		y = x + x;

		if(x < x){
			z = y * x;

			if(x != z){
				z = z * x;
				y = y + z;

				if(x == y){
					x = z * x;
				}
				else{
					z = z + z;
					z = z * z;
					x = y - z;
					y = y * x;
					x = y - y;
				}

				y = z - z;
			}
			else{
				z = z - y;
				x = x - z;
				x = x + x;

				if(z > x){
					z = x - y;
					x = y - x;
				}

			}

			x = y + y;

			if(x == z){
				x = x + y;
			}

		}
		else{
			z = x * x;
			z = y + y;
			z = z * x;
		}

		z = z - z;
		z = y - y;
		z = x - z;
	}


	printf("%d", z);
}
