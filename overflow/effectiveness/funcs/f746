/* function auto-generated using seed: 6731056 */
void f746(int x, int y){
	int z = 1;
	x = y + x;

	if(y != z){
		y = y + x;
		z = z * y;

		if(z < x){
			z = z - z;
		}

		y = y - z;
		x = x * z;
	}
	else{
		y = y * y;
		y = y - x;
		y = y + y;
		y = z + z;
	}

	y = x - x;

	if(y != y){
		x = y - z;
	}

	z = y + z;

	if(y < x){
		z = y * z;
		x = y * y;
		y = y + y;
		y = z * y;
	}


	printf("%d", z);
}
