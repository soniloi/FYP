/* function auto-generated using seed: 295400 */
void f803(int x, int y){
	int z = 1;
	x = x * x;

	if(z == x){
		x = x + x;
		z = z - z;
		x = y * x;
		x = z - z;
		x = x + x;
	}
	else{
		z = x - y;

		if(z >= z){
			x = z * z;
		}
		else{
			y = y * x;
			y = y + z;

			if(z == z){
				y = z + z;
			}

			y = z * z;
			z = y - y;
		}

	}


	printf("%d", z);
}
